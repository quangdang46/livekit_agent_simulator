apiVersion: agent-sim/v1
kind: Scenario
metadata:
  id: smoke-hello
  locale: en-US
  tags:
  - smoke
persona:
  name: Alex
  brief: You are calling a business for the first time. You just want to confirm you reached the right
    place, then politely end the call.
  goals:
  - Hear the agent's greeting or response
  - Say you will call back later
  style: polite, brief
execute:
  max_turns: 2
  timeout_s: 90
  first_speaker: user
# Contract path (caller_contract single path): caller_steps drive
# ContractCallerDriver; persona/caller/telepathy blocks below unchanged.
caller_steps:
  - say: "Hi, did I reach the right place?"
  - say: "I'll call back later. Thanks, bye."
  - end: true
pass_criteria:
  criteria:
  - The agent responded to the caller
  - The agent responded in the caller's language

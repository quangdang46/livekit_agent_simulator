# Plan — Rust full port of `livekit-agent-simulator` (lks)

> Implements: `src/livekit_agent_simulator_rust/` (exists, empty). Research: `docs/rust-port-research.md` (companion; every crate/API claim below cites it). **Revision 3 (2026-08-13) — consolidated single document, with Revision 3a corrections from a repo-ground-truth audit:** this plan is self-contained; an implementer needs nothing else (no Python source, no separate appendix files). The web-layer spec, behavior constants, metric formulas, event-kind contracts, wire protocols, and per-phase enrichment + edge-case catalog are all inlined in §Appendices below. Revision 2 corrections are preserved and marked REVISED: cross-checked against the Python ground truth (21 MCP tools, 23 CLI commands — 22 data + `mcp`, 25.1 kLOC / 105 files, 16 kHz conversation.wav, `publish_dtmf` API) and the crate sources (gemini-live `SpeechConfig` has no `language_code`; reconnect policy differs from Python by design). **Revision 3a: the `metrics` block target is the current 36-key set from `metrics.py` (audio-onset keys are unconditional); the R5 fixture `114-people-pleaser-refuse-card-20260809-201652-8b32` is not in this repo and must be re-captured; the MCP `compare_runs` tool surface is the 4 gate params + `baseline`.** See `rust-port-appendices/review-verify-20260813.md`. Appendix B (behavior constants) was reconstructed from Python ground truth (`config.py`, `metrics.py`, `event_writer.py`, `sqlite_store.py`, full event-kind extraction) because the on-disk B appendix file was missing.

## Summary (read this first)
- **You asked:** Write the definitive research doc + full-port implementation plan for rewriting the Python package `livekit-agent-simulator` (lks) in Rust. Research is done → `docs/rust-port-research.md` (written this date, 2026-08-13); this plan seals the implementation.
- **What is going on:** The Python package (`src/livekit_agent_simulator/`, ~25.1 kLOC incl. subdirs, 105 files) is an MCP + `lks` CLI tool that dials any LiveKit voice agent using `.agent-sim/` in a target repo: config, scenarios, a realtime model caller (Gemini Live / OpenAI Realtime), a scripted-cue caller, a forensic report writer (events.jsonl + runs.sqlite + summary.json), a web report player, an MCP server (21 tools), suite/compare/optimize/evals. The Python SDK chain (`livekit`, `livekit-api`, `google-genai`, `aiosqlite`) is heavy (uv venv, libwebrtc via `livekit[rtc]`), install is `pipx`-free but env-bound, and cross-platform packaging ships a zip with a wrapper (`install.sh`/`install.ps1`). The Rust rewrite targets a single static binary with embedded web assets, native-speed audio/PCM, and identical on-disk artifacts.
- **We recommend:** A Cargo workspace rooted at `src/livekit_agent_simulator_rust/` organized as **crates/**, split at the Python dependency seams — **REVISED (2026-08-13, fine-grained)**: the earlier single-crate D1 is superseded. Six crates: **`lks-core`** (pure logic, no livekit/pyo3: config, scenario, script models/parse/verify/summary, metrics, asserts, suite, evals, optimize, persona/behavior, logging/sqlite, and the `CallerBridge`/`SimLeg` traits — so P0/P1 tests build without libwebrtc), **`lks-livekit`** (everything media/network: room/dispatch/observer/sim_legs, callers gemini+openai, audio/*, script/runtime, run_orchestrator), **`lks-web`** (axum report + REST servers, cues pipeline, rust-embed assets), **`lks-mcp`** (rmcp server, 21 tools), **`lks-plugins`** (pyo3 embedded CPython for `.py` verify plugins), and **`lks`** (the binary: main.rs thin, cli/cli_render/ops glue). Dependency direction mirrors Python: `ops`/`web`/`mcp` sit on top of `run_orchestrator`/`preflight` (which import `livekit`), so `lks-web`/`lks-mcp`/`lks` depend on `lks-core` + `lks-livekit`. Vendored `gemini-live` fork and `agent-sim-proto` (prost-generated `AgentSessionMessage`) are workspace crates too. Rationale: config/scenario (P0/P1) iterate fast without building libwebrtc; `gemini-live` is a real third-party dependency and must be a crate, not a module. Decision recorded in §Key decisions D1.
- **Key crate choices (verified, research §1–§3):** `livekit 0.8.3` + `livekit-api 0.6.3` (realtime + server API; `rustls-tls-webpki-roots` feature), Gemini Live via **`gemini-live` 0.1.8 vendored/forked** (Candidate B; thin typed wrapper; hand-rolled tokio-tungstenite fallback) — REQUIRES a small fork patch (see D4/Open question 2; the crate's `SpeechConfig` lacks `language_code`), OpenAI Realtime hand-rolled on `tokio-tungstenite`, **`rusqlite` 0.40 bundled** (not sqlx — fixed schema, sync writes fit the per-event flusher), **`rmcp` 3.1.2** for MCP (official rust-sdk), **`rust-embed` 8.12** for `web/dist` (~200 KB), `axum 0.8` for the report/API server, `serde`/`yaml_serde`, `clap 4.x`, `tokio 1.x`, `hound` (WAV), `jiff` + `chrono-tz` (datetime parity), `pyo3 0.27` for plugins (P8).
- **Status:** Plan sealed. P0–P10 phased; P0–P4 are the report-parity core (P3 is the parity gate where Rust can read/write what Python reads/writes and vice versa; P3.5 is the web layer). Manual live gates (need real LiveKit + Gemini keys) are marked MANUAL and are not CI gates; every phase has an offline deterministic gate that CI runs.
- **What is CUT from v1** (AGENTS.md no-dead-features): `lks optimize` persona-prompt optimizer is ported last (P9) and only if the MCP tool + CLI command surface is exercised by a real user flow (the Python MCP tools list is the contract — see Non-goals 6/7 and Phase P9); `evals` (custom judge backends) ported at P7 with `http_anthropic`/`http_openai`/`gemini` backends only (research §2.2: google-genai Rust Live unverified — judge REST calls are standard HTTP, no gap); plugins (P8) run the *existing* `.py` verify plugins unchanged via **embedded CPython (pyo3)** — no plugin-source rewrite; Windows release binary deferred (research §1.6 VERIFY) with Python as the documented Windows path until proven.

---

## Non-goals (do not do in this port)

1. **No behavior redesign.** The Rust port is a *behavioral* port with byte-compatible artifacts (events.jsonl, runs.sqlite, summary.json, meta.json) and config/scenario compatibility. No new features, no scenario-schema evolution, no report-format changes. Python remains the reference implementation until P10; any divergence is a bug in the port (recorded as an issue with a pinned Python-side golden).
2. **No second language runtime in the hot path.** PCM handling, cue synthesis, VAD, event writing, sqlite — all native Rust. The only embedded interpreter is CPython via pyo3 for **verify plugins** (P8) — deliberately isolated behind a plugin loader; core never depends on Python.
3. **No sqlx.** Fixed-schema embedded DB with small per-event writes → `rusqlite` (bundled) is the sealed choice (§3.1 of research). No async DB layer.
4. **No LiveKit SDK audio feature attempt.** `livekit` has no `audio` feature (research §1.2); PCM handling is ours. Do not add one.
5. **No consumer keys in core.** Same hard rule as Python (AGENTS.md): credentials only in target `.agent-sim/config.yaml` (gitignored); `config_snapshot` redacts secrets (proven by R5 meta.json — only `url_host`, booleans, model names).
6. **No dead-feature surface.** Do not port CLI/MCP/config knobs that have no user flow. During porting, if a Python feature has zero tests and zero references in docs/templates (e.g. unused judge backends, half-implemented optimize knobs), drop it and record the drop in the phase's porting note; do not "port for completeness" (AGENTS.md no-dead-features rule).
7. **No dual-maintenance drift.** While both implementations exist, the Rust port is the *feature-complete* target; Python is frozen for bugfixes only after P3 (parity). No new features land in Python after P3 unless they are also ported in the same change.
8. **No legacy shims in Rust.** No `google_api_key` alias (config is the post-2026-08-06 schema: `simulator.provider`/`mode`/`api_key` — plan template PLAN-20260806-openai-caller.md §Migration); no `gemini_text` delivery naming (that rename is tracked as a Python-side migration; Rust implements the *current* schema only, see Open question 3).
9. **No streaming judge / transcription features.** LiveKit transcription text-stream plumbing is `VERIFY`-level in the Rust SDK (research §1.3); the port derives transcripts from audio + session-observer text streams exactly like Python's `Observer` (R4 `livekit/observer.py` consumes both `lk.transcription` text streams and `TranscriptionReceived`-style events), so no new SDK feature is required. Do not build on unverified SDK events. (REVISED: R4 `observer.py` registers `register_text_stream_handler("lk.transcription", …)` and reads attrs `lk.transcription_final`/`lk.segment_id`; Rust `TextStreamOpened` carries the same topic + attributes — the dual-path note below stands.)
10. **Windows release binary not promised in v1** (research §1.6 VERIFY). `install.ps1` adaptation is a P10 *documented* item, gated on a Windows CI build; if not proven, `install.ps1` keeps installing the Python zip until a Rust Windows binary exists. Rust code must still compile-check on Windows (CI `cargo check` job) so the future switch is cheap.

---

## Invariants (must hold after every phase; each is enforced by a golden/CI test from P1 on)

| ID | Invariant |
|----|-----------|
| I1 | **Report format byte-compatible.** `events.jsonl` envelope identical: field order `event_id, seq, run_id, turn, kind, ts, ts_mono_ms, datetime_utc, datetime_local, source, parent_event_id, spec` (dialogue snapshot injected before `spec` when `include_dialogue` — R4 `event_writer.py:108–123`); `event_id` = `evt_` + 12 hex (uuid v4 first 12 hex chars); `seq` monotonic from 1; `ts` epoch ms; `ts_mono_ms` = ms since run start (std::time::Instant origin), backdatable with `max(0, …)` clamp; `datetime_utc` ISO-8601 ms with `Z`; `datetime_local` ISO-8601 ms with tz offset (`chrono-tz`, default UTC); `spec` `{}` when none. `summary.json` key-for-key identical (R4 `metrics.py` key set — full **36-key** `metrics` block incl. `talk_ratio`, `agent_chars`, `user_chars`, `user_words_*`, `slow_turns_*`, `ttfa_run_ms`, `turn_taking_audio_ms`, `user_audio_source_count`, `agent_audio_onset_count` — REVISED from the 31-key Aug-09/11 ground truth, which predates the unconditional audio-onset metrics in commit `504577a`); `meta.json` shape identical (redacted). Verified by byte-diff golden tests against Python-written fixtures. |
| I2 | **runs.sqlite DDL byte-identical** (R4 `sqlite_store.py` SCHEMA: `runs`, `run_events` PK `(run_id,seq)` + `idx_run_events_kind`, `run_turns` PK `(run_id,turn)`; `run_events.payload_json` = FULL envelope JSON, not just spec) so Python `list_runs`/`get_run_log`/`compare_runs` read Rust-written DBs and vice versa. Cross-read test in P3. |
| I3 | **Config compatible.** `config.yaml` parsed with the post-2026-08-06 schema only (`livekit`, `simulator{provider, mode, api_key, language, voice}`, `observe`, `judge`, `project`, `cues`, `telephony`); `_require` fail-fast semantics preserved; defaults portable (`en-US`/`UTC`). Rust must reject unknown `provider`/`mode` values with actionable errors. |
| I4 | **Scenario compatible.** Both legacy `.jsonl` (agent-sim/v1) and canonical `.yaml` parse to identical `Scenario` structs; `convert` re-emits YAML from JSONL; golden tests against `templates/scenario-scaffold.*`, `smoke-hello.*`, `inbound-caller-sim.*`, `outbound-callee-sim.*`, `outbound-human-pickup.*` (R6). |
| I5 | **Black-box rule.** Core never interprets consumer-specific dispatch metadata keys (passed through as JSON strings, Python `Dispatch.metadata`). No consumer env vars in config schema. Test: `dispatch_metadata_set` snapshot flag semantics preserved (R5). |
| I6 | **Portable defaults.** Core defaults `en-US`/`UTC`; timezone for `datetime_local` comes from the same default as Python (UTC) unless target config overrides. No host timezone leaks into artifacts (byte-compat requires deterministic `datetime_local` — golden tests pin it). |
| I7 | **One clear API.** CLI commands, MCP tool names/params/JSON return shapes identical to Python surface (21 MCP tools, 23 CLI commands listed in §CLI/MCP surface — 22 data commands + the `mcp` subcommand; REVISED counts, verified against `mcp_server.py` `@mcp.tool` markers and `cli.py` `@app.command` decorators). No aliases. Enforced by a tool-surface golden test (P4/P5). |
| I8 | **Caller brain parity.** `simulator.provider: google` → Gemini bridge, `openai` → OpenAI bridge; event kinds `sim.gemini_*` / `sim.openai_*` emitted with identical spec fields; reconnect/transport-drop semantics ported exactly — with one deliberate, documented divergence: Python *never* reconnects mid-call (sets `transport_dropped`), while `gemini-live` auto-reconnects on both GoAway and ConnectionLost (REVISED — see D4 and Open question 2). |
| I9 | **SimLeg topology parity.** `caller.mode` (`webrtc_sim`, `inbound_sip`, `outbound_sim_callee`, `outbound_human_pickup`, `agent_dials`) selects the SimLeg (REVISED — the five-mode set from R4 `scenario.py`; the plan previously dropped `agent_dials`); `simulator.mode`/`provider` are simulator capabilities, never scenario overrides (PLAN-20260806-openai-caller.md I7). |
| I10 | **Same scenario → comparable artifacts.** Running the same scenario id under Python and Rust against the same target yields `compare_runs`-compatible reports (cross-validation strategy §Cross-validation). `compare_runs`/`compare --baseline` gate semantics identical. |

---

## Feature planning

### Architecture

**Workspace** (D1 — fine-grained crates): `src/livekit_agent_simulator_rust/`:

```
Cargo.toml            # workspace root: resolver = "2", members = ["crates/*"], rust-version = "1.97"
rust-toolchain.toml   # 1.97.1 (matches livekit/rust-sdks pin, research §1.1)
crates/lks-core/      # pure logic (no livekit, no pyo3): config, scenario*, script(models/parse/verify/summary),
                      # metrics, asserts, suite, evals, optimize, persona/behavior, logging/sqlite
                      # + CallerBridge/SimLeg traits. Builds in P0/P1 without libwebrtc.
crates/lks-livekit/   # media/network: livekit room/dispatch/observer/agent_session/sim_legs, callers
                      # (gemini+openai), audio/*, script/runtime, run_orchestrator. deps: lks-core, livekit, gemini-live
crates/lks-web/       # axum report server (8765) + REST API (8787) + cues pipeline + rust-embed web/dist. deps: lks-core, lks-livekit
crates/lks-mcp/       # rmcp server, 21 tools. deps: lks-core, lks-livekit (via ops)
crates/lks-plugins/   # pyo3 embedded CPython for existing .py verify plugins. deps: lks-core (P8)
crates/lks/           # binary: main.rs thin (clap → tokio::main → ops), cli/cli_render/ops glue. deps: lks-core, lks-livekit, lks-web, lks-mcp, lks-plugins
crates/gemini-live/   # vendored fork of gemini-live 0.1.8 (MIT, ~1400 LOC core) + the D4 fork patches
crates/agent-sim-proto/ # prost-generated AgentSessionMessage (+ build.rs, prost-build)
```

Legacy single-crate layout (below) is retained for reference only — **module paths move per the table in §Python→Rust module mapping** (e.g. `src/config.rs` → `crates/lks-core/src/config.rs`, `src/callers/*` → `crates/lks-livekit/src/callers/*`, `src/web/*` → `crates/lks-web/src/*`).

```
crates/lks/           # LEGACY single-crate view — now distributed across the crates above.
                      # Module → crate map: config/scenario*/script(non-runtime)/metrics/asserts/suite/evals/optimize/
                      #   persona_*/behavior_compile/logging/run/errors/paths/portable_layout/authoring → lks-core
                      #   callers/*, livekit/*, audio/*, script/runtime, preflight → lks-livekit
                      #   web/*, mcp/*, ops, cli, cli_render, main.rs → lks-web (web) / lks-mcp (mcp) / lks (ops, cli, main)
                      #   plugins/* → lks-plugins
  Cargo.toml          # name = "lks", bin "lks" + lib
  src/main.rs         # thin: clap parse → tokio::main → lib entry
  src/lib.rs          # pub mod … (library surface; bin re-exports)
  src/config.rs       # Config structs + load + _require semantics + redaction
  src/scenario.rs     # Scenario dataclass mirror (locked fields, §5.3)
  src/scenario_yaml.rs# YAML parser (canonical)
  src/scenario_jsonl.rs # legacy JSONL parser (agent-sim/v1)
  src/scenario_convert.rs # convert: JSONL → YAML re-emit
  src/scenario_from_run.rs # promote finished run → draft YAML (P5 — REVISED: moved from P6)
  src/scenario_from_dict.rs # in-memory dict execution (P4 — REVISED: moved from P6)
  src/script/mod.rs   # script models (agent_speaking/delay_ms/delivery/say/…)
  src/script/parse.rs # script_parse.py port
  src/script/runtime.rs # ScriptRunner: cue timing, barge, hold, mute, DTMF (LocalParticipant::publish_dtmf — REVISED)
  src/script/farewell.rs  # farewell heuristics (P3 — REVISED: moved from P6)
  src/script/hang_up_gate.rs # hang_up_gate.py port (P3 — REVISED: moved from P6)
  src/script/verify.rs # script_verify counters
  src/script/summary.rs    # script summary.md (P3 — REVISED: moved from P6)
  src/callers/mod.rs       # CallerBridge trait + factory
  src/callers/base.rs      # shared: mic publish, watch agent tracks, end_call, transport_dropped, play_pcm
  src/callers/gemini.rs    # Gemini Live bridge (vendored gemini-live)
  src/callers/openai.rs    # OpenAI Realtime bridge (hand-rolled WS)
  src/callers/end_call.rs  # end_call.py heuristics (P3 — REVISED: moved from P6)
  src/caller_nudge.rs  # nudge_caller_after_agent_greeting (P3)
  src/interrupt_rate.rs
  src/behavior_compile.rs  # caller policy compile (P2 — REVISED: moved from P6)
  src/persona_traits.rs    # prompt composition (P2 — REVISED: moved from P6)
  src/caller_policy.rs     # DefaultCallerPolicy + prompt_sections (P2 — REVISED: moved from P6)
  src/livekit/mod.rs        # room, dispatch, observer facade
  src/livekit/room.rs       # Room connect, events (livekit crate)
  src/livekit/dispatch.rs   # AgentDispatchClient + RoomServiceClient (livekit-api)
  src/livekit/observer.rs   # track audio + lk.transcription text streams → transcripts
  src/livekit/agent_session_observer.rs # lk.agent.session byte streams (prost decode — P3)
  src/livekit/sim_leg/mod.rs  # SimLeg trait + factory (5 modes — REVISED)
  src/livekit/sim_leg/webrtc.rs  # webrtc_sim leg (P2 killing test)
  src/livekit/sim_leg/inbound.rs  # inbound_sip (P6)
  src/livekit/sim_leg/agent_dials.rs  # agent_dials (P6) — REVISED: was mislabeled outbound_callee
  src/livekit/sim_leg/human_pickup.rs # outbound_human_pickup (P6)
  src/livekit/sim_leg/sim_callee.rs   # outbound_sim_callee helpers (P6)
  src/livekit/sim_leg/room_resolve.rs # room naming + prepare_ms (P6)
  src/audio/mod.rs
  src/audio/mic_mixer.rs   # ParallelMicMixer
  src/audio/local_recorder.rs # conversation.wav (16 kHz PCM16 stereo L=sim/R=agent — REVISED) + sim audio recorded events
  src/audio/pcm_cue.rs     # room_pcm cue WAV play + gain
  src/audio/cue_catalog.rs # builtin:voice.* / noise.* / legacy aliases + resolution
  src/audio/vad.rs         # RMS onset (agent audio) + silence detection
  src/audio/degradation.rs # DTMF-tone + dropout degradation effects (P3 — REVISED: moved from P6)
  src/audio/sapi_tts.rs    # sapi delivery (subprocess edge — see P3 note; non-Windows error parity)
  src/logging/mod.rs
  src/logging/event.rs     # EventKind enum (union of Python kinds) + envelope writer
  src/logging/sqlite.rs    # SCHEMA-identical DDL + RunStore
  src/logging/summary.rs   # summary.json writer
  src/logging/meta.rs      # meta.json writer (redacted config_snapshot)
  src/metrics.rs           # metrics.py key set (full 36-key metrics block — REVISED)
  src/run.rs               # run_orchestrator.rs port (phases, dispatch, end_condition, get_run_report + suspicious_turns — P3)
  src/asserts.rs           # asserts.py port (P4)
  src/suite.rs             # suite.py port (evaluate_run_result, gate)
  src/ops.rs               # ops.py port: 21 MCP-mirror functions (sync + async)
  src/cli.rs               # clap app: 23 commands (22 data + mcp), --json, exit codes (0/1/130 — REVISED)
  src/cli_render.rs        # human renderers
  src/mcp/mod.rs           # rmcp server: 21 #[tool]s
  src/web/mod.rs           # axum app: static embed + /api/v1 routes (P3.5)
  src/web/report_server.rs # report UI server port 8765 (P3.5)
  src/web/api_server.rs    # REST API server port 8787 (P3.5)
  src/web/cues.rs          # cues.json + payload assembly (P3.5)
  src/web/report_time.rs   # report_time.py (P3.5)
  src/web/source_priority.rs # source_rank/texts_similar (P3.5)
  src/web/windows.rs       # utterance/active-window estimation (P3.5)
  src/web/transcript_cues.rs # raw/dedupe/ghost/timing (P3.5)
  src/web/markers.rs       # all marker kinds (P3.5)
  src/web/tool_events.rs   # tool spans + session summary (P3.5)
  src/web/speech_origin.rs # cue tagging + synthetic barges (P3.5)
  src/web/static_assets.rs # rust-embed map + package_web_dir (P3.5)
  src/evals/mod.rs         # judge backends + runner (P7)
  src/evals/backends.rs    # gemini / http_openai / http_anthropic
  src/optimize/mod.rs      # persona optimizer (P9)
  src/plugins/mod.rs       # pyo3 loader for existing .py verify plugins (P8)
  src/plugins/api.rs       # plugin API (before_run/after_run/verify)
  src/preflight.rs         # preflight.py (P2)
  src/paths.rs             # .agent-sim/ path resolution
  src/portable_layout.rs   # portable defaults validation
  src/authoring.rs         # authoring.py port (scaffolds, GUIDE text)
  src/errors.rs            # ConfigError / ScenarioError / RunError
  tests/                   # integration + golden tests (see §Test strategy)
  assets/web/              # web/dist copy embedded via rust-embed (build-time)
```

**Python → Rust module mapping** (every Python module accounted for; evidence: repo layout, `wc -l` ~25.1 kLOC — REVISED, research R4). Module paths below are crate-relative; **crate ownership** is fixed by the block map at the legacy layout above (lks-core / lks-livekit / lks-web / lks-mcp / lks-plugins / lks).

| Python module | Rust module | Notes / evidence |
|---|---|---|
| `config.py` | `config.rs` | Schema §5.2; `_require` fail-fast; redaction; Appendix A |
| `scenario.py` | `scenario.rs` + `scenario_jsonl.rs` | Locked fields §5.3; legacy JSONL; CALLER_MODES 5-value set |
| `scenario_yaml.py` | `scenario_yaml.rs` | Canonical YAML; templates `scenario-scaffold.yaml` etc. |
| `scenario_from_run.py` | `scenario_from_run.rs` | Draft YAML from a finished run (P5) |
| `scenario_from_dict.py` | `scenario_from_dict.rs` | In-memory dict execution (P4) |
| `script/` (models, parse, runtime, verify, summary, farewell, hang_up_gate) | `script/*` | Cue timing; barge; hold; mute; verify counts; DTMF via `publish_dtmf` |
| `script_parse.py` | `script/parse.rs` | Script step schema §5.3 |
| `script_runner.py` | (re-export; no Rust counterpart — stable import path only) | AGENTS.md layout note |
| `run_orchestrator.py` | `run.rs` | run_id (`{NNN}-{slug}-{YYYYMMDD}-{HHMMSS}-{xxxx}`), seq from report dirs, phases, dispatch, end_condition, `sim.leg_error` mapping (R4:287), get_run_report + suspicious_turns (P3) |
| `livekit/adapter.py` | `livekit/mod.rs` | wraps livekit-api clients; build_token/isolate_sip_handset/update_subscriptions etc. |
| `livekit/observer.py` | `livekit/observer.rs` | `lk.transcription` text streams + AudioStream(16k) → transcript events |
| `livekit/agent_session_observer.py` | `livekit/agent_session_observer.rs` | `lk.agent.session` byte streams (register_byte_stream_handler; prost decode) |
| `livekit/sim_leg/*` (webrtc, inbound, agent_dials, human_pickup, sim_callee, room_resolve, factory, protocol, errors) | `livekit/sim_leg/*` | P2 webrtc_sim; P6 the rest |
| `callers/` (base, gemini, openai, factory, end_call) | `callers/*` | P2 killing test is gemini bridge; I8 |
| `caller/` (default_policy, policy, prompt_sections) + `persona_traits.py` | `caller_policy.rs` + `persona_traits.rs` | deterministic string composition, golden-testable (P2) |
| `caller_nudge.py` / `interrupt_rate.py` | `caller_nudge.rs` / `interrupt_rate.rs` | P3 |
| `audio/` (cue_catalog, degradation, local_recorder, mic_mixer, pcm_cue, sapi_tts, vad) | `audio/*` | hound WAV; rubato/soxr resample (research §3); sapi_tts = Windows SAPI subprocess (P3 note) |
| `logging/` (event_writer, sqlite_store) | `logging/*` | I1/I2; byte-identical envelope + DDL |
| `metrics.py` | `metrics.rs` | key set frozen (R4); formulas in Appendix B |
| `asserts.py` | `asserts.rs` | P4 |
| `suite.py` | `suite.rs` | evaluate_run_result + gate |
| `web/` (server, api, cues, markers, report_time, speech_origin, transcript_cues, tool_events, cue_helpers/*) | `web/*` | axum + rust-embed; P3.5; routes in P3.5 spec |
| `mcp_server.py` | `mcp/mod.rs` | 21 tools; rmcp 3.1.2 |
| `cli.py` + `cli_render.py` | `cli.rs` + `cli_render.rs` | 23 commands (22 data + mcp); clap |
| `ops.py` | `ops.rs` | 21 MCP-mirror ops + compare gate |
| `evals/` | `evals/*` | P7 |
| `optimize/` | `optimize/*` | P9 |
| `plugins/` | `plugins/*` | P8 (pyo3) |
| `preflight.py` | `preflight.rs` | P2 |
| `behavior_compile.py`, `paths.py`, `portable_layout.py`, `authoring.py` | `behavior_compile.rs`, `paths.rs`, `portable_layout.rs`, `authoring.rs` | behavior_compile at P2; rest early |
| `scenario_from_dict.py`, `scenario_yaml.py` | covered above | — |

**Dependencies (chosen crates + versions, evidence research §1–§3). Crate ownership: `livekit`/`livekit-api`/`tokio`/`tokio-tungstenite`/`hound`/`rubato`/`soxr-sys` → `lks-livekit`; `serde`/`serde_json`/`yaml_serde`/`thiserror`/`anyhow`/`uuid`/`jiff`/`chrono-tz`/`rusqlite`/`log`/`env_logger`/`rand` → `lks-core` (+ any crate needing them); `clap` → `lks` (binary); `rmcp` → `lks-mcp`; `axum`/`tower-http`/`rust-embed`/`tower` → `lks-web`; `pyo3` → `lks-plugins`; `prost`/`prost-build` → `agent-sim-proto`; `gemini-live` vendored at `crates/gemini-live`.**

| Crate | Version | For | Evidence |
|---|---|---|---|
| `livekit` | 0.8.3 (features `rustls-tls-webpki-roots`) | realtime room/tracks/data/text; `LocalParticipant::publish_dtmf(SipDTMF)` for DTMF cues (REVISED — API verified in R1 `local_participant.rs:783`) | research §1.1 |
| `livekit-api` | 0.6.3 | room/agent_dispatch/sip services + access_token | research §1.4 |
| `gemini-live` | 0.1.8 (vendored/forked) | Gemini Live bridge — REQUIRES fork patch for `language_code` + reconnect knob (REVISED) | research §2.3 (fallback §2.4) |
| `tokio` | 1.x (full) | runtime; SDK requires tokio | research §1.1 |
| `tokio-tungstenite` | 0.30 | OpenAI bridge + fallback WS | research §2.5 |
| `rusqlite` | 0.40 bundled (REVISED — was 0.37; 0.37 predates the current rusqlite line and tokio-rusqlite 0.7 pins `^0.37` but we use `spawn_blocking`, no wrapper) | runs.sqlite | research §3.1 |
| `rmcp` | 3.1.2 (server feature) | MCP server | research §3 |
| `rust-embed` | 8.12 (`debug-embed`, `compression`, `mime-guess`) | web/dist (~200 KB — REVISED, measured) | research §5.4 |
| `axum` | 0.8.9 (+ `tower-http` 0.6.11 features `fs`, `trace`, `catch-panic`; **not** 0.7.0 — breaking changes unneeded) | web + REST API | research §3, P3.5 |
| `serde`/`serde_json`/`yaml_serde` | 1.x / 1.x (preserve_order) / 0.10.x | config/scenario/artifacts (yaml_serde is the official YAML-org fork of archived serde_yaml — REVISED) | research §3 |
| `clap` | 4.x (derive) | CLI | research §3 |
| `thiserror`/`anyhow` | 2.x/1.x | errors | research §3 |
| `uuid` (v4) | 1.x | event_id/run_id hex | research §3 |
| `jiff` (+ `chrono-tz` if tz offsets beyond UTC are needed) | 0.2.35 | datetime_utc/datetime_local parity (REVISED: jiff strtime gives exact `%Y%m%d-%H%M%S` parity; `datetime_local` needs a tz-aware formatter — `chrono-tz` only, since run_id is pure UTC) | research §3 |
| `hound` | 3.5 | WAV read/write (cues, conversation.wav) | research §3 |
| `rubato` (or `soxr-sys` 0.1.3) | latest | resample room_pcm → 16 kHz / 24 kHz (REVISED: sim audio out is 16 kHz for the room; Gemini bridge needs 16 kHz in / 24 kHz out) | research §3 |
| `rand` | 0.9 | jitter/interrupt classes | research §3 |
| `log` + `env_logger` | 0.4/0.11 | logging | research §3 |
| `pyo3` | 0.27.x | plugins (P8) | research §3 |
| `prost` | 0.13.x | `lk.agent.session` proto decode (P3 — REVISED: explicit P3 dep) | research §3 |
| `reqwest` | 0.12 (rustls) | judge/evals HTTP (P7) | research §3 |
| `ctrlc` | 3.x | Ctrl+C in `lks web`/`serve` | research §3 |

### Key decisions (with rationale)

- **D1 — Fine-grained workspace, not a single crate (REVISED 2026-08-13).** Supersedes the earlier single-crate decision. The Python package is one importable unit, but it has hard seams: `ops.py`/`web`/`mcp` import `run_orchestrator`/`preflight` which import `livekit`. Splitting at those seams gives real wins: (1) config/scenario (P0/P1) build and test **without building libwebrtc** — the `livekit` crate pulls a C++ libwebrtc build, so a single-crate workspace forces the long first build on every test from P0; (2) `gemini-live` is a vendored third-party dependency with its own `Cargo.toml`/version — it belongs in `crates/`, not as a module of `lks`; (3) proto-generated types (`agent-sim-proto`) are codegen output best isolated. Workspace layout: `crates/lks-core`, `crates/lks-livekit`, `crates/lks-web`, `crates/lks-mcp`, `crates/lks-plugins`, `crates/lks` (binary), `crates/gemini-live` (vendored fork), `crates/agent-sim-proto`. Dependency direction mirrors Python's import graph: `lks-livekit` → `lks-core`; `lks-web`/`lks-mcp`/`lks` → `lks-core` + `lks-livekit`; `lks-plugins` → `lks-core`. The binary crate stays named `lks` for CLI parity. (Research §0/§4: no prior art exists — greenfield composition, so no ecosystem constraint forces any particular shape.)
- **D2 — tokio runtime, full features.** `livekit` requires tokio (research §1.1: "Currently, Tokio is required to use this SDK"); `gemini-live` and `rmcp` are tokio-native. `tokio::time` (cue timing), `tokio::process` (judge subprocesses, sapi TTS), `tokio::sync` (mpsc for the event writer) are all needed.
- **D3 — `livekit` + `livekit-api` for realtime + server.** No gap vs Python's `livekit.api` surface (research §1.4). TLS via `rustls-tls-webpki-roots` for a self-contained binary (research §1.2).
- **D4 — Gemini Live via vendored `gemini-live` 0.1.8 (Candidate B), fallback hand-rolled (Candidate C).** REVISED: the crate covers every wire feature (setup with realtimeInputConfig/activity handling, session resumption, GoAway, reconnect, transcript events, typed `ServerEvent`) but has **two known deltas vs Python**, resolved by vendoring + a ~2-field patch: (1) `SpeechConfig`/`VoiceConfig` has **no `language_code` field** — the Python caller sets `speech_config.language_code=voice.language`; add one serde `camelCase` field (`language_code`) to the vendored `SpeechConfig` (see Appendix D §1.2 — the patch goes on `SpeechConfig`, NOT `VoiceConfig`; this corrects an earlier note). (2) **Reconnect policy cannot distinguish GoAway (wanted) from mid-call ConnectionLost (Python deliberately does NOT reconnect)** — `ReconnectPolicy { enabled: true, max_attempts: Some(10) }` auto-reconnects on both. Patch knob (`~10 lines`, Appendix D §1.5): add `pub reconnect_on_drop: bool` (default false) to `ReconnectPolicy`; GoAway → always reconnect (inject resume handle); ConnectionLost → reconnect only if `reconnect_on_drop`, else emit `ServerEvent::Closed{reason}` + `SessionStatus::Closed` (Python parity: `transport_dropped=True`, no mid-call reconnect). Also patch `src/transport.rs:311-324` so `RawFrame::Close` carries the close code for the §1.6 classification table. Fallback if the fork patch surface grows: raw tokio-tungstenite client (~600–900 lines, Candidate C). OpenAI Realtime stays hand-rolled on tokio-tungstenite (Python precedent: `callers/openai.py` uses raw `websockets`; research §2.5).
- **D5 — `rusqlite` bundled, not sqlx.** Fixed DDL (3 tables) + small sync writes; sqlx's async + macro build step adds nothing (research §3.1). REVISED: use rusqlite **0.40** bundled behind a thin `spawn_blocking` wrapper (not tokio-rusqlite — it pins rusqlite `^0.37`).
- **D6 — `rmcp` 3.1.2 for MCP.** Official SDK; `#[tool]` macros; MCP spec 2026-07-28. Tool surface parity is a golden test, not a code-gen concern.
- **D7 — Report format reuse (byte-compatible).** Full envelope/DDL/summary parity per I1/I2, from R5 ground truth + R4 sources (research §5.1).
- **D8 — Web embed via rust-embed 8.12** with `debug-embed` so `cargo run` and the release binary both serve `web/dist`; axum serves static + `/api/v1` (research §5.4). Web player JS unchanged.
- **D9 — JSONL scenario format reuse.** Both legacy `.jsonl` and `.yaml` parsed to identical structs; `convert` re-emits YAML (research §5.3). Golden tests from `templates/`.
- **D10 — Plugin strategy: pyo3 embedded CPython running the existing `.py` verify plugins unchanged** (research §3). Deliberate: target repos have `.agent-sim/plugins/*.py` files that call `livekit_agent_simulator.plugins.api` — rewriting them in Rust is impossible (they are consumer Python). pyo3 0.27 embeds a Python 3.11+ interpreter into the binary; the plugin API is a thin bridge. Risk-mitigated: isolated behind `plugins/` module; core never imports Python. If pyo3 is unacceptable at P8 review, fallback is documented in P8.
- **D11 — Gemini wire is snake_case from Python's SDK; vendored crate emits camelCase.** Server accepts both (verified); field presence is what matters — do not chase byte parity on key casing (Appendix D §3).
- **D12 — Web range requests: keep tower-http real Range support.** Python sends `Accept-Ranges: bytes` but ignores Range (always 200 full body). The bundled UI never issues Range requests; tower-http's 206 partial responses are a strict superset that never breaks the UI — documented divergence in the porting note (P3.5).

### CLI/MCP surface (frozen contract — I7)

- **MCP tools (21 — REVISED, verified count of `@mcp.tool` markers in `mcp_server.py`):** `guide, web, init_project, preflight, list_scenarios, list_plugins, list_cues, validate_scenario, export_scenario, init_scenario, convert_scenario, execute_scenario, optimize_persona, execute_scenarios, execute_scenario_dict, scenario_from_run, get_run_status, get_run_log, get_run_report, compare_runs, list_runs` (research §5.5). No 22nd tool exists.
- **CLI commands (22 — REVISED, verified against `@app.command` decorators in `cli.py`):** `init, guide, web, serve, preflight, scenarios, cues, plugins, validate, export, convert, scenario-init, execute, execute-all, execute-dict, optimize, status, log, report, compare, runs, scenario-from-run` + the `mcp` subcommand (a 23rd command). There is **no `version` command** — `lks --version` comes from typer's built-in flag (clap gives it via `#[command(version)]`). The CLI has no hidden `--as-json` flag; the flag is `--json` (typer Option string), with the Python parameter named `as_json` internally to avoid shadowing the stdlib — Rust needs only the `--json` flag.
- **Exit codes (REVISED — verified `cli.py` `_run` helper):** 0 success, 1 failure/gate-fail/ConfigError/ScenarioError/RuntimeError, **130 on Ctrl+C** ("Interrupted — stopping."), not 2. No code-2 usage errors.
- **REST API (`web/api.py`):** REVISED — the surface is the full 10-route set under `/api/v1`: GET `/health` (→ `{ok, root}` — **no `version` key**), GET `/runs`, GET `/runs/<id>`, GET `/runs/<id>/report`, GET `/scenarios`, GET `/scenarios/<id>`, POST `/validate`, POST `/execute`, POST `/preflight`. (The plan previously listed only 6 of the 10 routes.) Full route table in P3.5.

---

### Phased milestones

#### P0 — Scaffold + CI (offline gate)
- **Scope:** workspace + CI that builds and tests; rust-toolchain pin; dependency baseline. (no enrichment from the forensics reports; the edge-case catalog in §Test strategy/Appendix H still applies).
- **Files:** `Cargo.toml` (workspace, `members = ["crates/*"]`), `rust-toolchain.toml` (1.97.1), `crates/lks-core/Cargo.toml` + `src/lib.rs` + `src/errors.rs`, `crates/lks/Cargo.toml` + `src/main.rs` (prints `lks 0.1.0-rust`), `.github/workflows/rust-ci.yml`, `.gitignore` additions (`src/livekit_agent_simulator_rust/target/`, `.agent-sim/` already ignored). Other crates (`lks-livekit`, `lks-web`, `lks-mcp`, `lks-plugins`, `gemini-live`, `agent-sim-proto`) are scaffolded as empty `Cargo.toml` stubs in P0 so the workspace `members = ["crates/*"]` resolves; their deps land in their own phases.
- **Steps:**
  1. `cargo init` the workspace; set resolver 2, `rust-version = "1.97"`, `members = ["crates/*"]`; scaffold each crate dir.
  2. Add base deps to **`lks-core`**: `serde`, `serde_json`, `thiserror`, `anyhow`, `log`, `env_logger`, `uuid`, `jiff`, `rand`, `yaml_serde`, `rusqlite` (deferred: rusqlite at P3 — base P0 list is serde/serde_json/thiserror/anyhow/log/env_logger/uuid/jiff/rand). `clap` goes to `crates/lks` (the binary) at P0.
  3. `crates/lks/src/main.rs` parses `--version`/`--help` (clap `#[command(version)]` — the CLI has no `version` subcommand); `crates/lks` depends on `lks-core`. This is the **whole point of the fine-grained split**: `cargo test -p lks-core` and `cargo check` for the P1 golden tests do NOT build `livekit`/libwebrtc, which the `lks-livekit` crate adds only from P2.
  4. CI workflow: `cargo fmt --check`, `cargo clippy -D warnings`, `cargo test`; matrix `ubuntu-latest`, `macos-latest` (aarch64); `windows-latest` runs `cargo check` only (research §1.6 VERIFY: no Windows release promise in v1, but compile-check keeps the door open). Use GH Actions `actions/cache` for `~/.cargo` + `target/` (research §1.6: first build is long). Gate all of it on `-p lks-core` in P0/P1 so CI stays libwebrtc-free until P2.
- **Tests:** `cargo test -p lks` with one smoke test `cli_smoke_prints_version` (asserts binary runs). CI gate: `cargo fmt/clippy/test` green.
- **Deps added:** lks-core: serde/serde_json, thiserror/anyhow, log/env_logger, uuid 1, jiff 0.2.x, rand 0.9; lks (binary): clap 4.x.

#### P1 — Config + scenario parsing (offline gate; golden tests)
- **Scope:** `Config` load/validate/redact; `Scenario` parse from YAML + legacy JSONL; `convert`; `validate`/`export`/`list_scenarios` ops; `init_project`/`init_scenario` scaffolds. Enrichment: P1 steps 2-10 below carry the full config-coercion, YAML digit-quoting, JSONL exactness, scenario-model, and preflight contracts (Appendix G §5–§6 summarize the config/preflight subset).
- **Files:** `crates/lks-core/src/config.rs`, `crates/lks-core/src/scenario.rs`, `crates/lks-core/src/scenario_yaml.rs`, `crates/lks-core/src/scenario_jsonl.rs`, `crates/lks-core/src/scenario_convert.rs`, `crates/lks-core/src/authoring.rs` (scaffolds), `crates/lks-core/src/paths.rs`, `crates/lks-core/tests/golden_config.rs`, `crates/lks-core/tests/golden_scenario.rs`. (ops start and scenario_from_dict stay in `lks`/`lks-core` respectively per their phase; P1 is config/scenario parsing only.)
- **Steps:**
  1. Mirror `config.py` schema §5.2 + Appendix A §1: `serde` structs with defaults; `_require` fail-fast (`ConfigError` with actionable message). Use `yaml_serde` 0.10.x (official YAML-org fork of the archived `serde_yaml` — REVISED). `_require` fails iff value is `None` OR (string and whitespace-only); `0`/`false`/`0.0` PASS. Error strings byte-exact: ``Missing `livekit.api_key` in .agent-sim/config.yaml. Copy the value from LiveKit Cloud / your worker and try again.`` (pattern reused for `livekit.url`/`api_secret`/`agent_name`/`simulator.api_key`); ``Missing `livekit:` section in {path}.``; ``{path} not found. Run `lks init` (or the `init_project` MCP tool) to scaffold .agent-sim/ first.``; ``{path} is not valid YAML: {e}``; ``{path} must be a YAML mapping at the top level.``. Unknown top-level keys IGNORED silently. `int("12abc")` propagates raw `ValueError` (NOT wrapped) — replicate unwrapped propagation.
  2. **Bool-coercion trap**: `bool("false") == True` in Python. Affected fields: `observe.lk_transcription`/`lk_agent_session`/`record_audio` (default True), `observe.audio_onset.enabled` (default False), `telephony.wait_until_answered` (default True), `telephony.krisp_enabled` (default False). Decide at P1 kickoff: a `BoolLike` helper (YAML bool→bool; string `"false"`/`"no"`/`"0"`→False; other string→True, matching Python) or documented divergence.
  3. Provider/mode/endpoint/vad coercion: all `str().strip().lower()`-coerced before validation ("GOOGLE" passes). Error strings byte-exact: `simulator.provider` must be `google` or `openai` (got {v!r}); `simulator.mode` must be `realtime` (got {v!r}); `judge.endpoint_type` must be openai or anthropic; `observe.audio_onset.vad` must be `rms`; `observe.audio_onset.threshold` must be between 0.0 and 1.0 (strict: 0.0 < t < 1.0); `cues.dirs` must be a list; `cues.aliases` must be a mapping; `observe.audio_onset` must be a mapping (or absent). `judge:` absent/non-dict → whole block skipped (judge=None). Non-dict `voice` silently → `{}`. Defaults: `DEFAULT_LANGUAGE="en-US"`, `DEFAULT_TIMEZONE="UTC"`, `DEFAULT_VOICE_MODEL="gemini-3.1-flash-live-preview"`, `DEFAULT_JUDGE_MODEL="gemini-2.5-flash"`. `room_prepare_ms` default 500, `agent_join_timeout_ms` default 25_000 (int()); `dispatch_metadata` = `str().strip() or None`. `sip_trunk_id` is a legacy alias for `outbound_trunk_id` (sip wins if both). `transcript_dedupe_window_ms` code default 15_000 (template says 5000 — code wins); `silence_threshold_ms` 4_000; `turn_taking_warn_ms` 2_500.
  4. **`config_snapshot` key ORDER is a contract** (dict-literal order): `project, livekit, simulator, judge_enabled, judge, cues, observe, telephony, observe_gaps`. Nested: `livekit{url_host, agent_name, agent_join_timeout_ms, dispatch_metadata_set}`, `simulator{provider, mode, voice_model, voice, language}` (language = voice.language, authoritative), `judge{model, http, endpoint_type, api_key_set}` (model falls back to DEFAULT_JUDGE_MODEL; endpoint_type None when no base_url; judge null when judge None), `cues{dirs, alias_keys (SORTED), target_cues_dir}`, `observe{lk_transcription, lk_agent_session, record_audio, data_topics, silence_threshold_ms, audio_onset_enabled, audio_onset_threshold}`, `telephony{outbound_trunk_set, inbound_trunk_set, dial_in_set, sim_inbound_number_set, prepare_ms, wait_until_answered, krisp_enabled}`, `observe_gaps` = `["tool_events"]` IFF (not lk_agent_session) AND (no tool_event_patterns), else `[]`. Secrets NEVER in snapshot. `url_host = url.split("://")[-1].split("/")[0]`.
  5. **YAML digit-quoting (write path)**: `_str_representer` single-quotes a string iff `data.isdigit()` OR (`len>1` and `data[0]=="0"` and rest digits). So `"123"`, `"0"`, `"00"`, `"09"` quoted; `"0x1F"`, `"1e5"`, `"-1"`, `"1.5"`, `"1_000"` PLAIN (YAML 1.1 coercion traps). Applies to ALL strings incl. mapping keys. Dump params: `default_flow_style=False`, `sort_keys=False`, `allow_unicode=True`, `width=100`. Comments never survive round-trip. `_clean` drops None and empty dict/list values. Read path: emulate PyYAML 1.1 (`enabled: on` → bool; `0123` → int) — decision at P1 kickoff.
  6. Mirror `Scenario` dataclass locked fields §5.3 (`id, path, locale, tags, persona, context, simulator{max_turns=6, timeout_s=120, first_speaker=agent}, execute{...}, dispatch{metadata}, caller{mode=webrtc_sim}, telephony{...}, pass_criteria, pass_judges, pass_criteria_mode, script_steps, script_verify, plugin_modules, asserts, behavior_spec, caller_policy`); caller mode validated against the 5-value CALLER_MODES (`webrtc_sim, inbound_sip, outbound_human_pickup, outbound_sim_callee, agent_dials` — REVISED). `run_spec = Execute overrides Simulator per-field (None → simulator's)`. `hold_music_timeout_s()`: Execute wins, else persona.speech_conditions.hold_music_timeout_s, else None; clamp [5.0, 300.0] inclusive, `:g` formatting in the error. `effective_locale()`: Persona.language || Persona.locale (stripped) || metadata.locale || "en-US". `dispatch_metadata()`: scenario Dispatch wins over config `livekit.dispatch_metadata`; must `json.loads` at parse.
  7. **JSONL exactness**: only FULL-LINE `//` (after strip) lines are comments; inline `//` is a JSON error. Blank lines skipped. `_`-prefixed keys stripped from every JSONL record (NOT from YAML). Line 1 must be `kind=Scenario` + `apiVersion == "agent-sim/v1"` (error `{path}:{line}: apiVersion must be `agent-sim/v1` (got {…!r})`). Unknown kind → `{path}:{line}: unknown kind {kind!r} (expected one of {sorted(KNOWN_KINDS)})` — 12 kinds: Persona, Context, Simulator, Execute, Dispatch, PassCriteria, Script, Behavior, Plugins, Assert, Caller, Telephony. Spec non-dict → `spec must be an object`. Duplicate kinds: Persona/Simulator/Execute/Dispatch/Caller/Telephony/PassCriteria/Behavior/Assert last-wins; Plugins EXTENDS; Script REPLACES. YAML path: no apiVersion/kind check; `persona.brief is required` (different string than JSONL's `Persona.spec.brief is required — the simulator needs a caller brief`); BOM tolerated by YAML, breaks JSONL (replicate or strip).
  8. **YAML group wrapper + multi-doc**: wrapper only checked on sections[0] when `"scenarios"` in it; `scenarios` must be non-empty list (`{path}: scenarios must be a non-empty list`), exactly 1 item (`group wrapper with {n} scenarios — LKS uses one scenario per file; split into separate files`), item must be dict. Group `metadata`/`tags`/`locale`/`id` carried down only if not already present. Multi-doc merge: list+list APPENDS, everything else overwrites (dict+dict does NOT merge). Trailing `---` → None doc filtered. Empty file → `{path}: empty scenario file` (both formats).
  9. `find_scenario`/`list_scenarios` precedence: YAML shadows same-stem `.jsonl`/`.yml` in `list_scenarios`. `find_scenario`: direct `<id>.yaml` → `<id>.yml` → `<id>.jsonl` first; else scan `*.jsonl` then `*.yaml` then `*.yml` (each sorted, deduped) matching `metadata.id`; first match wins. Scenario-id regex `^[a-zA-Z0-9][a-zA-Z0-9_-]{0,63}$` applied to the QUERY only (a file with id "bad id!" parses fine but is unfindable; `find_scenario` raises `Invalid scenario_id {id!r}: use letters/digits/[_-], start with alnum, max 64 chars` as ScenarioError). `_write_yaml_atomic`: write `<dest>.yaml.tmp`, validate via `load_scenario_yaml` BEFORE `os.replace` — broken YAML never lands on dest.
  10. Telephony validation fail-fast messages (P1 step 10; see also Appendix G §5): `Scenario {id} mode={mode} requires telephony.outbound_trunk_id in config or Telephony.sip_trunk_id in the scenario.` / `…requires Telephony.call_to (human/PSTN number that will answer). For Gemini-as-callee hairpin use mode=outbound_sim_callee + sim_inbound_number.` / `…requires Telephony.call_to or config telephony.sim_inbound_number (sim DID Gemini answers via Cloud hairpin). Do not put a real handset PSTN here — that is mode=outbound_human_pickup. The DID must dispatch into the sim-room where Gemini already sits (see docs/telephony.md, docs/PROBLEM.md).` / `…requires Telephony.dial_in or config telephony.dial_in (agent-side inbound DID).`
  11. `convert_scenario` re-emits YAML from JSONL (keep `.jsonl`; idempotent; `force`). Scaffolds: `init_project` (`.agent-sim/config.yaml` + dirs, templates via `include_str!`) and `init_scenario` (`#` guide comments + example sections). Ops: `list_scenarios`, `validate_scenario`, `export_scenario` with Python-identical JSON shapes.
- **Tests (golden):** parse `templates/scenario-scaffold.yaml`, `smoke-hello.yaml`, `inbound-caller-sim.yaml`, `outbound-callee-sim.yaml`, `outbound-human-pickup.yaml` and their `.jsonl` twins → assert struct equality; golden snapshot of `export_scenario` JSON vs Python; `convert` idempotency; config golden (`templates/config.yaml` + one with `simulator.provider: openai`); redaction test (`config_snapshot` never contains `api_key`/`api_secret` values — R5). Names: `golden_scenario_yaml_matches_python`, `golden_scenario_jsonl_matches_yaml`, `convert_roundtrip_idempotent`, `config_require_fails_fast`, `config_require_zero_passes` (E61), `config_snapshot_redacts_secrets`, `yaml_digit_quoting` (E53), `yaml_bool_coercion` (E53), `bool_string_trap` (E62), `yaml_shadows_jsonl` (E26), `unknown_kind_rejected` (E29), `duplicate_ids_first_wins` (E30), `invalid_scenario_id_query` (E31), `persona_brief_required` (E32), `multi_doc_merge_semantics` (E54), `empty_scenario_file` (E51), `bom_crlf_handling` (E52), `invalid_yaml_errors` (E28), `tool_event_patterns_filtered` (E63), `config_require_fails_fast` (E27).
- **Acceptance gate:** offline (CI): all P1 tests green; Python `lks validate`/`lks export` output for the same root matches byte-for-byte where deterministic (JSON key order preserved via `serde_json` `preserve_order` feature — VERIFY: Python `json.dumps` preserves dict insertion order, so Rust must too).
- **Deps added:** yaml_serde 0.10.x, serde_json (preserve_order), (rusqlite deferred to P3).

#### P2 — Room + dispatch + Gemini caller POC (killing test: one webrtc_sim run end-to-end with events.jsonl)
- **Scope:** the vertical slice that makes the port real: `lks execute <scenario> --root <target>` against a real LiveKit room with a real Gemini Live caller, writing a valid `events.jsonl`. Also `preflight`. **Enrichment (Appendix G §3): caller_policy/persona_traits/behavior_compile land HERE (REVISED — moved from P6), because the P2 bridge slice builds the system instruction from the persona prompt and the killing test must emit the correct `sim.caller_midcall` bootstrap.** The midcall + silent-mode plumbing starts here.
- **Files:** `crates/lks-livekit/src/livekit/{mod,room,dispatch}.rs`, `crates/lks-livekit/src/livekit/sim_leg/{mod,webrtc}.rs`, `crates/lks-livekit/src/callers/{mod,gemini,base}.rs` (trait in `lks-core`, factory + bridges here), `crates/lks-livekit/src/run.rs` (run_id, seq, phases: connect → dispatch → agent_join wait → caller start → end_condition), `crates/lks-livekit/src/preflight.rs`, `crates/lks-livekit/src/audio/{mic_mixer,pcm_cue}.rs` (minimal), `crates/lks-livekit/src/logging/event.rs` (writer only, no sqlite yet), `crates/lks-livekit/tests/mock_ws.rs` (fake Gemini server), `crates/lks-livekit/tests/run_orchestrator.rs`. The **policy/prompt composition is `lks-core`** (REVISED — moved from P6): `crates/lks-core/src/caller_policy.rs`, `crates/lks-core/src/persona_traits.rs`, `crates/lks-core/src/behavior_compile.rs` — they are pure string composition, no livekit dep, and P2's bridge consumes them via the `lks-core` seam. `crates/lks-core/src/callers/mod.rs` defines the `CallerBridge` trait (the seam); `lks-livekit` implements it.
- **Steps:**
  1. Vendor `gemini-live` 0.1.8 at kickoff. Apply the fork patches per D4/Appendix D §3: (a) add `#[serde(skip_serializing_if = "Option::is_none")] pub language_code: Option<String>` to `SpeechConfig` (src/types/config.rs:56-60 — the struct is already `#[serde(rename_all = "camelCase")]` → serializes `languageCode`; this corrects the earlier "on VoiceConfig" note); (b) add `pub reconnect_on_drop: bool` (default false) to `ReconnectPolicy` (src/session.rs:71-87) with the GoAway-vs-ConnectionLost branch (session.rs:436-462 on DisconnectReason session.rs:408-414; GoAway → always reconnect, inject resume handle — already done at session.rs:634-644); (c) surface close codes in `RawFrame::Close` (src/transport.rs:311-324). Verify `ServerEvent::ModelAudio` is 24 kHz raw PCM and `setup` accepts `inputAudioTranscription`/`outputAudioTranscription` (expected PASS — same proto as Python `google-genai`). Do NOT rely on the crate for VAD/speech-started (ServerEvent has none) — manual activity markers + TurnComplete/Interrupted is the contract.
  2. Room: `Room::connect(url, token, RoomOptions)`; subscribe `RoomEvent`; track `TrackSubscribed` for agent audio (publish an audio source at 16 kHz mono — REVISED: Python `callers/base.py` publishes `rtc.AudioSource(16000, 1)`; but the sim mic/AudioSource for BOTH providers is created at **24 kHz** per Appendix D §0 — the 16 kHz figure is the *agent-audio input* `AudioStream`), `ParticipantConnected/Disconnected`, `RoomDisconnected`, `DataPacketReceived`, `TextStreamOpened` (research §1.3).
  3. Token: `livekit-api` access_token (HS256 via in-crate HMAC provider; research §1.4).
  4. Dispatch: `AgentDispatchClient` create dispatch with scenario `Dispatch.metadata` (opaque pass-through, I5); emit `dispatch.created` spec `{room, agent_name, dispatch_id, metadata_set, mode}` (Appendix C); `dispatch.agent_joined` `{identity, mode}`; `sim.connected` `{identity, room, mode}` (per `livekit/sim_leg/webrtc.py`).
  5. Caller brain: `GeminiCallerBridge` — `setup` message per Appendix D §1.2 (exact JSON: `model` from `voice.model`, `generationConfig.responseModalities: ["AUDIO"]` ONLY — requesting TEXT closes the socket with 1011 — `speechConfig` with `voice_config.prebuilt_voice_config.voice_name` + patched `language_code` from `voice.language`, `systemInstruction` with persona prompt, `tools: []`, `realtimeInputConfig.automatic_activity_detection.disabled: true`), base64 PCM in `realtimeInput` for agent audio from `rtc::AudioStream` (16 kHz mono — REVISED: `AudioStream(track, sample_rate=16000, num_channels=1)` in R4 `observer.py:298`; the 24 kHz path is the *model output*, consumed by `_play_pcm`), `_play_pcm` for `ModelAudio` into mic mixer → `AudioSource` (24 kHz), `InputTranscription`/`OutputTranscription` → dialogue, `Interrupted` → interruption event, `GoAway`/reconnect per D4 (I8; incl. 1008-policy-violation retryable drop — research §2.1 + R4). WebSocket client args: `ping_interval=30`, `ping_timeout=60` (without them the default 20 s ping kills calls ~4 min in with 1011 "keepalive ping timeout"). Connect URL: `wss://generativelanguage.googleapis.com/ws/google.ai.generativelanguage.v1beta.GenerativeService.BidiGenerateContent?key=<api_key>`.
  6. **Prompt composition (P2 — REVISED moved from P6)**: port the full 10-section `DefaultCallerPolicy` composition now — order RoleSection, GoalsSection, StyleTraitsSection, NaturalSpeechSection, ConstraintsSection, SpeechConditionsSection, ContextSection, ScriptTimingSection, FirstSpeakerSection, GuardrailsSection; `render(ctx) -> Vec<String>`; join ALL lines with `"\n"` (no trailing newline); blank lines appear INSIDE sections only. `resolved_verbosity` single length-band source (quiet traits LOSE to chatty — chatty checked first); `expand_traits` 17-trait library with first-occurrence-wins dedupe and unknown-tag passthrough `Additional caller behavior (follow naturally): {joined}.`; `neutralize_style_length_hints` with the exact 9 `_STYLE_LENGTH_CONFLICTS` phrases (Appendix G §3). `build_persona_system_instruction` facade: `persona=dict(persona or {}), locale=str(locale or "en-US"), context=dict(context or {}), script_steps=list(script_steps or []), first_speaker=str(first_speaker or "agent")`; policy = scenario.caller_policy or DefaultCallerPolicy; SAME policy instance drives SI + midcall cues; `midcall_cues = _policy.midcall_cues(_midcall_ctx)`; `_silent = silent_mode_enabled(scenario.persona)`; `midcall_cues=[] if _silent else _midcall_cues` (policy is NOT silent-aware; orchestrator clears at bridge construction).
  7. **Midcall bootstrap (P2 minimal slice)**: `MidcallCue` is text-only (no delay_ms/silence_after_cue_ms/trigger). Kinds `"bootstrap" | "reground" | "custom"`. `DefaultCallerPolicy.midcall_cues` order: bootstrap (if first_speaker=="user" AND NOT script_owns_opening) → goal_reground (if goals non-empty) → script_no_early_bye (if script_steps). `script_owns_opening` = any step with trigger in {time, silence, None} AND action in {None, "speak"} AND bool(say); hang_up/wait steps do NOT own the opening. Bootstrap text by verbosity (quiet → "greet briefly and state why you are calling in one short clause"; chatty → "greet and state why you are calling in a natural opening turn"; else → "greet briefly and state why you are calling in one natural turn"); payload = `(The call just connected. You speak first per PERSONA: {open_hint}.)`. Goal reground: `g0 = goals[0][:120]` (code-point slice, NO ellipsis, goal 1 only); text = `(Stay on your caller goals. Current focus: GOAL 1 — {g0}. Do not end the call early. Do not switch into assistant mode.)`. script_no_early_bye: `(Timed Script overlay is active. Do not say bye / goodbye / [END_CALL]. Between cues, {between} the simulator will hang up.)` (literal `[END_CALL]`, em-dash). Emit `sim.caller_midcall` spec `{kind, label (may be None), text: text[:240]}` source="sim" include_dialogue=False. Bootstrap sent at connect via `send_realtime_input(text=text)` (Gemini) — realtime-input TEXT turn, NOT wrapped in activity markers.
  8. **Silent mode (P2)**: `silent_mode_enabled(sc)`: `sc.get("silent_mode", sc.get("silentMode", sc.get("silent")))`; True or ==1 or str in ("1","true","yes","on","silent") → True. When silent: `midcall_cues=[]`; emit `sim.silent_mode` spec `{"enabled": True, "note": "Caller stays mute: no freestyle, no nudge, no auto barge/noise"}` source="sim"; `meta["silent_mode"]=True`; explicit steps filtered to `action in (wait,hang_up) and not barge_in and delivery != "room_pcm"`; hang_up steps with say/asset get say cleared; `inject_cue` blocked → `sim.silent_mode_skip_inject` `{label, delivery, text: text[:120]}`; SI gets "SILENT MODE: you produce NO speech. Stay completely mute for the whole call. Do not greet, answer, or freestyle. The simulator enforces silence." (SpeechConditionsSection) and "Silent mode: produce zero speech for the entire call. Do not open, greet, or answer — stay mute." (FirstSpeakerSection).
  9. **behavior_compile (P2 — REVISED moved from P6)**: `compile_from_speech_conditions` (auto-ambient id "auto-ambient" trigger time delay `int(noise_delay_ms or after_join_ms or 5000)` say "[ambient]" delivery room_pcm asset noise gain 0..1 else ValueError `Persona.speech_conditions.noise_gain must be between 0.0 and 1.0` once True loop; auto-barge-1 id "auto-barge-1" trigger agent_speaking delay/min_agent_active_ms `max(100, after//2)` after `int(barge_after_agent_ms or after_agent_ms or 600)` say default "Sorry — one second —" barge_in True; auto-user-silence id "auto-user-silence" trigger time delay `int(silence_arm_ms or 400)` action wait silence_after_cue_ms `int(silence_ms or user_silence_ms or 0)` (silent + <500 → `int(silent_hold_ms or 12000)`), only if >= 500). `compile_from_behavior_spec` + `merge_script_steps` (explicit wins on id) + `default_verify_for_compiled` per Appendix G §3. `apply_caller_behavior` always runs speech-conditions compile; behavior spec extends only when not silent.
  10. Event writer (minimal): envelope per I1, kinds for the slice: `run.started` (spec `{scenario_id, caller_mode, config_snapshot}`, include_dialogue=False), `dispatch.created`, `dispatch.agent_joined`, `room.participant_connected`, `room.track_subscribed`, `sim.connected`, `sim.gemini_connected` (spec `{model, voice, language, voice_gain, silent_mode, resume}`), `sim.mic_published` (`{sample_rate: 24000, mixer: "parallel"}`), `sim.agent_audio_bridged` (`{track_sid}`), `sim.gemini_activity`, `transcript.user.final`, `transcript.agent.final`, `sim.script.*` (stub), `run.end_condition` (spec `{reason}`), `run.ended` (spec `{status, summary_digest: {turn_count, tool_errors, duration_ms}}`, include_dialogue=False).
  11. Run orchestration: `new_run_id` (`{NNN}-{slug}-{YYYYMMDD}-{HHMMSS}-{xxxx}`, seq from report dirs — R4 run_orchestrator.py:52–104), report dir under `<target>/.agent-sim/reports/<run_id>/`, end_condition loop — end_reason set: `gemini_socket_drop` (end_call + transport_dropped), `sim_end_call`, `agent_disconnected`, `max_turns` (turn >= run.max_turns AND agent_replied_this_turn), `timeout`, `hold_music_timeout` (emit `sim.hold_timeout` `{timeout_s, agent_idle_ms, note: "Caller gave up waiting on agent dead air (hold_music_timeout_s)"}` + `bridge.sim_hang_up()`), `dead_call_silence` (silent_for >= 3 × silence_threshold_ms, not scripted_hold, not hold_armed, any_activity_occurred) — `silence.detected` emitted at >= 1 × threshold `{duration_ms, scripted_user_silence}` source="observer" (spec values verified from run_orchestrator.py). **Re-emit rule (REVISED — F10): `silence.detected` re-emitted at most once per `cfg_silence_s` (silence_threshold_ms); `silence_reported_at` resets on any activity** (run_orchestrator.py). Script-end (hang_up step) arrives via `bridge.sim_hang_up()` → `end_call.set()` → `sim_end_call`.
  12. `preflight` (full contract, P1 step 12; see also Appendix G §6): check ORDER + names: `config` → `livekit.url` → `observe.timezone` → `folders` → `simulator.api_key[{provider}]` → `livekit.api` (gated `if connectivity and result.ok`) → `telephony`. Config fail → early return (result, None), NO folders created, no API check. `livekit.url` fail iff not startswith ws://|wss://|http://|https://. timezone via ZoneInfo try → ``Unknown IANA timezone `{tz}` ``. folders: mkdir reports_dir + scenarios_dir (side effect mid-preflight). api_key: stripped, empty → fail `missing — `simulator.api_key` required for provider {provider}`; len < 20 → WARN "Key looks unusually short" (still proceeds). livekit.api: `room.list_rooms(ListRoomsRequest())` — fail detail ``Cannot reach LiveKit server API with the configured credentials: {Type}: {msg}``, pass detail "list_rooms OK". telephony: skipped unless outbound_trunk_id or dial_in or sim_inbound_number set; bits `outbound_trunk=set|missing; dial_in=set|unset; sim_inbound=set|unset`, pass iff outbound_trunk_id else warn; plus the three recipe warns. Preflight NEVER aborts the caller process — returns a result; orchestrator does not gate on it. NO plugin/cue-dir checks.
- **Tests (offline):** `mock_ws` fake Gemini server (accepts `setup`, serves `SetupComplete` + scripted `ModelText`/`ModelAudio` frames) → assert: setup payload shape (incl. patched `language_code`), `sim.gemini_connected` emitted, audio frames played into mixer, `transcript.user.final` on `InputTranscription`, interruption path, reconnect on transport drop (fake close + server reaccept), events.jsonl lines parse and envelope fields correct (schema-assert, not byte-golden yet — ts fields are time-dependent; golden-test the *shape* and the deterministic fields `seq`, `kind`, `spec`). Names: `gemini_setup_payload_matches_python`, `gemini_audio_plays_into_mic`, `gemini_transcript_events`, `gemini_transport_drop_reconnects`, `events_envelope_shape`, `run_id_format`, `report_dir_seq_increments`, `midcall_bootstrap_injected` (E9), `no_bootstrap_when_script_owns_opening` (E10), `bootstrap_with_wait_only_script` (E11), `agent_first_script_no_nudge` (E12), `goal_reground_truncation_120` (E13), `empty_goals_no_reground` (E14), `trait_conflict_chatty_wins` (E15), `unknown_trait_passthrough` (E16), `unknown_verbosity_falls_back_natural` (E17), `behavior_gain_out_of_range` (E20), `user_silence_hold_too_short` (E21), `preflight_config_fail_early_return` (E60).
- **Acceptance gates:** (a) **offline/CI**: the above green. (b) **MANUAL** (needs live creds — not a CI gate): real `lks execute` against the repo's own demo target (`demo/dtmf-feature/.agent-sim` is a real consumer) or any LiveKit agent; report dir contains parseable `events.jsonl` with `sim.gemini_connected`; `lks log <run_id>` renders. This is the P2 **killing test**: "one webrtc_sim run end-to-end with events.jsonl" — do not proceed to P3 until it passes.
- **Deps added:** livekit 0.8.3 (rustls-tls-webpki-roots), livekit-api 0.6.3, gemini-live 0.1.8 (vendored), tokio full, tokio-tungstenite 0.30, hound, rubato/soxr-sys.

#### P3 — Audio pipeline + observer + event writer + sqlite + summary (report parity)
- **Scope:** full audio pipeline (mic mixer, local recorder, PCM cues, VAD/silence detection, degradation), observer (agent audio → transcripts + `lk.agent.session` byte streams), full event-kind set, SQLite RunStore, summary.json/meta.json/timeline.md/review.md writers, `get_run_log`/`get_run_status`/`list_runs`/`get_run_report` ops. **Parity gate.** **Enrichment (Appendix G §3): farewell/hang_up_gate/summary/end_call/degradation/prost/suspicious_turns/midcall-silent completion all land here (REVISED — moved from P6).**
- **Files:** complete `crates/lks-livekit/src/audio/*` (incl. `audio/degradation.rs` — REVISED moved from P6), `crates/lks-livekit/src/livekit/observer.rs`, `crates/lks-livekit/src/livekit/agent_session_observer.rs` (prost decode via `crates/agent-sim-proto` — REVISED explicit), `crates/lks-core/src/logging/{sqlite,summary,meta,event}.rs` (writer + store — pure, no livekit), `crates/lks-core/src/metrics.rs`, `crates/lks-livekit/src/caller_nudge.rs`, `crates/lks-livekit/src/interrupt_rate.rs`, `crates/lks-core/src/script/{farewell,hang_up_gate,summary}.rs` (REVISED moved from P6 — pure heuristics/regex), `crates/lks-livekit/src/callers/end_call.rs` (REVISED moved from P6), `crates/lks-livekit/src/run.rs` (get_run_report + suspicious_turns — REVISED moved from P4/P5), `crates/lks-livekit/tests/parity_events.rs`, `crates/lks-livekit/tests/parity_sqlite.rs`, `crates/lks-livekit/tests/parity_summary.rs`, `crates/lks-livekit/tests/golden_kinds.rs`. **Crate split note:** the event writer + sqlite store + metrics + script farewell/hang_up_gate/summary are pure logic → `lks-core`; the observer/audio/run_orchestrator that touch livekit stay in `lks-livekit`.
- **Steps:**
  1. EventKind enum: union of every kind in Appendix C (run.*, room.*, dispatch.*, transcript.*, tool.start, assert.verify, script.verify, judge.verdict, sim.* incl. all sim.gemini_*/sim.openai_* wire frames, interruption, cue kinds, `sim.script.dtmf`, `sim.script.hang_up_deferred`, `session.*` (`agent_state`, `user_state`, `usage`, `error`, `overlapping_speech`, `debug`, `chat_history`, `tool_execution`), `handoff`, `sim.agent_greeted_nudge(_skipped)`, `sim.interrupt_rate(_skip)`, `sim.silent_mode(_skip_inject)`, `sim.caller_midcall`). `EventKind` accepts the full union; `serde` to the exact string.
  2. Writer completes: `include_dialogue` snapshot logic (dialogue inserted before `spec`), `parent_event_id`, `datetime_local` via `chrono-tz` (default UTC — I6), `ts_mono_ms` from `Instant` origin, flush-on-write to `events.jsonl` (append, utf-8, one JSON object per line — Python opens `"a"` mode, R4 event_writer.py:57). Dialogue snapshot `{user: {text, final, at_ms} or note "user has not spoken yet this turn", agent: same}`; `begin_turn(turn)` keeps user utterance, clears ONLY agent dialogue. `turn_metrics` rows only for turn > 0: `{turn, user_text, agent_text, turn_taking_ms (guarded: only overwrite when spec.turn_taking_ms is not None), tool_count, tool_errors, interrupted}`.
  3. SQLite: DDL byte-identical (copy the SCHEMA string verbatim — case + whitespace; I2, Appendix C §3); `create_run/append_event/update_turn/finalize_run` mirroring `sqlite_store.py`; sqlite connection per run with `busy_timeout`; `run_events.payload_json` = the FULL envelope JSON (not just spec) — REVISED per R4 `sqlite_store.py` insert. `runs` row fields: run_id, scenario_id, room_name, agent_name, status ('running'), started_utc, report_dir; finish_run updates status/ended_utc/duration_ms/turn_count/tool_errors/verdict/summary_json.
  4. `metrics.rs`: compute the R4 key set — REVISED: the full **36-key** `metrics` block, key-for-key from the current `metrics.py` dict literal (commit `504577a`): `schema, turn_taking_ms, ttfw_ms, ttfw_source, ttfa_run_ms, ttfa_source, turn_taking_audio_ms, user_audio_source_count, agent_audio_onset_count, recovery_ms, barge_count, barges_recovered, barge_recovery_rate, interruption_count, silence_events, agent_finals, user_finals, tool_calls, tool_errors, tool_error_rate, talk_ratio, agent_chars, user_chars, user_words_count, user_words_p10/p50/mean, user_words_natural_count/p10/p50/mean, user_words_script_count/p50/mean, slow_turns_over_2500ms, slow_turns_over_5000ms`. **The 5 audio-onset keys are UNCONDITIONAL in `metrics.py`** (not gated on `observe.audio_onset.enabled`, not omitted when empty) — a 31-key report (the Aug-09/11 ground truth) predates that commit and is NOT byte-parity with current Python. The parity fixture for `parity_summary_keys` must be regenerated from a fresh Python run. Port `metrics_digest` for `compare_runs` with its exact 12-key flat set). Formulas in Appendix B.
  5. `summary.json` writer: exact key order from R5 ground truth (top-level `run_id, status, duration_ms, turn_count, event_count, turn_taking_ms, metrics, tool_calls, tool_errors, interruptions, silences, verdict, turns` + `end_reason` when set — REVISED: R5 `summary.json` has 13 top-level keys without the summary_extra ones; `caller_mode, end_reason, dial_ms` and `script_verify, assert_verify, caller` merged from summary_extra only when events carry them — port the merge logic verbatim from `event_writer.finalize` + run_orchestrator summary_extra assembly). `event_count = seq + 1` (the run.ended emitted below). `turn_count = max((e.turn for e in events), default=0)`. `caller.behavior_summary` included; recovery_ms enriched from assert recovery checks when present.
  6. `meta.json`: scenario id, room name, agent_name, redacted config snapshot (R5 — REVISED: `config_snapshot` also carries `judge: {model, http, endpoint_type, api_key_set} | None` and `observe_gaps: [str]`; verified in R5 meta.json); `silent_mode: True` when silent; `caller_mode`, `leg`/`dial_ms` from SimLeg meta.
  7. `timeline.md`/`review.md`: deterministic regeneration (research §5.1: human-readable, not byte-locked, but golden-testable). Timeline format: `# Timeline — {run_id}` + header `| local time | +ms | turn | kind | source | detail |`; detail `_describe` per kind (transcript text[:120] with `|` escaped; tool parts name/duration/error; session states `old → new`; silence `{duration_ms}ms of silence`; generic keys name/identity/topic/status/room/node_id/reason). review.md render rules + `_FINAL_ASSESS_CATS` per event_writer.py.
  8. Observer: `lk.transcription` text streams (register via `TextStreamOpened`, topic == "lk.transcription", read `lk.transcription_final`/`lk.segment_id` attributes — `attrs.get(...).lower() == "true"`; read_all() = FULL stream, plain text not JSON; empty text dropped; role `agent` iff participant_identity == agent_identity else `user`); `lk.agent.session` byte streams → agent session state via prost-decoded `AgentSessionMessage` (framing: accumulate ALL chunks, parse ONE concatenated proto per byte stream — NOT length-prefixed; `observer.error` on parse failure); AudioStream(16 kHz mono) → agent PCM recording + RMS onset (`sim.agent.audio_onset` with backdated `ts_mono_ms` — REVISED: R4 `observer.py` computes `audio_t0_mono = (recorder.started_mono − writer.t0_mono)*1000` and `ts = max(0, audio_t0_mono + onset_ms)`; payload EXACT `{channel: "agent", sample_rate: 16000, onset_frame_idx, frames_before_first_onset, vad: {method, threshold, win_ms, energy_frames, refractory_ms}}` source="sim" include_dialogue=False turn=None). RmsOnsetDetector: sample_rate 16000 hardcoded, win_ms 20, threshold 0.012, energy_frames 3, exit_frames 5, refractory_ms 60; energy_frames CONSECUTIVE windows >= threshold; onset fires at FIRST frame of run; refractory checked BEFORE speech entry; rolling `array("h")` buffer, odd byte dropped, flush drops partial window; `RMS = sqrt(sum(s^2)/n)/32768.0`; `onset_to_audio_ms = int(round(frame_idx * 1000 / rate))`.
  9. Audio: `ParallelMicMixer` (room_pcm cues + caller voice mixing with gain, saturating sum, `int(round(s*gain))` clamped to [-32768, 32767], effects applied post-mix to the exact bytes the agent hears, `capture_frame` ALWAYS even for silence, speech preroll 150 ms with no zero-punch, `end_speech_turn` drains with silence pad), `LocalConversationRecorder` (**conversation.wav at 16 kHz** PCM16 stereo L=sim/R=agent — REVISED: R5 `meta.json` audio shows `sample_rate: 16000`; the plan previously said 24 kHz; `mark_start()` pins recorder t=0 right after track publish; gap-pad only when gap_s > 0.02), VAD (silence detection for dead-call + OpenAI path), degradation effects (DTMF-tone + dropout — REVISED moved from P6; port `audio/degradation.py` and its `test_cues.py`/`test_silent_mode.py` vectors), `sapi_tts` via `tokio::process` (Windows SAPI; on non-Windows fail identically to Python — clear error, not silent no-op).
  10. **caller_nudge.rs** (full contract, Appendix G §3): `AGENT_GREETED_NUDGE = "(The agent has finished greeting you. Respond now in the language of your persona.)"` — NON-AUDIBLE activation. Gate: first_speaker != "agent" → return (no event); silent → `sim.agent_greeted_nudge_skipped` `{reason: "silent_mode"}`. Loop poll 0.15 s: `agent_has_spoken and not user_has_spoken` → sleep 1.0 s debounce → re-check end_call/user_has_spoken (abort silently) → `bridge.nudge_freestyle_answer(agent_hint=...)`; success → `sim.agent_greeted_nudge` `{debounce_s: 1.0, audible: False}`, return. RuntimeError → sleep 0.15 s retry. Orchestrator creates the task ONLY when `run.first_speaker == "agent" and not scenario.script_steps and not _silent`; cancelled in finally. Nudge event ids `evt_nudge_{int(time.monotonic()*1000)}` (OpenAI).
  11. **interrupt_rate.rs** (full contract, Appendix G §3): `INTERRUPT_RATE_INTERVALS_MS = {none: None, low: 90_000, medium: 45_000, high: 30_000}`; `MIN_INTERVAL_MS=1000`; `DEFAULT_SAY="Sorry — one second —"`; `DEFAULT_MIN_AGENT_ACTIVE_MS=700`. `parse_interrupt_rate(persona)`: raw None or in ("","none","off","0","false") → None; not in map → ValueError `Persona.speech_conditions.interruption_rate must be one of none|low|medium|high (got {rate!r})`; `interruption_interval_ms` override >= 1000 else ValueError; gain 0..1 else ValueError; class normalized with barge_in=True; silent → None (validation STILL runs first). Runner: emit `sim.interrupt_rate` ONCE at start (spec keys exactly: rate, interval_ms, class, say, asset, delivery, min_agent_active_ms; source="sim.interrupt_rate", include_dialogue=False). Loop poll 0.05 s; armed bursts; `recently_spoke = last_agent_activity within 3.0 s`; `agent_active = agent_is_active_speaker or recently_spoke`; skip events `sim.interrupt_rate_skip` `{reason: "agent_not_active", active_ms}` / `{reason: "active_ms_below_min", active_ms, min}`; `_fire`: step_id `rate-barge-{n}` from 1; blip when with_blip && delivery != "room_pcm" — inject "[barge blip]" label `{step_id}-blip` delivery room_pcm asset "builtin:noise.blip"; interruption event ONLY if `agent_is_active_speaker` at fire AND no inject error (spec `{by: "sim", barge_in: True, class, false_positive: class in (noise, backchannel), step_id, label, say, note: "Interrupt-rate barge while agent was active speaker"}`); `sim.script.cue` ALWAYS emitted per fire incl. error field with the full key set.
  12. **Midcall/silent completion (REVISED)**: full `sim.caller_midcall` injection (bootstrap at connect + `inject_reground` — FIRST cue with kind=="reground" wins, no-op if no session, label param overrides) and silent-mode bridge gating (`midcall_cues=[]`, `sim.silent_mode_skip_inject`, `_persona_audio_allowed` False unless `_script_hangup_farewell`). `release_after_milestone` is a deliberate no-op.
  13. **farewell + hang_up_gate + end_call (REVISED moved from P6)**: port `end_call.py` regexes byte-for-byte (`END_CALL_TOKEN = "[END_CALL]"`; `_SPOKEN_END_RE`, `_FAREWELL_RE` incl. Vietnamese forms — Appendix E §4); `strip_end_call_signal`/`strip_farewell_signal` pipelines; `should_end_call_on_turn(*, pending_script, ended, farewell, scripted_farewell)`. `hang_up_gate.py`: `agent_left_open_turn` with `_CLOSING_MARKERS` (13) + `_OPEN_PROMPT_MARKERS` (27) — REVISED (ground truth hang_up_gate.py:6-50); `_hang_up_ready` reason order (`awaiting_agent_reply`, `open_agent_question`); defer budget `max(0, open_question_idle_ms)` from FIRST defer, NEVER reset by new agent finals; `sim.script.hang_up_deferred` `{step_id, reason, prior_reason, deferred_ms, budget_ms, last_agent_final: (text or "")[:240]}` source=sim.script include_dialogue=False. `script/summary.rs` zeros-safe computation per Appendix E §6.
  14. **get_run_report + suspicious_turns (REVISED moved from P4/P5)**: full report payload (summary incl. `caller.behavior_summary`, script/assert verify, judge, suspicious turns, paths). Port the suspicious-turn detection (metrics-driven: slow turns, no agent finals, etc. — mirror `run_orchestrator.py` report assembly). `get_run_log`/`get_run_status`/`list_runs`/`compare_runs` read paths with Python-identical JSON shapes. **`list_runs` limit default 20** (sqlite_store.py:172 — REVISED F12); **suite scenario_id extraction `rid.rsplit("-", 3)[0]`** (suite.py:98 — REVISED F14); **EventWriter warn marker on `turn_taking_ms > turn_taking_warn_ms`** (event_writer.py:473-474 — REVISED F13); **default voice `Puck`** (config.py:58 — REVISED F16).
- **Tests (parity):** the critical suite:
  - `parity_events_bytes`: run the Python package's own test fixtures (`tests/test_event_writer.py` vectors) through the Rust writer → byte-identical `events.jsonl` (deterministic fields; ts fields normalized by seeding). Empty-run vector: events.jsonl exists with only `run.started`/`run.ended`; summary event_count=0; metrics default-safe (E1).
  - `parity_sqlite_cross_read`: write `runs.sqlite` from Rust, read with Python `sqlite_store.RunStore` (via a small `uv run python -c` harness in CI) → same rows; and vice versa (Rust reads a Python-written DB).
  - `parity_summary_keys`: summary.json key set + order matches R5 ground-truth file.
  - `golden_kinds`: every EventKind string serializes to exactly the Python string (incl. `sim.gemini_socket_drop`, `sim.openai_socket_drop`, `sim.gemini_resumption_handle`, `sim.script.dtmf`).
  - `metrics_values`: feed a scripted events log → identical metrics dict vs Python `metrics.py` (port the Python unit test vectors).
  - `observer_dedupes_transcripts` (E25), `conversation_wav_no_agent_audio` (E44), `nudge_retry_on_runtime_error` (E22), `interrupt_rate_waits_for_agent` (E23), `interrupt_rate_blip_failure` (E24), `interrupt_rate_invalid_value` (E18), `interrupt_interval_below_min` (E19), `silent_mode_end_to_end` (E8), `test_end_call.py`/`test_hang_up_gate.py` vectors port (E4), `test_silent_mode.py` vectors port (E3/E8), `run_agent_join_timeout` (E2), `script_step_agent_speaking_no_match` (E6), `test_script*.py` barge vectors (E5), `dtmf_cue_sends_sip_dtmf` (E7, mock).
- **Acceptance gates:** (a) **offline/CI**: parity suite green. (b) **MANUAL**: re-run the P2 real run and diff `events.jsonl`+`summary.json` against a Python-produced report for the same scenario (same target, same day, non-deterministic fields normalized: ts, run_id) — "report parity with Python" gate. Also MANUAL: `lks web <run_id>` serves the Rust-written report and plays audio.
- **Deps added:** rusqlite 0.40 bundled, chrono-tz, prost 0.13 (+ agent-session proto generated types — REVISED explicit), (hound/rubato already at P2).

#### P3.5 — Web report player + REST API (parity)
- **Scope:** port the two HTTP servers and the full cues-report pipeline: (1) report UI server (port **8765**) serving the embedded SPA (`web/dist`) + `/api/runs*` JSON + `/runs/<id>/*` file routes; (2) REST API server (port **8787**, prefix `/api/v1`) bridging to the ops layer; (3) `cues.json` pipeline — report_time (timing base), source_priority, windows, transcript_cues (dedupe + ghost-STT), markers, tool_events, speech_origin, cues payload assembly. Byte-parity target: `cues.json` output identical to Python `web/cues.py` for the same report dir (golden-tested), header-level parity for HTTP (exact Content-Type / Cache-Control / no-store / 302 semantics). This phase runs after P3 (report files exist on disk) and before P4 (ops passthrough routes land against the Rust ops layer; until then `POST /api/v1/execute|validate|preflight` return op-stub shapes that the P4 op layer fills in — the web layer only guarantees status codes + error mapping, payload shapes are ops-owned).
- **Files:** `crates/lks-web/src/{mod,report_server,api_server,cues,report_time,source_priority,windows,transcript_cues,markers,tool_events,speech_origin,static_assets}.rs`, `crates/lks-web/assets/web/` (web/dist embed), `crates/lks-web/tests/{parity_cues,web_routes,cues_constants,markers,speech_origin,transcript_dedupe,static_serving,range_requests,api_errors}.rs`. The cues pipeline + both servers depend on `lks-core` (report_dir reading, ops stubs) + `lks-livekit` (behavior_summary recompute).
- **Steps:**
  1. **Timing base (`report_time.rs`)** — implement in this order; everything else depends on it:
     - `wav_duration_ms(path) -> Option<i64>`: `int(frames * 1000 / rate)` — hound `WavReader` frames; `rate = getframerate() or 1` (rate 0 → 1); any error → `None` (corrupt header/unsupported format tolerated).
     - `load_events(path) -> Vec<Event>`: skip empty lines; skip lines that fail JSON parse (corrupted jsonl tolerated per-line); missing file → `[]`.
     - `load_json(path) -> serde_json::Value`: missing/corrupted → `{}`; non-object → `{}`.
     - `resolve_audio_t0_ms(meta, events) -> i64`: `meta.audio.t0_mono_ms` (int, `max(0, …)`); else first event with kind starting `transcript.` or kind in `{sim.mic_published, sim.gemini_connected}` → `max(0, ts_mono_ms)`; else 0.
     - `mono_to_audio_ms(mono, t0, duration_ms) -> Option<i64>`: `start = max(0, mono - t0)`; **if duration_ms is Some and start > duration_ms + 2000 → None** (events beyond 2 s past audio end dropped).
     - `clamp_end(start, end, duration_ms) -> i64`: `end = max(start + 120, end)`; if duration: `end = min(end, max(start + 120, duration_ms))`.
     - **No datetime formatting exists in this module.** The only datetime parse in the whole web layer is the run-list sort key (step 3 below).
  2. **Source priority + text similarity (`source_priority.rs`)** — pure functions, table-driven:
     - `SIM_CALLER_SOURCES = ["sim.gemini", "sim.openai"]`; USER table: sim.gemini=0, sim.openai=0, data=2, lk.transcription=3; AGENT table: data=0, lk.transcription=1, sim.gemini=2, sim.openai=2. `source_rank(source, role)`: exact table match on role == "user" (any other role → agent table); else if source non-empty AND not in {sim.gemini, sim.openai, lk.transcription} → opaque data-topic source: 1 (user) / 0 (agent); else (empty or builtin-not-in-table) → 9.
     - `texts_similar(a, b)`: normalize lowercase + strip; remove apostrophes `'` `'` `'`; `re.sub(r"[^\w\s]", " ", t)` → every non-word/non-space char to space; collapse whitespace. Either empty → false. Match iff `a_n == b_n || a_n in b_n || b_n in a_n`. Token-canonical: `_ASR_TOKEN_ALIASES = {"okey":"okay", "ok":"okay", "k":"okay", "thank":"thanks", "thx":"thanks", "byebye":"bye"}` per whitespace token; `inter = ta ∩ tb`; true iff `inter.len() >= max(1, int(min(len(ta),len(tb)) * 0.5 + 0.5))` — **round-half-up of the smaller set size, min 1**.
  3. **Windows (`windows.rs`)** — all constants verbatim:
     - `estimate_utterance_ms(text, role)`: empty text → **800 (agent) / 600 (user)**. `words` = split on `" "` after replacing `\n` with `" "`, non-empty entries; `units = max(len(words), max(1, len(text)/4))` (**integer division**); `ms = units * (95 if agent else 85)`; clamp agent to `[700, 22_000]`, user to `[500, 14_000]`.
     - `collect_interim_starts(events, duration_ms)`: kinds `transcript.*.interim`; role from `"agent"`/`"user"` inside kind; text = `spec.text` stripped; skip empty; ms via `mono_to_audio_ms` (skip None).
     - `collect_agent_active_windows(events, duration_ms)`: from `room.active_speakers` events; `agent_on` iff any identity `starts_with("agent-")` **or** `"agent" in identity.to_lowercase()`; missing/empty identities → off; non-list `spec.identities` → `[]`. Points sorted by ms; **`gap_close_ms = 2800`**: while on, if gap since last-on > 2800 → close window `(start, last_on + 600)` and start new; on off → close `(start, last_on + 600)`. Trailing window end = `last_on + 600`, capped at duration_ms. Merge pass: if `w0 <= prev_w1 + 1500` → merge.
     - `best_interim_start(interims, role, final_ms, text, est_ms, prefer_source)`: `window_lo = max(0, final_ms - est_ms - 3000)`; `window_hi = final_ms - 500`; if `window_hi <= window_lo` → None. Candidate match: 12-char truncated prefix match either direction. Rank = `source_rank(src, role)`; if `prefer_source == Some(s)` and `src == s` → rank = **-1**. Sort by `(rank, ms)`; return the min `ms` among the best rank.
     - `best_active_window(windows, final_ms, est_ms)`: keep windows where `final_ms >= w0 - 300 && final_ms <= w1 + 2000`; `span = max(1, w1 - w0)`; `score = abs(w1 - final_ms) + abs(span - est_ms) / 3` (**integer division**); lowest score wins → `(w0, w1)`.
  4. **Transcript cues (`transcript_cues.rs`)** — pipeline: raw → dedupe → ghost filter → timing → final sort.
     - Raw cues from `transcript.user.final` / `transcript.agent.final`: fields `role, final_ms, text, turn, source, kind, ts_mono_ms` (`final_ms` from `mono_to_audio_ms`, skip None; non-int `ts_mono_ms` → skip event).
     - **Dedupe** (window `max_delta`): sort raw by `(final_ms, source_rank(source, role), 0 if role=="agent" else 1)` — agent first on ties. For each cue walk backward over same-role cues: `delta = abs(prev.final_ms - c.final_ms)`; `similar = texts_similar(...)`; `cross_source` = both sources non-empty and different. `max_delta = 15000` if user+similar+cross_source; **6000** if agent+similar+cross_source; **4000** if similar; **2500** if not similar. **If `delta > max_delta` → BREAK (do not continue scanning; sorted order relied on).** If similar: replace iff `cur_rank < prev_rank` OR (equal rank AND `len(c.text) > len(prev.text)`); replaced → break. Not replaced → append.
     - **Ghost-STT filter**: only runs when at least one user cue has source in `{sim.gemini, sim.openai}`. Drop user cue `c` iff ALL of: (a) src not in `{sim.gemini, sim.openai}`; (b) `src == "lk.transcription"` OR (src non-empty AND not in `{sim.gemini, sim.openai, sim.script}` AND `"transcript" in src`); (c) exists provider user cue `g` with `abs(c.final_ms - g.final_ms) <= 2500`; (d) `!any(texts_similar(c.text, g.text) for g in near)`. Non-user cues always kept; `sim.script` sources never dropped.
     - **Timing per cue** (after dedupe/ghost): `est = estimate_utterance_ms(text, role)`; agent → `best_active_window(agent_windows, final_ms, est)` → `(start, end_hint)`; `prefer_source = Some("sim.gemini")` for user else None; `interim_start = best_interim_start(...)`; if `interim_start < start` (or start None) → `start = interim_start`. Fallback `start = max(0, final_ms - est)`. If `start >= final_ms` → `start = max(0, final_ms - 400)`. **Clamp `start = max(0, min(start, final_ms - 200))`.** `tail = 350`; `end = final_ms + 350`; if `end_hint > final_ms` → `end = max(end, min(end_hint, final_ms + 800))`; if duration known → `end = min(end, max(start + 200, duration_ms))`; finally `end = max(start + 200, end)`. Final sort `(start_ms, 0 if agent else 1)`.
  5. **Markers (`markers.rs`)** — kind strings verbatim: `barge_in, script_cue, user_audio_source, audio_onset, silence_wait, silence, interruption, recovery, backchannel, false_interrupt, dtmf, tool, tool_error`.
     - `_collect_script_injects`: kind `sim.script_inject`; `start = mono_to_audio_ms(ts_mono_ms)`; `dur = int(spec.duration_ms or 0)`; **if `dur <= 0`: `dur = 2200` if `delivery != "room_pcm"` else `800`**; `end_ms = start + max(200, dur)`; fields `start_ms, duration_ms, end_ms, label, text, delivery, asset`.
     - `_inject_duration_near(at_ms)`: candidate injects with `abs(inj.start_ms - at_ms) <= 900`; `same` if label substring (either direction) or `_text_overlap(label, say)`; `score = d - (200 if same else 0)`; best (lowest score) → its `duration_ms`.
     - `sim.script.cue`: `mtype = barge_in` if `spec.barge_in` else `script_cue`; overridden by icls: `backchannel`→backchannel, `noise`→false_interrupt, `dtmf`→dtmf (`icls = spec.class or spec.interrupt_class`). `detail = "trigger={trigger or '?'} · during_agent={bool}"` + (`" · class={icls}"`) + (`' · say="{say}"'`) + (`" · waited={waited}ms"`). **Span: if barge and icls not in (noise, backchannel): `span = max(2200 if during else 1400, (inj_dur or 0) + 400)`; else `span = max(400, min(waited, 2000) or 400, (inj_dur or 0) + 200)`.** `end = clamp_end(start, start + span, duration_ms)`. Label prefix: `"⚡ "` (barge+during, not noise/backchannel), `"💬 "` (backchannel), `"🔇 "` (noise). Fields: `type, start_ms, end_ms, label, detail, step_id` (None if empty), `say` (None if empty), `during_agent_speech, barge_in, class`, `audio_ms = inj_dur or span`. `barge_points.append(start)` only if `barge && icls not in (noise, backchannel, dtmf, silence)`.
     - `sim.script.wait`: `span = waited` if `waited > 0` else **1500**; `win_start = max(0, start - span)`; `end = clamp_end(win_start, start + 200)`; type `silence_wait`; `label = label or step_id or "user pause"`; detail `"script wait · trigger={trigger or 'silence'} · held≈{span}ms"`; fields `step_id, trigger`.
     - `silence.detected`: `span = duration_ms` if `> 0` else **4000**; `win_start = max(0, start - span)`; `end = clamp_end(win_start, start)`; type `silence`; label `"silence detected"`; detail `"observer silence ≥ threshold ({span}ms)"`; field `duration_ms`.
     - `interruption`: `end = clamp_end(start, start + 500)`; type `interruption`; label `"interruption ({by})"` (`by = spec.by or "unknown"`); `detail = note.strip() or "by={by}"`; fields `class` (strip or None), `by`.
     - `sim.agent.audio_onset`: `end = start + 300`; type `audio_onset`; label `"agent audio onset"`; detail `"onset_frame={spec.onset_frame_idx} · vad={spec.vad.get('method','?')}"`; field `onset_frame_idx`.
     - `sim.caller.audio_source_start`: `end = start + 300`; type `user_audio_source`; label `"caller audio source"`; detail `"provider={spec.provider or '?'} · via={spec.via or '?'}"`.
     - `Recovery`: agent_finals = all `transcript.agent.final` audio-ms; for each barge point in order, first agent final `> barge_ms` not already consumed; `end = clamp_end(recovery_ms, recovery_ms + 800)`; type `recovery`; label `"agent recovery"`; detail `"agent final after barge-in @ {barge_ms}ms"`; field `after_barge_ms`.
     - **Final sort: `(start_ms, type_string)`** — same start_ms → type sorts alphabetically (`audio_onset < backchannel < barge_in < … < user_audio_source`).
  6. **Tool events (`tool_events.rs`)**: `_UNCLOSED_TOOL_TAIL_MS = 500`, `_TOOL_BAND_MIN_MS = 400`, `_TOOL_BAND_CAP_MS = 400`. Events `tool.start` / `tool.end` / `tool.error`. Pending key: `"call:{call_id}"` (call_id = first of `spec.call_id`/`spec.id`) else `"evt:{event_id}"`. `name = first of spec.name/tool_name or "tool"`; `arguments = first of spec.arguments/args/payload`; `call_id = first of spec.call_id/id`. `tool.start` stores `{call_id, name, start_ms, turn, source, arguments, parent_event_id, start_event_id}`. End matching order: (1) `call:{call_id}` key; (2) scan pending for `start_event_id == parent_event_id`; (3) `len(pending) == 1` → take the only one. `dur = int(spec.duration_ms or 0)`; if `dur <= 0` and start_row → `dur = max(0, start - base_start)`. `is_error = kind == "tool.error" || bool(spec.is_error)`. `output = first of spec.output/result/error`; `error = first of spec.error/message` if is_error. **`end_ms = base_start + max(400, min(dur, 400))` if dur > 0 else `base_start + 500`**, then `clamp_end`. Span fields: `call_id, name, start_ms, end_ms, duration_ms` (None if dur <= 0), `turn, source, arguments, output, is_error, error, parent_event_id`. Unclosed pending: `end = base + 500` clamped; `duration_ms = None, is_error = false, output = None, error = None`. Sort `(start_ms, name)`.
     - `_build_session_summary`: `session.usage` → `usage = dict(spec)`; `session.agent_state` → transitions `{at_ms, from (str|None), to}` appended only if `new_state` is not None; `session.error` → errors `{at_ms, message = first of spec.message/error or "session error"}`. Returns None if all empty; else only non-empty keys present: `{"usage"} / {"state_transitions"} / {"errors"}`.
     - `_extract_chat_history`: last `session.chat_history` event (reversed scan), `spec.items` if list else None.
     - `_build_tool_summary`: `tool_count` from `summary.tool_calls` (int) else `summary.metrics.tool_calls`; `tool_errors` from `summary.tool_errors` else `metrics.tool_errors`; if `<= 0` → from spans (len / count is_error). Returns `{"tool_count", "tool_errors"}` ints.
  7. **Speech origin (`speech_origin.rs`)** — the cue-tagging pass:
     - `_norm_speech`: keep only alnum + whitespace (everything else → `" "`), lowercase, collapse whitespace. `_CONTENT_STOP` (verbatim): `{"được","không","mình","bạn","với","cho","của","này","đó","là","và","các","một","như","để","có","thì","rồi","nữa","when","what","that","this","with","have","from","your","will","been","were","they","them","than","then","also","just","into","over","more","hook"}`.
     - `_text_overlap(a, b)`: empty → false; `len(shorter) >= 5 && shorter in longer` → true; content-word sets (len >= 4, not in `_CONTENT_STOP`); empty intersection → false; `any(len(w) >= 5 for w in inter)` → true; else `len(inter) >= 2`.
     - `_mostly_script_say(text, say)` (on normalized `nt`, `ns`): `nt == ns` → true. `ns in nt`: extras = `nt` with `ns` removed once (single occurrence → `" "`, strip); extras empty → true; `len(extras) <= 12 && len(extras.split()) <= 2` → true; content words in extras: `len(content) >= 2 || len(extras) > max(24, int(len(ns) * 0.45))` → false; else true. `nt in ns` (STT split of script): `len(content) >= 2 || len(nt) >= 12` → true; `len(ns) - len(nt) <= 20` → true. Fallthrough: `_text_overlap(text, say) && len(nt) <= len(ns) + 24`.
     - `_tag_cues_with_markers(cues, markers)`: marker `m` "near" cue `c` iff `abs(m.ms - final_ms) <= 8000 || abs(m.ms - start) <= 1200 || (m.ms <= end && m.end_ms >= start)` where `final_ms = c.final_ms` if not None else `c.end`. Non-user role → `speech_origin = "natural"` (no tagging).
     - **User scoring vs script markers** (barge_in + script_cue types): `delta = final_ms - m.ms`; skip if `delta < -800 || delta > 15000`. `text_hit = _mostly_script_say(text, say)` only if `say` non-empty and not starting with `[`. `tiny = len(text.split()) <= 3 && len(text.strip()) <= 28`. **barge markers**: `text_hit && -500 <= delta <= 15000` → accept, `score = 100 - min(40, max(0, delta) / 400)` (integer division); `tiny && 0 <= delta <= 3500` → accept, `score = 70 - min(30, delta / 200)`; else 0. **non-barge**: `accept = text_hit && 0 <= delta <= 8000`, `score = 50`. `origin = "script_barge"` if barge else `"script_cue"`. Best score wins.
     - **Time-only fallback**: origin still natural, `len(text.split()) <= 2 && len(text.strip()) <= 24` → first barge marker with `0 <= delta <= 3500` → `script_barge`.
     - **Late-STT fallback**: `len(text.split()) <= 4`; barge markers with `say` non-empty, not starting `[`, `_text_overlap(text, say)`, `delta >= -500`; key = `(phrase, -abs(delta))` where `phrase = 2` if `nt in ns || ns in nt` else 1; max key wins → `script_barge`.
     - `_pin_script_window`: sets `script_step_id, script_say, script_label` from the matched marker; `inject_ms = matched.start_ms or final_ms`; `audio_ms = matched.audio_ms or 0`; **if `audio_ms <= 0`: `2200` if origin == "script_barge" else `900`**. `start_ms = max(0, inject_ms - 80)`; `end_ms = max(final_ms + 500, inject_ms + audio_ms + 350, existing end_ms)`; sets `inject_ms`.
     - **Full-line preference**: if `say` non-empty, not starting `[`, `len(text.strip()) < len(say)`, `len(text.split()) <= 6` → `stt_text = original text`; `text = say`.
     - `_synthetic_script_barge_cues(markers, cues)`: covered_steps (existing cues with speech_origin in {script_barge, script_cue} and a script_step_id) + covered_injects (their inject_ms). For each marker with `barge_in` or type `barge_in`: skip if `step_id in covered_steps`; skip if `any(abs(inject_ms - t) < 600 for t in covered_injects)`. `say = m.say` strip; `label = m.label or step_id or "script barge"`; `display = say` unless say starts `[` AND ends `]` → label; strip leading `"⚡"`. `audio_ms = m.audio_ms or 0`; if `<= 0` → `max(1200, m.end_ms - inject_ms)`; `end_ms = max(inject_ms + audio_ms + 350, inject_ms + 1200)`. Cue fields: `role "user", start_ms = max(0, inject_ms - 80), end_ms, final_ms = end_ms, text = display, speech_origin "script_barge", script_step_id, script_say = say or display, script_label = label, inject_ms, synthetic: true, source: "sim.script", marker_tags: ["barge_in"]`. Mark step + inject covered.
  8. **Payload assembly (`cues.rs`)** — exact build order: load events/meta/summary → duration (wav first, else `int(audio_meta["duration_ms"])`, TypeError/ValueError → None) → t0 → transcript cues → tool spans → markers → `_tag_cues_with_markers` → `cues.extend(_synthetic_script_barge_cues)` → script_verify/assert_verify from summary, fallback to LAST `script.verify` / `assert.verify` event spec (reversed scan) → behavior_summary: `summary.caller.behavior_summary` (dict) → `summary.behavior_summary` → recompute via `script::build_caller_behavior_summary(events)` (shared ops/script module) → observe_gaps: `meta.config_snapshot.observe_gaps` (list) → `config_snapshot.observe.observe_gaps` (list) → `[]`. `marker_counts` = count per marker type string. Payload shape below. `write_cues_json`: `serde_json::to_writer` with 2-space indent, `ensure_ascii = false`, utf-8 → `report_dir/cues.json`.
     - Payload shape: `{run_id, scenario_id (meta or summary), audio {file: "conversation.wav"|null, duration_ms, t0_mono_ms, channels {left: "sim", right: "agent"}}, cues [], markers [], marker_counts {}, script_verify, assert_verify, caller {behavior_summary}|null, behavior_summary, tool_events [], tool_summary {tool_count, tool_errors}, session_summary, chat_history, observe_gaps []}`. Empty-state: `audio {file: null, duration_ms: null, t0_mono_ms: 0, channels {left: "sim", right: "agent"}}`, all aggregates null/empty. `channels` default applies only when `audio_meta.channels` missing/null/empty.
  9. **Report UI server (`report_server.rs`, port 8765)** — axum app with the route table below; static assets via rust-embed. Threading model: Python `ThreadingHTTPServer` = one OS thread per connection; axum with `tokio::spawn`-per-connection is the equivalent — keep the server off the CLI's blocking ops (own runtime or `Handle::spawn_blocking` for cues regeneration; the pipeline is sync and cheap — keep it sync per request like Python).
     - Route table (all under port 8765):
       - GET `/`, `/index.html` → `player_dir/index.html` body, `Content-Type: text/html; charset=utf-8`, `Cache-Control: no-store`; 200; **missing `index.html` at startup → server refuses to start**: `Web UI assets missing: {player_dir}/index.html — maintainers: pnpm --dir web install && pnpm --dir web build (CI attaches web/dist into the wheel as web_static; or use pnpm --dir web dev with lks web in another terminal)`.
       - GET `/player.html?run=<run>` → 302, `Location: /?run=<run>` (non-empty) else `Location: /` — **only header, no body, no Content-Type**.
       - GET `/assets/<name>` → file bytes, `Content-Type: mimetypes.guess_type(name) or "application/octet-stream"`; path-escape → **403 `asset not found`** (prefix check on resolved path); not a file → 404 `asset not found`.
       - GET `/player.js`, `/player.css` → bytes, `.js → text/javascript; charset=utf-8`, `.css → text/css; charset=utf-8` (suffix override), `Cache-Control: no-store` (suffix in `.html/.js/.css`); missing → 404 `missing {name}`.
       - GET `/api/runs` → JSON array newest first; element shape `{"run_id", "scenario_id", "status", "duration_ms", "turn_count", "tool_count", "has_audio", "started_utc", "mtime_ms"}`; `has_audio = (report_dir/"conversation.wav").exists()`; `mtime_ms = int(dir.stat().mtime * 1000)` (0 on error); scenario_id/started_utc from summary.json, fallback meta.json; tool_count = summary.tool_calls else summary.metrics.tool_calls if object. **Sort key**: started_utc parsed ISO-8601 UTC (`Z` → `+00:00`; parse failure → fall through); fallback mtime_ms if > 0 → `(mtime_ms/1000.0, run_id)`; else `(0.0, run_id)`; descending, tie-break run_id. Only dirs containing `events.jsonl` listed.
       - **Live-run fallback** (only if `summary.get("status")` is None): read events.jsonl line-by-line; `run.started` → status "running"; `run.ended` → `spec.status or "done"`; `transcript.*.final` with int turn → live_turns set; `transcript.agent.final` → live_agent_finals += 1; `tool.start` → live_tools += 1. Blank lines skipped; **a JSON parse error aborts the whole scan, keeping partial counts**. Still no status → "running" if live_agent_finals > 0 or events file exists else "queued". `turn_count = len(live_turns) or (1 if live_agent_finals > 0 else 0)`; `tool_count = live_tools`.
       - GET `/api/runs/<id>` and `/api/runs/<id>/cues` → cues payload JSON, regenerated per request, `application/json; charset=utf-8`, `Cache-Control: no-store`; 404 `run not found` if id empty or report dir missing; any other second segment → 404 `unknown api path`.
       - GET `/runs/<run_id>/<name>` → `name == "" or "player"` → 302 `Location: /?run=<run_id>` (no body); `name == "cues.json"` → regenerate + serve `application/json; charset=utf-8` + no-store; else file bytes, `ctype = guess_type or "application/octet-stream"`; suffix override per `_serve_file`. report_dir escapes reports_root → **403 `forbidden`**; not a dir → 404 `run not found`; target escapes report_dir → **403 `forbidden`**; not a file → 404 `file not found`.
       - anything else → 404 `not found`.
     - `Accept-Ranges: bytes` only when content-type starts with `audio/`. tower-http `ServeDir` implements real Range handling — keep it (D12): assert 206 partial body in tests; Python returns 200 full body (divergence client-visible only to range-issuing clients, which the bundled UI never is).
     - **Static dir resolution** (`package_web_dir`): prefer the **newest** of (1) repo-root `web/dist` found by walking up ≤ 6 parent dirs for `web/dist/index.html`, (2) embedded copy. If both exist: `dist/index.html.mtime >= packaged/index.html.mtime` (**f64 ≥ f64, sub-second; `>=`, not `>`**) → dist wins; stat error → prefer dist. In an installed context only the embedded copy exists.
     - `_serve_file` suffix override beats caller content type: `.js` → `text/javascript; charset=utf-8`, `.css` → `text/css; charset=utf-8`, `.html` → `text/html; charset=utf-8` + no-store; `.json` has no override. Errors: `text/plain; charset=utf-8` + Content-Length, no Cache-Control. Redirects: 302 + Location only — no body, no Content-Type, no Cache-Control.
     - Report dir layout read by the web module (all under `<root>/.agent-sim/reports/<run_id>/`): `events.jsonl`, `summary.json`, `meta.json`, `conversation.wav`, `cues.json` (web-written). SQLite `runs.sqlite` is **not** read by the web layer (list runs derives from report dirs + summary.json).
     - **Live-run behavior**: `cues.json` regenerated on EVERY request to `/api/runs/<id>`, `/api/runs/<id>/cues`, `/runs/<id>/cues.json` — never cached (no-store); must match or a stale-file race appears for in-progress runs. **Pre-warm**: `start_web_server` with a run_id whose report dir exists writes `cues.json` once at startup.
     - `behavior_summary` recompute: when summary lacks `caller.behavior_summary` and `behavior_summary`, recompute via `script::build_caller_behavior_summary(events)` — lazy, exactly like Python's lazy import.
     - Return values: `start_web_server(host=127.0.0.1, port=8765, open_browser=true, run_id=None, blocking=true) -> {"url", "base_url", "host", "port", "run_id", "runs", "reports_dir", "player_dir"}`; `url = {base}/?run={run_id}` if run_id else `{base}/`; blocking prints `Open: {url}` + `UI assets: {player_dir}`; `open_browser` false skips browser open; browser-open failure swallowed.
  10. **REST API server (`api_server.rs`, port 8787, prefix `/api/v1`)** — route table:
      - GET `/api/v1/health` → `{"ok": true, "root": "<resolved project_root>"}` — **no `version` key** (docstring claims one; code does not emit it).
      - GET `/api/v1/runs` → `ops::list_runs` passthrough. GET `/api/v1/runs/<id>` → `ops::get_run_status`; empty id → 400 `missing run id`. GET `/api/v1/runs/<id>/report` → `ops::get_run_report` (matched when rest ends with `/report` after strip); `…/report/foo` → run_id `x/report/foo` → 404 from ops.
      - GET `/api/v1/scenarios` → `ops::list_scenarios`; GET `/api/v1/scenarios/<id>` → `ops::export_scenario`; empty → 400 `missing scenario id`.
      - POST `/api/v1/validate` `{"scenario_id"}` → `ops::validate_scenario`; missing → 400 `validate needs scenario_id`.
      - POST `/api/v1/execute` `{"scenario_id", "repeat" (int, default 1), "pass_at_k", "run_name", "agent_name"}` → `ops::execute_scenario(...)`; missing sid → 400 `execute needs scenario_id`; `int(repeat)` — float truncates toward zero, `"3"` works, `"abc"` → ValueError → 400.
      - POST `/api/v1/preflight` `{"connectivity": bool, default true}` → `ops::preflight(root, connectivity=...)`; **`bool(value)` semantics: only JSON false/0/""/null are falsy; string `"false"` is truthy — replicate exactly**.
      - unknown POST → 404 `{"error": "unknown POST route: {rest}"}`; other unknown → 404 `{"error": "unknown route: {path}"}`; non-`/api/v1` prefix → 404 `unknown path — REST API under /api/v1`.
      - Query strings stripped before routing. `_read_json`: Content-Length or 0; `len <= 0` → `{}`; empty body → `{}`; JSON/unicode decode error → 400 `invalid JSON body: {exc}`; non-object → 400 `JSON body must be an object`. No max body size. All responses: 200, `Content-Type: application/json; charset=utf-8`, `Cache-Control: no-store`, `Content-Length`, body JSON `ensure_ascii = false`, 2-space indent. Errors: `{"error": msg}` — `ensure_ascii = false`, **no indent**.
      - Ops error mapping: `ValueError` → 400; `ops::ConfigError` → 404; `FileNotFoundError` → 404; other → 500, body `"{TypeName}: {msg}"`. Async ops run on a fresh current-thread runtime per request (Python `asyncio.run` equivalent). Blocking execute allowed. `start_api_server` returns `{"url": base+PREFIX, "base_url", "host", "port", "root"}` (+ `tokio::task::JoinHandle`); blocking mode prints `REST API: {url} (root: {root})` + `Ctrl+C to stop`; poll interval 0.5 s; Ctrl+C → shutdown + close.
  11. **Logging contract**: report server logs only requests containing `404`; REST API logs only status codes starting with `4`/`5` (tracing: debug for 2xx, warn for 4xx/5xx) — tests grep logs.
- **Tests:**
  - `parity_cues_golden` — Rust `build_cues_payload` output byte-diffed against Python `web/cues.py` output for a fresh real-run fixture (REVISED: the earlier R5 fixture `114-people-pleaser-refuse-card-20260809-201652-8b32` is not in this repo — capture a current Python run into `tests/golden/` before this phase, per Cross-validation §1) and one live/in-progress report dir (all fields incl. marker order, dedupe outcome, synthetic barge cues, `marker_counts`, tool bands, empty-state payload).
  - `test_web_routes` — every route: 200/302/403/404 statuses, exact headers (Content-Type, Cache-Control no-store, Accept-Ranges on audio), redirect Location-only, `/api/runs` element shape + sort order, live-fallback status derivation (running/queued/done, partial-scan abort semantics), run-id split behavior, `cues.json` regeneration freshness.
  - `test_cues_constants` — `source_rank` table (incl. opaque data-topic 1/0 and fallback 9), `texts_similar` vectors (apostrophes, punctuation, alias pairs okey/okay, round-half-up boundary), `estimate_utterance_ms` boundaries (empty, clamps, division), `_mono_to_audio_ms` + `_clamp_end`.
  - `test_markers` — every marker kind's span/end/label/detail/prefix; inject duration fallback 2200/800; barge span 2200/1400 vs non-barge formula; recovery consumes first agent final per barge point; final (start_ms, type) ordering.
  - `test_speech_origin` — `_mostly_script_say` all branches; `_text_overlap` with `_CONTENT_STOP`; scoring table (100/70/50); tiny + late-STT fallbacks; `_pin_script_window`; full-line preference; `_synthetic_script_barge_cues` dedupe (<600) and fields.
  - `test_transcript_dedupe` — 15000/6000/4000/2500 windows; break-on-overflow; longer-text-wins ties; ghost-STT filter (fires only with provider user; drops only agent-side STT; keeps sim.script).
  - `test_static_serving` — rust-embed asset resolution, package_web_dir mtime precedence (`>=`), suffix override, no-store set, missing-index.html refusal, `.html` override no_store.
  - `test_range_requests` — tower-http ServeDir range support: 206 partial body, `Accept-Ranges: bytes`, full-body 200 without Range (documented divergence).
  - `test_api_errors` — REST error mapping (400/404/500 + body formats), `int(repeat)` coercion incl. `"abc"` → 400, `bool(connectivity)` truthiness, unknown-route bodies, health has no `version` key.
  - `web_logging_filter` — report server logs only 404-bearing lines; API logs only 4xx/5xx.
- **Acceptance gates:**
  - **(a) offline/CI**: all tests green, including `parity_cues_golden`.
  - **(b) MANUAL** (not a CI gate): `lks web <run_id>` serves the Rust-written report and plays audio — run `lks execute` under Rust, then `lks web` with `--no-open`, open `http://127.0.0.1:8765/?run=<run_id>`, verify timeline markers/cues/tool bands render and conversation audio plays; then `curl -s localhost:8787/api/v1/runs` returns the same list shape as Python `lks` REST.
- **Deps added:** axum 0.8.9, tower-http 0.6.11 (features `fs`, `trace`, `catch-panic`), rust-embed 8.12 (features `debug-embed`, `compression`, `mime-guess`), tokio (full), serde_json (`preserve_order`), jiff 0.2.35, hound (already at P2), tower (via axum).

#### P4 — Asserts + suite + CLI parity
- **Scope:** `asserts.py` port (tool asserts, transcript asserts, SIP asserts — R4 `asserts.py`), `suite.py` (`evaluate_run_result`, gate, suite matrix + suite-*.json/md), full CLI (all 23 commands — 22 data + `mcp`) with `--json` + exit-code semantics, `cli_render` human renderers, `execute-dict`, `execute-all` (parallel + wait). **Enrichment (Appendix G §1): scenario_from_dict.rs lands here (REVISED — moved from P6).**
- **Files:** `crates/lks-core/src/{asserts,suite,scenario_from_dict}.rs` (pure logic — REVISED: scenario_from_dict moved from P6), `crates/lks/src/{cli.rs,cli_render.rs,ops.rs}` (binary crate: CLI + ops glue over core + livekit), `crates/lks/tests/{cli_parity,asserts,suite_gate}.rs` (asserts/suite tests live with lks-core; CLI parity tests with the binary).
- **Steps:**
  1. Asserts: port each assert kind with identical spec payloads and `assert.verify` events: tool asserts (`tool_called`/`tool_not_called` with name/calls/times?), transcript asserts (`transcript_contains`/`agent_said`/`user_said` with text/mode/turn windows), SIP asserts (`sip_participant`/`sim.leg_error` context), and `goals_met` with `min_goals: int = 0` (default `min_goals=1` from `int(raw.get("min_goals", 1))`). **`judge_goals` is NOT a PassCriteria field — it is driven by AssertSpec outcome type "goals_met". There is NO `min_confidence`, `must_include`, or `must_avoid` anywhere — do not port them.** SIP asserts need `sim.leg_error`/telephony context (P6 data stubbed in tests).
  2. Suite gate rule table (`evaluate_run_result(result, strict_judge)` — hard-reason ORDER matters for reports): hard reasons = not_executed (+error), validation_failed, status != "done", assert_verify.pass false (unless skipped True), script_verify.pass false; judge: verdict "fail" → soft "judge_fail", hard "judge_fail" ONLY if strict_judge; "maybe" → soft "judge_maybe", hard only if strict_judge; "error" → soft "judge_error", NEVER hard (even strict); "skipped" → ignored. `ok = no hard reasons`. gate ∈ {"hard","soft","pass"}.
  3. pass@k: `k = pass_at_k or repeat`; `ValueError("repeat must be >= 1")` for repeat < 1; `ValueError` if k > repeat; `ok = hard_passes >= k`; in the ops.py path gate is computed WITHOUT strict_judge (strict only enters via suite/optimize).
  4. Transport-drop retry (ops.py:509-524): Gemini Live 1006 abnormal closure retried ONCE per iteration before gating; result marked `retried_from_drop`.
  5. CLI: clap derive for all 23 commands (22 data + `mcp`), flags identical (incl. `--repeat`/`-n`, `--pass-at-k`/`-k`, `--name`, `--agent-name`, `--optimized`, `--strict-judge`, `--no-report`, `--parallel`/`-p`, `--wait`, `--baseline` + the four `--max-*-regression-ms` options — `--max-ttfw-regression-ms`, `--max-turn-p95-regression-ms`, `--max-duration-regression-ms`, `--max-barge-recovery-drop` — there is NO `--max-ttfa-*`/`--max-turn-audio-*`/`--require-status-done` on the CLI, matching `cli.py`), `--no-open`, `--host`/`--port`, `--no-connectivity`, `--resolve`, `--root`, `--json`); exit codes: 0 success, 1 failure/gate-fail (incl. ConfigError/ScenarioError/RuntimeError), 130 on Ctrl+C (REVISED — Python `_run` raises `typer.Exit(130)`; clap/`tokio::signal` must map to exit 130; message "Interrupted — stopping.").
  6. **execute-dict**: `scenario_from_dict.rs` — in-memory dict path (same section mapping as YAML; `run` alias for execute; `pass_criteria` dict → parse_pass_criteria, non-dict → flat `[str(c) for c in raw]`; `pass_judges` non-dicts dropped silently; `assert`/`asserts` alias; `plugin_modules`/`plugins` alias). `persona.brief` check runs BEFORE dispatch JSON and first_speaker checks (validation order matters for error precedence). Distinct error string: `{path}: persona.brief is required`.
  7. Renderers: port `cli_render.py` output text (golden test against Python `--json`-driven runs where deterministic; human renderers golden-tested for the table shape, not byte-locked — AGENTS.md one-clear-API means same *information*, cosmetic table width may differ; note in test doc).
- **Tests:** `cli_help_lists_all_commands` (23 commands — 22 data + `mcp`), `cli_as_json_shapes`, `execute_dict_stdin`, `execute_all_parallel`, `suite_gate_fail_exits_1`, `suite_gate_no_criteria_strict` (E42), `asserts_emit_verify_events`, `compare_baseline_gate` (port test_baseline_compare.py vectors), `repeat_lt_one_errors` (E39), `pass_at_k_gt_repeat_errors` (E40), `transport_drop_retried_once` (E46), `goals_met_gate_vectors` (E55).
- **Acceptance:** offline/CI green; MANUAL: `lks execute --repeat 2` on demo target with gate behavior matching Python.

#### P5 — MCP server
- **Scope:** `rmcp` server exposing the same 21 tools with same names/params/return JSON. **Enrichment (Appendix G §2): scenario_from_run.rs lands here (REVISED — moved from P6) — it is the `scenario_from_run` MCP tool, part of the P5 21-tool surface.**
- **Files:** `crates/lks-mcp/src/lib.rs` (rmcp server, 21 `#[tool]`s, thin over the `lks` ops layer via `lks-core`+`lks-livekit`), `crates/lks-core/src/scenario_from_run.rs` (REVISED — moved from P6; pure YAML promotion), `crates/lks-mcp/tests/{mcp_tool_surface,mcp_client_harness}.rs`.
- **Steps:**
  1. `#[tool]` per Python `@mcp.tool` function; param names exact (`project_root`, `scenario_id`, `run_id`, `repeat`, `pass_at_k`, `run_name`, `agent_name`, `optimized`, `strict_judge`, `tag`, `write_report`, `parallel`, `wait_s`, `held_out`, `candidates`, `max_candidates`, `name`, `force`, `connectivity`, `limit`, `kind`, `turn`, `source`, `since_mono_ms`, `run_id_a`/`run_id_b` + **`max_ttfw_regression_ms` (1500) / `max_turn_p95_regression_ms` (2000) / `max_duration_regression_ms` (30000) / `max_barge_recovery_drop` (0)** + `baseline` — REVISED: the MCP `compare_runs` tool surface is the **4 gate params + `baseline`** from `mcp_server.py`/`cli.py`/`ops.compare_runs_with_baseline`. The extra internal gate knobs `max_ttfa_regression_ms` (2000) / `max_turn_audio_p95_regression_ms` (2500) / `require_status_done` (true) exist ONLY on `ops.evaluate_baseline_gate` and are never surfaced on any Python public path (they run at their defaults at every call site — `ops.py:1011` passes only the 4) — do NOT expose them on the MCP/CLI surface. The internal Rust gate may still implement the full 7 checks, mirroring `evaluate_baseline_gate`).
  2. Return shapes: port `ops.py` dicts verbatim (they are already JSON-serializable; Rust `serde` structs with `#[serde(rename_all = "camelCase")]` where Python uses camelCase — verify per op, e.g. `run_id`, `report_dir`).
  3. `scenario_from_run` tool: promote finished run → draft scenario YAML (dry-run default; `write=True` writes `.agent-sim/scenarios/<id>.yaml`; returns scenario_id, yaml text, warnings, stats). Params: `run_id`, `scenario_id`, `write`.
  4. `compare_runs` gate (`baseline=True`): golden run A vs run B; hard-fails regressions beyond the thresholds.
  5. `lks mcp` subcommand spawns the server (stdio transport, same as Python's console script `lks-mcp`).
- **Tests:** `mcp_tool_surface`: list tools from both servers (Rust + a Python sidecar in CI, `uv run` — optional if CI lacks uv; alternative: golden JSON of tool list + param schemas) and assert name/param parity. `mcp_client_harness`: drive `init_project`/`list_scenarios`/`validate_scenario` through a real rmcp client against the in-process server.
- **Acceptance:** tool-surface golden test green (research §6: "Tool-surface golden test: run both `lks mcp` servers against a scripted client, diff tool lists + call shapes"); MANUAL: point Claude Code / any MCP client at the Rust server and run `guide` + `execute`.

#### P6 — Remaining sim legs (inbound_sip, outbound_sim_callee, agent_dials, outbound_human_pickup)
- **Scope:** `inbound_sip`, `outbound_sim_callee`, `agent_dials`, `outbound_human_pickup` (REVISED — the 4 non-webrtc legs from CALLER_MODES; the plan previously listed `outbound_callee` which is not a real mode), `sim_callee` (callee-side call handling), `room_resolve` (room naming + prepare_ms + wait-until-answered), telephony config checks. **P6 shrinks to sim legs + telephony checks only** (REVISED — behavior_compile/persona_traits/caller_policy moved to P2; farewell/hang_up_gate/summary/end_call/degradation moved to P3; scenario_from_run moved to P5; scenario_from_dict moved to P4).
- **Files:** `crates/lks-livekit/src/livekit/sim_leg/{inbound,agent_dials,human_pickup,sim_callee,room_resolve}.rs`, `crates/lks-core/src/telephony.rs` (config checks — pure), `crates/lks-livekit/tests/{sim_leg,telephony}.rs`.
- **Steps:** port each leg using `livekit-api` `services::sip` (SIP participant creation, dialing — research §1.5); DTMF via `LocalParticipant::publish_dtmf(SipDTMF)` (REVISED — verified at R1 `local_participant.rs:783`; not `livekit::webrtc::SipDTMF` — the struct lives in `livekit::room` and `publish_dtmf` is on `LocalParticipant`); the demo `dtmf-feature` suite exercises `dtmf` script cues; leg events (`sim.leg_error`, hold_timeout, human_pickup semantics) per R4 `sim_leg/*`; telephony fail-fast messages per P1 step 10 gated per leg. `room_resolve`: room naming + prepare_ms + wait-until-answered (`inbound.answered`/`inbound.dial_started`/`inbound.dial_failed`/`inbound.agent_room_discover`/`inbound.agent_room_resolved`/`inbound.agent_room_deterministic`, `outbound.prepare`/`outbound.dial_started`/`outbound.dial_answered`/`outbound.dial_failed`/`outbound.wait_agent_dial`/`outbound.handset_isolated` event names per R4 sim_leg/*). **Timing constants (REVISED — F9, from R4 sim_leg/*): room `empty_timeout = 300` s / `max_participants = 8`; `wait_for_sip_participant` timeout 30 s with 400 ms poll; `agent_dials` wait `max(60_000, join_timeout)`; `sim_callee` hairpin wait `min(3000, max(1500, prepare_ms))`; inbound prepare 10 s; `room_resolve` polls 400 ms (resolve) / 300 ms (sip-call-id) + freshness checks; identity strings `lks-caller`, `lks-obs-{run_id[:8]}`, `sip-in-{run_id[:12]}` / `sip-out-{run_id[:12]}`; rooms `lks-{run_id}` / `lks-sip-{run_id}`; token VideoGrants = `room_join` + `room` ONLY (no other grants).**
- **Tests:** `sim_leg_factory_selects_leg`, `inbound_sip_creates_sip_participant` (mock livekit-api HTTP), `outbound_dials_number`, `dtmf_cue_sends_sip_dtmf` (mock — assert `publish_dtmf(SipDTMF{code, digit})` payload maps `0-9*#w` per R4 `script/runtime.py` DMAP `{"0":0.."9":9,"*":10,"#":11}`, `w` → sleep 0.12 s, digit → publish + sleep 0.15 s), port of `tests/test_sip_asserts.py` + `test_room_resolve.py` vectors (E47), `test_hold_timeout.py` vectors (E48), `room_full_fails_cleanly` (E49). MANUAL: inbound/outbound real smoke against a SIP-enabled project (needs creds; gate: same events as Python run).
- **Acceptance:** offline green; MANUAL per-leg real run.

#### P7 — Judge / evals
- **Scope:** PassCriteria LLM judge (default Gemini via REST, `judge.endpoint_type: http_openai`/`http_anthropic` custom endpoints) + `evals/` (relevancy, evidence, aggregate, presets, runner) used by `optimize` (P9). **Enrichment (Appendix G §4): full judge contract — the `judge.verdict` event + summary verdict field already landed in P3's writer (event kind union + `writer.finalize(status, meta, verdict=...)` are P3 report parity); only the LLM backends are P7.**
- **Files:** `crates/lks-core/src/evals/{mod,backends,relevancy,aggregate,evidence,presets}.rs` + `crates/lks-core/src/judge.rs` (judge logic + REST backends — pure HTTP, no livekit), `crates/lks-core/tests/{evals,judge_http}.rs`. (The `judge.verdict` event + summary verdict field already landed in P3's `lks-core` writer.)
- **Steps:**
  1. Judge resolution precedence: `endpoint_type = config.judge.endpoint_type → JUDGE_ENDPOINT_TYPE env → "openai"`; base_url set → http mode (needs `judge.api_key` or `JUDGE_API_KEY` else NOT ready, skip_reason "HTTP judge needs judge.api_key or JUDGE_API_KEY."); no base_url → gemini mode using the SIMULATOR api_key (skip_reason "Gemini judge needs a Gemini key (simulator.api_key with provider: google; no base_url)."); `base_url.rstrip("/")` in the ready case. Model: `judge.model → JUDGE_MODEL → "gemini-2.5-flash"`. Temperature f64 default 0.0 (no range validation).
  2. Backend wire formats byte-exact (Appendix G §4): OpenAI: endpoint `base` or `{base}/chat/completions`, body `{"model", "temperature", "stream": False, "messages": [{role system, content system}, {role user, content user}], "response_format": {"type": "json_object"}}`, headers `Authorization: Bearer`, `Content-Type: application/json`, `Accept: application/json`, timeout 180 s. Anthropic: endpoint `base` or `{base}/messages`, body `{"model", "max_tokens": 2048, "temperature", "stream": False, "system", "messages": [user only]}`, headers BOTH `Authorization: Bearer` AND `x-api-key` AND `anthropic-version: "2023-06-01"`, timeout 90 s. No retries anywhere. Error strings: `HTTP judge {code}: {err_body}` (err_body = first 500 chars, errors="replace"), `HTTP judge unreachable: {e}`, `HTTP judge empty choices: {payload[:300]}`, `HTTP judge returned empty content`, `HTTP anthropic judge {code}: {err}`, `HTTP anthropic judge unreachable: {e}`, `Anthropic judge empty content: {payload[:300]}`. Gemini REST: `generateContent` with `contents=[{role:user, parts:[{text:user}]}]`, `generationConfig.systemInstruction`, `temperature`, `responseMimeType:"application/json"`; parse `candidates[0].content.parts[].text`; empty → `Gemini judge returned empty content`.
  3. Content-list gateways: openai joins parts with `type in (None, "text")`; anthropic SKIPS `type=="thinking"` parts.
  4. Judge prompts: `JUDGE_SYSTEM` verbatim (59-line block — port byte-for-byte incl. the JSON structure spec with `\"verdict\": \"pass\"|\"fail\"|\"maybe\"` etc.). `build_user_prompt` parts joined with "\n": `"PASS CRITERIA:"` + `- {c}` per criterion; blank; `"TRANSCRIPT:"` + transcript or `"(empty)"`; blank; `"TOOL SPANS:"` + tool_spans or `"(none)"`; if flow_digest: blank + `"FLOW EVENTS (node lifecycle — strong evidence for hold/advance behavior):"` + digest; if goals_met: blank + `"NOTE: This is a goals_met check. Evaluate whether the CALLER (simulated human) stated or pursued each listed goal. Agent responses alone do not satisfy caller goals."`
  5. Verdict normalization: verdict string lowercase; in {pass, fail, maybe, skipped, error} else → "error". Missing → "error". Confidence in {low, medium, high} only. `parse_judgment_payload` field aliases (criterion/evidence/issue/severity/agent_line/why/impact/title/recommendation/improvement/how_to_improve/point/item/works/rationale/pass/reasoning/judge_id). `to_dict` omits keys when falsy (needs_human_review/critical_failure only when True).
  6. **`_parse_llm_json` / truncated-JSON repair** — 6-step algorithm EXACTLY (Appendix G §4): fence strip → trim to first `{` → char walk (`"` → `_json_string_end` with backslash skip-2; `{[` push; `}`/`]` mismatch → truncate at index and break; top-level `}` close → truncate after it) → unterminated trailing string: drop trailing `\` then append `"` → close open containers LIFO with original opener char, strip trailing comma before each close, NO rebalancing → final cleanup regex `,\s*([}\]])` → `$1`, repeated endswith(",") strip, trailing `\` strip → dangling-key regex `,\s*"[^"]*"\s*([}\]])$` → cut from the comma, keep the closer (comma-preceded key position only; `: "value"}` must survive) → final gate: must `json.loads` to a dict else None. Any failure → `JudgmentResult(verdict="error", notes=f"Judge returned non-JSON: {text[:500]}")`.
  7. judge_goals gate (run_orchestrator.py:616-660): criterion string verbatim `The simulated caller stated or pursued the following goal(s) before the call ended: {goals}. Verify at least {min_goals} of {len(goals)} goals were explicitly mentioned or pursued.` (`{goals}` = Python `str(list)` — replicate brackets/quotes for byte-identical prompts). goals_met=True adds the NOTE block. Skip cases (judge_cfg None / not ready / backend None / no criteria) → verdict "skipped" + score 0, `assert.goals_met` emitted with pass=True, skipped=True, status NOT flipped. Exception → soft-skip with error note. Gate: `gs = int(score or 0)` (TypeError/ValueError → 0); `goals_pass = gv == "pass" and gs >= 50` (score >= 50 threshold applies ONLY here); fail → status "failed" + `meta["goals_failed"]` append oc.id. Empty goal list → skipped entirely. "verdict never flips status": skipped/error NEVER flip run status.
  8. Multi-judge aggregation (`aggregate_judges`): verdict_points pass=1.0, maybe=0.5, else 0.0. all-error → `{"verdict":"error","score":None,...,"notes":"multi-judge errors: {notes}"[:500],"needs_human_review":True}`. all-mode: scored = non-error/non-skipped rows; none scorable → error aggregate "multi-judge: no scorable groups (errors/skips only)"; ok = all pass; soft = all in (pass, maybe). any: ok = ≥1 pass; soft = no pass, ≥1 maybe, 0 fails. majority: ok = passes > n/2; soft = (passes + 0.5*maybes) > n/2 (skipped count in n, contribute 0). Score: mean of numeric scores (any/majority: all rows; all-mode divisor = len(scored)); if none numeric → verdict-points mean × 100 (n per mode). needs_human_review = any judge flag OR verdict=="maybe". Combined top-level: `{verdict, score, mode, judges, passed_count, failed_count, maybe_count, needs_human_review, notes: f"multi-judge mode={mode_l}: {len(passes)}/{n} passed", overall_summary, strengths, issues, missing_checks, language_naturalness}`.
  9. Relevancy post-processing (runs on EVERY single-judge result and each multi judge BEFORE aggregation): skipped/error/empty criteria → unchanged; no relevant → replace(verdict="maybe", confidence=result.confidence or "low", needs_human_review=True, notes + " All criteria marked irrelevant."); unmet relevant → verdict="fail", needs_human_review = result.needs_human_review or (confidence == "low"); all relevant met: verdict "fail" → "pass"; final verdict sanitized to pass/fail/maybe else "pass".
  10. Evidence packet (byte-exact): transcript `Caller` / `{mm:ss.xx} - {mm:ss.xx}` / text lines with cumulative `turn_taking_ms` (missing → 0), empty → "(empty)"; tool spans one JSON object per event `{kind, turn, name, error, duration_ms}` from `spec`; flow digest `k=v | k=v` excluding keys `_seq` and `ts`, fallback `json.dumps(payload, ensure_ascii=False)`; empty → "(none)". Flow events = kind "data.message", source ∈ observe.flow_topics, spec.payload non-empty dict.
  11. judge.verdict event: `writer.emit("judge.verdict", spec=verdict or {}, include_dialogue=False)` — spec is the FULL aggregated verdict dict; stored via `writer.finalize(status, meta, verdict=verdict)` into summary.json `verdict`.
  12. Presets (9 keys exact): task_completion, factual_accuracy, policy_compliance, conversation_flow, conversation_naturalness, empathy, escalation, accuracy, coherence. `expand_criterion`: "builtin:<key>" → PRESETS[key], unknown → KeyError(`Unknown judge builtin preset: {key}`); plain strings pass through. conversation_naturalness rubric = the 5-rule PM rubric (verbatim from `presets.py`).
  13. evals backends/runner: `backend_from_config` returns None when not ready; `_judge` catches exceptions → error verdict dict `{"verdict":"error","notes":f"Judge backend setup failed: {type(e).__name__}: {e}","score":0}`.
- **Tests:** `judge_verdict_event`, `judge_http_openai_mock`, `judge_http_anthropic_mock`, `evals_relevancy_vectors` (E57), `evals_aggregate_vectors` (E34/E35, port test_evals_judge.py), `judge_json_repair_vectors` (E33), `judge_skipped_no_config` (E36), `judge_http_timeout` (E37), `evidence_empty_transcript` (E38), `evals_parse_vectors` (E56). MANUAL: real judge run (needs API key).
- **Acceptance:** offline green; MANUAL judge smoke on a real scenario with `pass_criteria`.

#### P8 — Plugins (pyo3 or drop)
- **Scope:** run existing `.py` verify plugins (`before_run`/`after_run`/`verify` from `templates/plugins/example_verify.py` + `docs/plugins.md`) unchanged via embedded CPython. (Appendix G §5: no additions; `plugin_modules`/`plugins` aliases confirmed; missing module must fail validate.)
- **Files:** `crates/lks-plugins/src/{lib,api,loader}.rs` (pyo3 embedded CPython; `api.rs` exposes the same plugin API surface the .py files import; loader semantics per `plugins/loader.py`+`registry.py`), `crates/lks-plugins/tests/plugins_python.rs`. Deps: `pyo3 0.27` (abi3). `lks-plugins` depends on `lks-core` only — the pyo3/CPython runtime stays out of core and out of the livekit crate.
- **Steps:**
  1. Add pyo3 0.27 (abi3, `extension-module` off; interpreter = embedded CPython 3.11+).
  2. Bridge: the plugin modules `import` names from `livekit_agent_simulator.plugins.api` — provide a Rust-initialized module with the same names/functions (verify signature `(events, run context) -> verify result` per docs/plugins.md).
  3. Loader: `plugin_modules` in scenario → `PyModule::from_code` + call `before_run`/`after_run`/`verify` with the same arguments Python passes (port `plugins/loader.py` + `plugins/registry.py` semantics). YAML `plugin_modules: []` edge (non-list → `Plugins.spec.modules must be an array` error only for JSONL; dict path accepts lists).
  4. Decision point (documented, not blocking P8 start): if pyo3 embedding proves unacceptable (binary size, abi3 vs python version, CI image complexity), **drop plugin support from v1** and fail `validate_scenario` with a clear "plugins require the Python build" error when `plugin_modules` is set; document in README. A plugin that errors loudly beats a silently-skipped one.
- **Tests:** `plugins_python_executes_example` (run `templates/plugins/example_verify.py` against a synthetic events log → same verdict dict as Python), `plugins_missing_module_fails_validate` (E43).
- **Acceptance:** offline green; MANUAL: run a scenario with `plugin_modules` against a real target and compare `script.verify`/plugin output vs Python.

#### P9 — Optimize
- **Scope:** `lks optimize` + `optimize_persona` MCP tool (persona-prompt optimizer: baseline + LLM-proposed variants, evaluate over dataset, held-out generalization check, winner selection, write `.agent-sim/optimized/<name>/prompt.yaml`, `--optimized` apply). **Enrichment (P9 steps 1–6 below): full optimizer contract.**
- **Files:** `crates/lks-core/src/optimize/{mod,variant,eval,apply,mutate}.rs`, `crates/lks-core/tests/optimize.rs`.
- **Steps:**
  1. `PromptVariant`: frozen dataclass {id, verbosity? (quiet|natural|chatty), section_order? (subset of the 10 SECTION_NAMES), extra_guardrails?, extra_lines? {section: [lines]}, parent_id?, description?}; prompt.yaml = yaml dump omit-empty (variant_to_dict only includes non-empty fields; sort_keys=False, allow_unicode=True). Validator: verbosity in VALID_VERBOSITY; section_order ⊆ SECTION_NAMES no duplicates; extra_lines keys ⊆ SECTION_NAMES; errors: `optimized prompt artifact must be a mapping` / `invalid optimized prompt artifact: {joined}`.
  2. Winner selection (`select_winner`): candidate must STRICTLY beat baseline pass_rate; if heldout present must ALSO have heldout pass_rate >= 1.0 AND heldout ok (threshold constant 1.0); None → keep baseline, no prompt.yaml written.
  3. Optimize loop: train_ids = scenario_ids minus held_out (heldout_ids empty when held_out ∈ scenario_ids → no generalization check); baseline first; deterministic candidates first (dedup by id): verbosity-chatty, verbosity-quiet, reorder-constraints-first ("Role","Constraints","Goals","StyleTraits","NaturalSpeech","SpeechConditions","Context","ScriptTiming","FirstSpeaker","Guardrails"), guardrail-role-lock ("Never switch into assistant mode or offer to help — you are the caller."); then LLM proposer for up to max_candidates - len(seen) more; proposer exceptions swallowed; candidates capped at min(candidates=4, max_candidates=6); stage dir `__candidate__{id}` removed in finally; artifacts under `.agent-sim/optimized/<name>/` slug `optimize-{UTC %Y%m%d-%H%M%S}`: candidates/<id>.yaml, baseline.json (incl. the held_out "scenario_ids" always-[] quirk — port or fix knowingly), diff.txt (`=== candidate <id> (pass {rate:.0%}) ===` + unified diff), prompt.yaml ONLY if winner.
  4. `--optimized` apply (`_resolve_caller_policy`): None if not optimized; artifact `.agent-sim/optimized/<name>/prompt.yaml`; missing → ConfigError(`No optimized prompt at {artifact} — run `lks optimize --name {optimized}` first`). `policy_for_variant`: reorder build_default_sections per section_order (subset semantics: unlisted defaults appended at end; unknown names skipped); GuardrailsSection rebuilt with extra_guardrails + extra_lines["GuardrailsSection"] (other sections' extra_lines skipped); policy_source `variant:{id}`. Verbosity override into persona speech_conditions (handles speech_conditions/speechConditions keys). NOTE: `optimize_persona` stage param is `optimize` but `execute_scenario` kwarg is `optimized` — same string (stage name = artifact dir name).
  5. Proposer backend: reuses resolve_judge + backend_from_config with cfg.judge + cfg.simulator.api_key; not ready → RuntimeError(skip_reason or "judge not configured"). `_PROPOSER_SYSTEM` verbatim (gen.py:17-32). `_parse_candidates`: strip fences via split("```", 2), slice first "[" to last "]", json.loads → list, per-item setdefault id `llm-{i}`, invalid variants skipped.
  6. strict_judge in optimize: evaluate_variant → evaluate_run_result(result, strict_judge) — the suite gate from P4 item 2 is the optimizer metric.
- **Tests:** `optimize_no_winner_keeps_baseline` (E58), `optimize_writes_artifact`, `optimized_flag_applies_prompt` (port test_optimize.py vectors with a fake eval backend), `optimized_missing_artifact` (E59).
- **Acceptance:** offline green; MANUAL: one real `lks optimize` run on the demo target (needs keys).
- **CUT check (AGENTS.md no-dead-features):** if the `optimize_persona` MCP tool or `lks optimize` CLI has no user flow at P9 time (no consumer uses it), this phase shrinks to `optimize` ops returning a clear "not supported in Rust build" error and the P9 acceptance becomes "error is explicit, tests cover it" — decide at P9 kickoff against the Python tool-usage log. Default recommendation: port it (it has an MCP tool + CLI + docs recipe = a documented user flow).

#### P10 — Packaging (install.sh/install.ps1 adaptation, cross-platform CI release)
- **Scope:** release pipeline (GH Actions release workflow: matrix build macOS aarch64/x86_64 + Linux x86_64; `cargo build --release`, strip, tar.gz/zip per platform, sha256sums, GH Release assets), `install.sh` adaptation (same curl|bash UX, download the Rust binary tarball instead of the Python zip; keep `lks-mcp` alias wiring), `install.ps1` (research §1.6 VERIFY gate: only if a Windows Rust build is proven in CI; else keep Python zip for Windows and document), `web/dist` embed at build time (rust-embed `debug-embed` dev / `compression` release — decide size vs speed; R5 web/dist is ~200 KB, measured — REVISED).
- **Files:** `.github/workflows/release.yml`, `scripts/release.sh`, `install.sh` (rewrite), `install.ps1` (rewrite or leave), `crates/lks-web/assets/web/` (web/dist copy step in build.rs or CI), `docs/portability.md` note.
- **Steps:**
  1. Release workflow: build + test on matrix (macos-14 aarch64, ubuntu-latest, windows-latest optional), upload artifacts, create GitHub release with checksums (repo has GH Releases precedent: install.sh references `v0.1.0` releases).
  2. Rewrite `install.sh` to fetch `<release>/lks-<os>-<arch>.tar.gz`, verify sha256, install to `$HOME/.local/bin` — REVISED: preserve the actual current flags: `DEST`, `INSTALL_ROOT`, `LK_SIM_REF`, `CURRENT_DIR` symlink semantics, `--easy-mode` (appends DEST to PATH in shell rc), and the `--with-mcp`/`--with-rust-mcp` flag set if present — read the current script and preserve every flag.
  3. Verify `lks mcp` stdio server works from the installed binary (MCP clients exec `lks-mcp`).
- **Tests:** CI smoke: installed-binary smoke (`install.sh` from the draft release on ubuntu/macos runner → `lks --version` + `lks guide`), `cargo test` release-profile pass. MANUAL: fresh-machine install + one real `lks execute`.
- **Acceptance:** release assets downloadable and installable on macOS + Linux; Windows documented (research §1.6).

---

## Cross-validation strategy (Python ↔ Rust)
1. **Golden artifacts from Python.** Before each parity phase, capture Python outputs (events.jsonl, runs.sqlite, summary.json, meta.json, export/validate JSON, MCP tool list) for the template scenarios and a real run as `tests/golden/` fixtures. **REVISED — the R5 fixture cited in earlier revisions (`114-people-pleaser-refuse-card-20260809-201652-8b32`) is NOT in this repo (it lived on the author's machine, not in git); the Aug-11 reports in `demo/base-agent/.agent-sim/reports/` (e.g. `001-frontdesk-hours-20260811-071837-8197`) predate the audio-onset metrics commit and are a 31-key fixture — they must NOT be used for `summary.json`/`metrics` parity, which targets the current 36-key block (see P3 step 4).** The fixture must be captured fresh from a current Python run before P3/P3.5 parity. Rust golden tests diff against these byte-for-byte where deterministic (I1/I2) and key-for-key where time-dependent (ts fields normalized via seed/freeze).
2. **Cross-read DB test (P3):** Rust-written `runs.sqlite` read by Python `RunStore`; Python-written read by Rust (CI job with `uv run` sidecar, or a committed fixture DB).
3. **compare_runs across implementations (P4+):** same scenario id + same target run under Python and Rust → both report dirs under one `.agent-sim/reports/`; `lks compare <py-run> <rust-run>` (from either implementation) shows only non-deterministic diffs (ts, run_id, wav bytes) and zero structural diffs; the `--baseline` gate (max_ttfw_regression_ms etc., default 1500/2000/30000/0) is exercised as a regression check, not a pass gate (caller model timing differs run-to-run).
4. **Suite-level parity (P4/P6):** `execute-all` over the template scenarios under both implementations → identical pass/fail verdicts for hard gates (script_verify/assert_verify), judge verdicts excluded from strict equality (LLM non-determinism) and compared only for presence/shape.
5. **MCP surface parity (P5):** tool-name/param-schema golden diff (see P5 tests).

## Test strategy
- **Unit:** `tests/` per module with no network: mock WS server (`mock_ws.rs` — a tokio-tungstenite server speaking a scripted protocol), fake LiveKit via `livekit`'s `RoomOptions`/event mocks where the SDK allows (else abstract `RoomHandle` trait in `livekit/room.rs` so tests inject fake `RoomEvent` streams — the trait is ours, SDK stays behind it; VERIFY at P2 what the SDK exposes for tests, fallback is the trait), mock HTTP for livekit-api and judge backends (axum test server), time frozen via injected `Clock` (deterministic `ts`/`datetime_utc` for golden tests).
- **Integration (CI-gated optional):** real LiveKit room + real agent — only in the repo's own demo project with keys in CI secrets (marked `#[ignore]` + `LK_SIM_CI_LIVE=1` env gate; the P2/P3 MANUAL gates double as this). Never in default CI (no keys).
- **Golden:** `tests/golden/` fixtures from Python (above); regenerate script `scripts/refresh_golden.sh` that runs the Python package to re-capture fixtures and diffs them (`git diff` reviewable).
- **Porting vectors:** for every Python unit test that is deterministic and offline, port the *vectors* into Rust tests (test_config.py, test_scenario*.py, test_metrics.py, test_event_writer.py, test_asserts.py, test_mic_mixer.py, test_observer.py, test_suite.py, test_plugins.py, test_evals_judge.py, test_baseline_compare.py, test_gemini_reconnect.py (via mock_ws), test_openai_realtime.py (via mock_ws), test_room_resolve.py, test_sip_asserts.py, test_cues.py, test_interrupt_rate.py, test_hold_timeout.py, test_end_call.py, test_hang_up_gate.py, test_silent_mode.py, test_script*.py, test_web_server.py, test_rest_api.py, test_cli_render.py…). Non-deterministic tests (real latency) stay MANUAL.
- **Edge-case catalog** (Appendix H — every row is a CI-gated offline test; the "Test name" column is the Rust test):

| # | Scenario | Expected behavior | Where handled |
|---|----------|-------------------|---------------|
| E1 | Empty event log (run ends with zero events) | events.jsonl exists with only `run.started`/`run.ended`; summary event_count=0; metrics default-safe (no div-by-zero); judge/verify handle empty transcript ("(empty)") | logging/event.rs, metrics.rs, evals (build_user_prompt) |
| E2 | Agent never joins (agent_join_timeout_ms, default 25_000) | run fails with agent-join timeout; status "failed"; end_condition agent_join_timeout; `run.end_condition` spec names it | run.rs end_condition loop |
| E3 | Dead call (silence 12 s) | VAD silence detection → dead-call end_condition (3 × silence_threshold_ms); `silence.detected`/`run.end_condition` events; conversation.wav still written | audio/vad.rs + run.rs |
| E4 | hang_up before agent speaks | hang_up step fires without agent activity; farewell heuristics must not require agent audio; verify counts tolerate zero agent finals | script/farewell.rs, script/verify.rs, callers/end_call.rs |
| E5 | Barge during scripted say (agent speaking when a scripted say cue fires) | cue still injected per its trigger; interruption event only when class counts for recovery barge (`counts_for_recovery_barge`); no double-play into mic mixer | script/runtime.rs, audio/mic_mixer.rs |
| E6 | Script step trigger agent_speaking never matches (agent never speaks) | cue never fires; no hang; run continues until timeout/dead-call; verify `min_agent_finals_after_barge_in` unmet → script_verify fail (hard gate) | script/runtime.rs, script/verify.rs |
| E7 | DTMF during agent speech | `LocalParticipant::publish_dtmf(SipDTMF)` maps `0-9*#w` per DMAP; `sim.script.dtmf` event emitted even while agent speaks (no barge semantics) | script/runtime.rs, callers via livekit |
| E8 | Silent mode end-to-end | midcall_cues=[] at bridge construction; `sim.silent_mode` event + meta["silent_mode"]=True; nudge skipped/never created (`sim.agent_greeted_nudge_skipped` reason silent_mode); interrupt-rate None (validation still runs first); auto-compiles skipped; explicit speak/barge/room_pcm steps dropped, hang_up say cleared; inject_cue → `sim.silent_mode_skip_inject` (text[:120]); SI gets SILENT MODE lines; `_persona_audio_allowed` False except script hang-up farewell; silence_ms<500 → silent_hold_ms or 12000 | run.rs, callers/base.rs, behavior_compile.rs, caller_policy.rs |
| E9 | first_speaker=user with no script | bootstrap midcall cue emitted at connect (`sim.caller_midcall` kind bootstrap, text[:240]); FirstSpeakerSection "You speak first: after the call connects, {open_hint}. Do this from persona — no separate cue."; no nudge task | callers/gemini.rs, callers/openai.rs, caller_policy.rs |
| E10 | first_speaker=user + script that owns opening (trigger time/silence + action speak + non-empty say) | NO bootstrap cue (would double-open); FirstSpeakerSection script branch "Opening: stay silent at connect. The simulator injects your opening line as audio. After that opening (and after each later injected milestone), answer the assistant freely in freestyle until the next injection — do not stay mute waiting for a text cue." | caller_policy.rs (script_owns_opening), prompt_sections.rs |
| E11 | first_speaker=user + script with only wait/hang_up steps | script_owns_opening False → bootstrap cue IS emitted; "You speak first" SI also present | caller_policy.rs |
| E12 | first_speaker=agent + script | no bootstrap; FirstSpeakerSection script+agent branch ("assistant will greet you first… Wait for that cue"); NO nudge task (requires not scenario.script_steps) | run.rs (nudge gate), prompt_sections.rs |
| E13 | Goal reground truncation | `goals[0][:120]` code-point slice, NO ellipsis, only goal 1, only when goals non-empty; injected only once per call (first kind==reground wins; goal_reground precedes script_no_early_bye) | caller_policy.rs, callers (inject_reground) |
| E14 | Goals list empty | no GoalsSection, no goal_reground cue, no "You have {n} numbered goal(s)" guardrail; midcall list may still hold bootstrap + script_no_early_bye | caller_policy.rs, prompt_sections.rs |
| E15 | Trait conflicts (quiet + chatty) | chatty beats quiet/silent/terse in resolved_verbosity (checked first); ALL trait bullets still expand (both texts present in SI) | caller_policy.rs, persona_traits.rs |
| E16 | Unknown trait tag | passthrough line "Additional caller behavior (follow naturally): {joined}." preserving original casing | persona_traits.rs |
| E17 | Unknown verbosity string | → natural with debug log "unknown speech_conditions.verbosity=%r; using natural" (NOT an error) | caller_policy.rs |
| E18 | Unknown interruption_rate | ValueError fail-fast: `Persona.speech_conditions.interruption_rate must be one of none\|low\|medium\|high (got {rate!r})` | interrupt_rate.rs |
| E19 | interruption_interval_ms < 1000 | ValueError fail-fast | interrupt_rate.rs |
| E20 | Gains outside [0,1] (noise_gain/barge_gain/gain) | ValueError with exact messages | behavior_compile.rs |
| E21 | user_silence hold_ms < 500 | ValueError `user_silence[{i}] hold_ms must be >= 500` | behavior_compile.rs |
| E22 | Nudge RuntimeError mid-loop | retry every 0.15 s until success or end_call; single fire; abort silently if end_call set or user spoke during 1.0 s debounce | caller_nudge.rs |
| E23 | Interrupt-rate: agent never spoke | loop silently waits (no events) until agent_has_spoken; no `sim.interrupt_rate_skip` before interval elapsed; re-arm on each new active burst; active = is_active_speaker OR last activity within 3.0 s | interrupt_rate.rs |
| E24 | Interrupt-rate blip inject fails | `sim.script.error` for the blip, main inject still attempted; main inject failure → `sim.script.error` + interruption event suppressed but `sim.script.cue` still emitted with error field | interrupt_rate.rs |
| E25 | Transcript dedupe collisions (same segment within transcript_dedupe_window_ms, default 15_000) | duplicate transcript events deduped; "transcript.user.final" emitted once per segment | livekit/observer.rs |
| E26 | Both .jsonl and .yaml present (same stem) | YAML SHADOWS in list_scenarios; find_scenario direct-file preference yaml→yml→jsonl then metadata.id scan (*.jsonl then *.yaml then *.yml sorted); a stray `.yaml.tmp` would route to JSONL parser (why atomic write validates first) | scenario.rs, scenario_yaml.rs, ops.rs |
| E27 | Config missing keys (api_key etc.) | ConfigError with exact `_require` message; preflight turns it into checks[0] fail and RETURNS immediately (result, None) — no folders created, no API check | config.rs, preflight.rs |
| E28 | Invalid YAML (config or scenario) | config: `{path} is not valid YAML: {e}`; scenario: `{path}: invalid YAML — {e}`; YAML empty/only-comments → `{path}: empty scenario file` | config.rs, scenario_yaml.rs |
| E29 | Unknown scenario kind (JSONL) | `{path}:{line}: unknown kind {kind!r} (expected one of {sorted(KNOWN_KINDS)})`; header not line 1 → `{path}:{header_line}: first line must have kind=Scenario` | scenario_jsonl.rs |
| E30 | Duplicate scenario ids across files | list_scenarios lists both; find_scenario returns FIRST match in iteration order (direct <id>.yaml beats metadata.id match; jsonl before yaml within scan) | scenario.rs |
| E31 | Scenario id invalid for query | find_scenario/init_scenario: ScenarioError `Invalid scenario_id {id!r}: use letters/digits/[_-], start with alnum, max 64 chars` (parse itself tolerates) | scenario.rs |
| E32 | Persona without brief | JSONL: `{path}: Persona.spec.brief is required — the simulator needs a caller brief`; YAML/dict: `{path}: persona.brief is required` (different strings!) | scenario.rs, scenario_from_dict.rs |
| E33 | Judge JSON repair (truncated LLM output) | 6-step repair: fence strip → first `{` → char walk → close containers LIFO → comma cleanup → dangling-key cut; repair result must parse to dict else `Judge returned non-JSON: {text[:500]}` (soft judge_error, never hard) | evals (runner port) |
| E34 | All judges error (multi) | aggregate {"verdict":"error","score":None,"needs_human_review":True,"notes":"multi-judge errors: {joined}"[:500]} — soft, NEVER fakes a fail | evals/aggregate.rs |
| E35 | Multi all-mode with errors/skips mixed | errors excluded from scored set; all remaining non-error must be pass; none scorable → error aggregate "multi-judge: no scorable groups (errors/skips only)" | evals/aggregate.rs |
| E36 | Judge not configured/not ready | verdict "skipped" with skip_reason (no judge block / HTTP needs api_key / Gemini needs sim key); same UX as no PassCriteria; orchestrator only calls judge when `cfg.judge is not None and scenario.pass_criteria` | evals (resolve), run.rs |
| E37 | Judge timeout (180 s openai / 90 s anthropic) | URLError → `HTTP judge unreachable: {e}` → error verdict; no retries | evals/backends.rs |
| E38 | Empty transcript to judge | `TRANSCRIPT:\n(empty)` placeholder; judge decides | evals/evidence.rs |
| E39 | repeat < 1 | ValueError("repeat must be >= 1") | ops.rs |
| E40 | pass_at_k > repeat | ValueError | ops.rs |
| E41 | Empty script (no steps) | script_verify trivially passes; verify spec not required; ScriptTimingSection renders []; no script_no_early_bye cue | script/parse.rs, caller_policy.rs |
| E42 | No PassCriteria with strict_judge | judge not invoked; strict_judge has nothing to promote; gate hard only from status/assert/script reasons | suite.rs |
| E43 | Plugin module missing | validate fails loud ("plugins require the Python build" per P8 fallback, or module-import error); never silently skipped | plugins/mod.rs |
| E44 | conversation.wav with no agent audio | recorder still writes valid 16 kHz PCM16 stereo frames (sim side only); no crash; meta.json audio block present with sample_rate 16000 | audio/local_recorder.rs |
| E45 | Run interrupted Ctrl+C | exit 130, "Interrupted — stopping."; writer flushes events.jsonl + finalizes summary/status; sqlite finalized | cli.rs, run.rs (tokio::signal + ctrlc) |
| E46 | Transport drop mid-call (no reconnect; transport_dropped) | Python parity: `transport_dropped=True`, no reconnect mid-call (reconnects only on GoAway with resume handle); `sim.gemini_socket_drop`/`sim.openai_socket_drop`; per D4/Open question 2 the Rust decision (fork-patch reconnect_on_drop vs accept auto-reconnect and emit drop on Reconnecting-without-GoAway) must never silently diverge; 1006 abnormal closure retried once per iteration before gating | callers/gemini.rs, ops.rs |
| E47 | SIP participant timeout | leg timeout → `sim.leg_error` with telephony context; SIP asserts consume it | livekit/sim_leg/*.rs, asserts.rs |
| E48 | hold_timeout firing (hold_music_timeout_s in [5.0, 300.0]) | hold_music_timeout_s from Execute > Persona.speech_conditions > None; on fire: leg event + end condition; error uses `:g` formatting | run.rs, livekit/sim_leg/room_resolve.rs |
| E49 | Room full (max_participants 8) | LiveKit join/dispatch failure surfaces as leg error → run failed with actionable message; no hang | livekit/room.rs, run.rs |
| E50 | run_id seq collision | seq auto-incremented from existing report dirs (`<seq>-<slug>-YYYYMMDD-HHMMSS-<4hex>`); the 4-hex suffix is random (secrets.token_hex(2)); on same-second collision retry with seq+1 up to 10_000 attempts (REVISED — seq++ retry disambiguates, not the hex) | run.rs (new_run_id) |
| E51 | Empty scenario file | JSONL → `{path}: empty scenario file`; YAML only comments → same (no line number) | scenario_jsonl.rs, scenario_yaml.rs |
| E52 | BOM / CRLF / invalid UTF-8 | YAML tolerates BOM; JSONL `json.loads` fails on BOM (replicate or strip); CRLF fine via splitlines; invalid UTF-8 propagates raw UnicodeDecodeError in Python — decide replicate or wrap (document) | scenario_jsonl.rs, scenario_yaml.rs, config.rs |
| E53 | YAML 1.1 coercion traps (`on:`, `0123`, digit strings) | unquoted `on/off/yes/no` → bool; digit-strings single-quoted on WRITE (`isdigit` or leading-zero-all-digits rule); "0x1F"/"1e5"/"-1" plain (1.1 traps) — emulation decision at P1 | scenario_yaml.rs (dumper) |
| E54 | Multi-doc YAML merge | list+list appends, everything else overwrite (dict+dict does NOT merge); group wrapper only on sections[0]; len != 1 → hard error | scenario_yaml.rs |
| E55 | judge_goals score semantics | `goals_pass = verdict == "pass" and int(score or 0) >= 50` (exact); non-int score → 0; fail → run status "failed" + meta.goals_failed; skipped/error never flip status | run.rs |
| E56 | LLM unknown verdict / missing score | unknown verdict → "error"; missing/non-numeric score → None; aggregation skips None scores | evals (parse_judgment_payload) |
| E57 | Relevancy edge matrix | all irrelevant → maybe + needs_human_review (+confidence "low" default); unmet relevant → fail (+human review if confidence low); all met but verdict fail → promoted to pass; skipped/error/empty-criteria untouched | evals/relevancy.rs |
| E58 | Optimize no winner | no prompt.yaml written (files.prompt.yaml False); baseline kept; proposer LLM failure swallowed (deterministic set stands); heldout empty when held_out ∈ scenario_ids | optimize/eval.rs, optimize/apply.rs |
| E59 | `--optimized` with missing artifact | ConfigError `No optimized prompt at {artifact} — run `lks optimize --name {optimized}` first` | ops.rs |
| E60 | Preflight config fail | checks[0] fail, (result, None) early return; no mkdir, no API call; run_orchestrator runs preflight but does NOT gate on it | preflight.rs |
| E61 | `_require` value `0`/`False`/`0.0` | PASSES (not None, not non-empty string); only None/whitespace-string fails | config.rs |
| E62 | observe `"false"` string traps | quoted `"false"` → bool True (silent inversion) — replicate via BoolLike or document divergence; affects lk_transcription/lk_agent_session/record_audio/audio_onset.enabled/wait_until_answered/krisp_enabled | config.rs, scenario.rs |
| E63 | tool_event_patterns invalid entries | silently dropped (filter, no error); observe_gaps computed from the surviving set | config.rs |

## Risks / mitigations

| Risk | Evidence | Mitigation |
|------|----------|-----------|
| libwebrtc build burden (C++ from source, long first build, rustflags) | research §1.6 (webrtc-sys, download_ffi.py, workspace README) | Use prebuilt webrtc-sys FFI downloads; CI `actions/cache` + sccache for `target/`; rustflags from `rust-sdks/.cargo/config.toml` copied into workspace; macOS aarch64 + Linux x86_64 matrix only in v1 |
| `gemini-live` community maintenance (0.1.x, single maintainer, stalled ~4 months) | research §2.3 | **Vendor/fork the crate** (MIT, ~1400 LOC core) with a ~3-part patch (REVISED — `language_code` on SpeechConfig; `reconnect_on_drop` knob; close-code surfacing per D4/Appendix D §3); wire protocol documented (R2 protocol.md); fallback hand-rolled client (~600–900 lines, Candidate C) |
| `gemini-live` reconnect semantics differ from Python by design (auto-reconnects mid-call) | research §2.3 gaps | Decision point at P2 kickoff (Open question 2): fork-patch `reconnect_on_drop` for exact parity, or accept auto-reconnect as more robust and emit `transport_dropped` on `Reconnecting`-without-GoAway; never silently diverge — document the choice in the phase note |
| LiveKit Rust SDK gaps (transcription plumbing, event surface) | research §1.3 VERIFY items | Derive transcripts from audio + `lk.transcription` text streams + agent-session byte streams exactly like Python `Observer`; `RoomHandle` trait keeps the SDK swappable; feature-probe at P2, fallback documented |
| MCP parity drift | research §5.5, §6 (21 tools) | Tool-surface golden test (P5): scripted client against both servers, diff tool lists + call shapes; same for REST API (`test_rest_api.py` vectors) |
| Dual-maintenance cost (Python + Rust) | — | Rust is the feature target; Python frozen for bugfixes after P3; new features land in both or neither (Non-goal 7); Python stays until P10 then removal is a separate tracked decision |
| Report byte-compat drift | R5 ground truth + R4 sources | I1/I2 golden byte-diff tests in every phase touching logging; `scripts/refresh_golden.sh` re-captures from Python when Python legitimately changes (bugfix) |
| Windows support | research §1.6 VERIFY (builds.yml lists platforms but unproven for lks) | `cargo check` on Windows in CI; `install.ps1` keeps Python zip until a Windows Rust binary is proven; documented in README |
| Windows SAPI (`sapi` delivery) | R4 `audio/sapi_tts.py` | `tokio::process` calling SAPI exactly as Python does; non-Windows behavior mirrored (clear error); covered by `test_sapi_tts.py` vectors |
| pyo3 embedding for plugins (binary size, ABI) | research §3 | abi3; plugins behind `plugins/` isolation; documented fallback = loud "requires Python build" error and validate-fail when `plugin_modules` set (P8) |
| `yaml_serde`/`chrono` formatting drift (YAML emit, datetime strings) | research §3 VERIFY notes | Golden tests pin emitted YAML (convert) and datetime formatting; use `jiff` strtime for `%Y%m%d-%H%M%S` and `chrono-tz` `%Y-%m-%dT%H:%M:%S%.3f%z` with `Z` substitution per R5 sample (REVISED — serde_yaml is archived; yaml_serde is the official fork) |
| Optimizer is live-benchmark only (expensive to validate) | R4 `optimize/` | P9 port mirrors Python algorithm; tests use fake eval backend; MANUAL real run gated |
| First-release install script regression | install.sh current flags (DEST, INSTALL_ROOT, LK_SIM_REF, --easy-mode) | P10 CI install smoke from draft release; preserve every existing flag (REVISED — flag list corrected) |

## Deliverables / acceptance (table)

| Phase | Deliverable | Acceptance gate | Gate type |
|-------|-------------|-----------------|-----------|
| P0 | Workspace + CI | `cargo fmt`/`clippy`/`test` green on matrix; `lks --version` works | CI (offline) |
| P1 | Config + scenario parse + convert + scaffolds | Golden tests vs templates + Python export JSON; redaction test | CI (offline) |
| P2 | Room + dispatch + Gemini caller POC + prompt composition | mock-WS tests green; **one real webrtc_sim run end-to-end with events.jsonl** | CI + MANUAL (live creds) |
| P3 | Audio + observer + full event writer + sqlite + summary | Parity suite: events bytes, sqlite cross-read, summary keys; **report parity with Python** (same scenario, normalized diff) | CI + MANUAL |
| P3.5 | Web report player + REST API | `parity_cues_golden` byte-diff; route/header tests; **`lks web` serves Rust-written report, REST list shape matches Python** | CI + MANUAL |
| P4 | Asserts + suite + full CLI | CLI surface tests; gate exit codes; assert events; **`lks execute --repeat` gate behavior matches Python** | CI + MANUAL |
| P5 | MCP server | Tool-surface golden test; scripted-client harness; MANUAL MCP client smoke | CI + MANUAL |
| P6 | SIP/outbound legs + DTMF | SimLeg unit tests; MANUAL per-leg real run | CI + MANUAL |
| P7 | Judge + evals | Mock-HTTP judge tests; evals vectors; MANUAL judge smoke | CI + MANUAL |
| P8 | Plugins (pyo3) | `example_verify.py` verdict parity; missing-module validate-fail | CI (+ MANUAL) |
| P9 | Optimize | Fake-backend tests; MANUAL real optimize run | CI + MANUAL |
| P10 | Packaging + release | Install smoke from draft release on macOS/Linux; Windows documented | CI + MANUAL |
| All | Invariants I1–I10 | Each phase's CI gate includes the relevant golden tests; final PR runs the full cargo suite + the parity suite | CI |

## Open questions (tracked with recommended answers)

1. **Workspace shape (RESOLVED 2026-08-13 — D1).** Earlier this was an open question recommending a single crate; it is now decided as the fine-grained workspace in D1 (`lks-core`/`lks-livekit`/`lks-web`/`lks-mcp`/`lks-plugins`/`lks` + `gemini-live`/`agent-sim-proto`). Revisit the split only if a third-party consumer needs a subset (e.g. `lks-core` alone) — that is now already satisfiable by the crate layout.
2. **Gemini reconnect parity (REVISED — new):** Python *never* reconnects mid-call (`transport_dropped=True`, deliberate; reconnects only on GoAway with a saved resume handle), while `gemini-live` auto-reconnects on both GoAway and ConnectionLost with no policy knob distinguishing them. Recommended: fork-patch `reconnect_on_drop: bool` (default false) into the vendored crate per Appendix D §3 (GoAway → always reconnect; ConnectionLost → reconnect only if `reconnect_on_drop`, else `transport_dropped=True`), set it false for exact Python semantics; if the patch proves fragile, accept auto-reconnect as strictly-more-robust (emit `sim.gemini_socket_drop` + set `transport_dropped` when `SessionStatus` transitions to `Reconnecting` without a preceding `GoAway`). Decide at P2 kickoff, record in the phase note.
3. **`gemini_text` → `model_text` delivery rename (Python-side migration, PLAN-20260806 open question 1):** Recommended: Rust implements the *current* schema (`gemini_text`); when Python migrates, port the rename in the same change (one gap one PR). Not a blocker.
4. **`semantic_vad` eagerness for the OpenAI bridge:** Recommended: ship `eagerness: medium` (same as Python default) and expose no config knob in v1 (portable defaults); tuning is a future knob if real runs show talkativeness.
5. **`conversation.item.truncate` on barge (OpenAI):** Recommended: port Python's current behavior first (whatever `callers/openai.py` does today — read and mirror), add truncate only if Python adds it.
6. **reqwest vs ureq for judge/evals HTTP:** Recommended: `reqwest` (rustls) — already in the dependency graph via tokio ecosystem; one HTTP stack for web + judge.
7. **`web/dist` embed compression (rust-embed `compression` feature):** Recommended: enable in release builds if binary size is a concern (>200 MB is the pain line given libwebrtc); measure at P10 (web/dist itself is ~200 KB — REVISED, measured).
8. **Windows release binary in v1?** Recommended: no — research §1.6 VERIFY; `cargo check` on Windows in CI keeps it cheap to add later; `install.ps1` keeps the Python zip until proven.
9. **Drop `lks optimize` if unused at P9?** Recommended: port it (documented user flow: MCP tool + CLI + docs recipe); revisit with the Python usage log at P9 kickoff (AGENTS.md no-dead-features).
10. **Pyo3 abi3 version vs system Python:** Recommended: abi3-compatible embedding with CPython 3.11+; the target repos run any modern Python for their agents — plugins only need the *interpreter*, not site-packages (verify `docs/plugins.md` for what plugin `.py` files import — if they import target packages, note that embedded CPython must be the *system* Python via pyo3's `Python::with_gil` + `sys.path` extension; VERIFY at P8, fallback documented).
11. **`timeline.md`/`review.md` byte-lock?** Recommended: no (research §5.1: human-readable, not byte-locked); regenerate deterministically for golden *tests* (same input → same output) but don't promise cross-implementation byte equality.
12. **`language_code` on Gemini speech_config (REVISED — new):** the vendored `gemini-live` `SpeechConfig` has no `language_code` field; Python sets `speech_config.language_code=voice.language` (default `en-US`). Recommended: fork-patch one serde `camelCase` field (`language_code`) on `SpeechConfig` (Appendix D §1.2 corrects the earlier "on VoiceConfig" placement), mirror `voice.language` fallback chain (voice.language > simulator.language > `en-US`), and pin it with the `gemini_setup_payload_matches_python` test. Do NOT ship without it — the caller's language would silently fall back to server auto-detect.
13. **PyYAML 1.1 vs YAML 1.2 emulation on read (REVISED — new):** PyYAML parses unquoted `on/off/yes/no` as bool and `0123` as int; serde_yaml/yaml_serde are 1.2. Recommended: emulate PyYAML on read (users rely on `enabled: on`) via a pre-parse normalization pass; write path always quotes digits per the `_str_representer` rule (Appendix G §5, P1 step 5). Pin with `yaml_bool_coercion`/`yaml_digit_quoting`.
14. **BoolLike helper for string-bool fields (REVISED — new):** Python `bool("false") == True` silently inverts quoted booleans. Recommended: implement the `BoolLike` helper matching Python exactly (YAML bool → bool; `"false"`/`"no"`/`"0"` → False; other strings → True) rather than documenting a divergence; pin with `bool_string_trap` (E62).
15. **Web server Range requests (REVISED — new):** Python sends `Accept-Ranges: bytes` but ignores Range (always 200 full body). Recommended: keep tower-http's real Range support (D12) — 206 partials are a strict superset that never breaks the bundled UI; document in the porting note; pinned by `test_range_requests`.

---

## Appendices (self-contained implementation contracts)

> These appendices are the implementer's contracts. Appendix A–C reconstructed from the Python ground truth (`config.py`, `metrics.py`, `event_writer.py`, `sqlite_store.py`, source-wide event-kind extraction) since the on-disk B appendix was missing; Appendix D from the vendored-crate + wire verifications; Appendix E–H from the forensics reports. Any divergence from Python is a port bug.

### Appendix A: Behavior constants (config defaults, envelope, run loop)

| Domain | Constant | Value | Source |
|---|---|---|---|
| config defaults | `DEFAULT_LANGUAGE` / `DEFAULT_TIMEZONE` | `"en-US"` / `"UTC"` | config.py:19-20 |
| config defaults | `DEFAULT_VOICE_MODEL` / `DEFAULT_JUDGE_MODEL` | `"gemini-3.1-flash-live-preview"` / `"gemini-2.5-flash"` | config.py:21-22 |
| livekit | `room_prepare_ms` | 500 | config.py:44 |
| livekit | `agent_join_timeout_ms` | 25_000 | config.py:45 |
| livekit | `dispatch_metadata` | `str().strip() or None` | config.py:239-241 |
| livekit | `sip_trunk_id` | legacy alias for `outbound_trunk_id` (sip wins if both) | config.py:385 |
| observe | `transcript_dedupe_window_ms` | 15_000 (code wins over template's 5000) | config.py:135 |
| observe | `silence_threshold_ms` | 4_000 | config.py:136 |
| observe | `turn_taking_warn_ms` | 2_500 | config.py:137 |
| observe | `transcript_payload_types` | `["transcript_turn"]` | config.py:134 |
| observe | `lk_transcription` / `lk_agent_session` / `record_audio` | default True | config.py |
| observe | `audio_onset.enabled` | default False | config.py |
| audio onset | `win_ms` 20 / `threshold` 0.012 / `energy_frames` 3 / `exit_frames` 5 / `refractory_ms` 60 | defaults; `win_samples = (16000*20)//1000 = 320`; `refractory_samples = (16000*60)//1000 = 960`; VAD method string `"rms"` only (ConfigError otherwise); threshold `0.0 < t < 1.0` | observer.py:107-115 |
| telephony | `wait_until_answered` | default True | config.py |
| telephony | `krisp_enabled` | default False | config.py |
| telephony merge | scenario > config > builtin | defaults: prepare_ms 3000, wait_until_answered True, krisp False, handset "mute_and_unsubscribe" (no "mute_and_unanswered" mode exists — REVISED) | scenario.py |
| scenario | CALLER_MODES (5-value) | `webrtc_sim, inbound_sip, outbound_human_pickup, outbound_sim_callee, agent_dials` | scenario.py |
| scenario | `handset_isolation` | {mute_uplink, mute_and_unsubscribe, none, remove}; runtime invalid → silently "mute_and_unsubscribe" | scenario.py |
| scenario | `hold_music_timeout_s` | Execute wins > persona.speech_conditions.hold_music_timeout_s > None; clamp [5.0, 300.0]; `:g` error formatting | scenario.py |
| scenario | `max_turns` 6 / `timeout_s` 120 / `first_speaker` "agent" | simulator defaults | scenario.py |
| scenario | `effective_locale()` | Persona.language || Persona.locale (stripped) || metadata.locale || "en-US" | scenario.py |
| envelope | `event_id` | `"evt_" + uuid4().hex[:12]` | event_writer.py:109 |
| envelope | `seq` | 1-based, incremented BEFORE emit; summary `event_count = seq + 1` | event_writer.py:103 |
| envelope | `ts` | `int(now.timestamp()*1000)` (wall clock) | event_writer.py:114 |
| envelope | `ts_mono_ms` | `int((monotonic − t0_mono)*1000)`; backdate override `max(0, int(ts_mono_ms))` | event_writer.py:105-107 |
| envelope | `datetime_utc` | `isoformat(timespec="milliseconds").replace("+00:00", "Z")` | event_writer.py:116 |
| envelope | `datetime_local` | same with `ZoneInfo(timezone_name)` (default UTC) | event_writer.py:117 |
| envelope | `dialogue` | injected before `spec` when `include_dialogue` (default True); `{user:{text,final,at_ms}|note "user has not spoken yet this turn", agent: same}`; `begin_turn` clears ONLY agent dialogue | event_writer.py:55-88 |
| envelope | `spec` | `{}` when None | event_writer.py:123 |
| run loop | `end_reason` set | `gemini_socket_drop, sim_end_call, agent_disconnected, max_turns, timeout, hold_music_timeout, dead_call_silence` (script hang_up arrives via `sim_hang_up()` → `end_call` → `sim_end_call`) | run_orchestrator.py:760-833 |
| run loop | `silence.detected` spec | `{duration_ms: int(silent_for*1000), scripted_user_silence}` source="observer" at >= 1 × threshold; dead-call at >= 3 × threshold with any_activity_occurred | run_orchestrator.py:808-833 |
| run loop | `run.ended` spec | `{status, summary_digest: {turn_count, tool_errors, duration_ms}}`, include_dialogue=False | event_writer.py:173-177 |
| run loop | `run.end_condition` spec | `{reason}` include_dialogue=False | run_orchestrator.py:448 |
| run_id | format | `{prefix}-{slug}-{YYYYMMDD}-{HHMMSS}-{4hex}`; prefix `{NNN}` seq from existing report dirs; stamp `YYYYMMDD-HHMMSS-{secrets.token_hex(2)}` | run_orchestrator.py:55-122 |
| summary | top-level key order | `run_id, status, duration_ms, turn_count, event_count, turn_taking_ms, metrics, tool_calls, tool_errors, interruptions, silences, verdict, turns` + `end_reason` (when set) + summary_extra merge (`caller_mode, end_reason, dial_ms, script_verify, assert_verify, caller`) | event_writer.py:150-171 + run_orchestrator |
| summary | `turn_count` | `max((e.turn for e in events), default=0)` | event_writer.py:154 |
| turn_metrics | rows | turn > 0 only: `{turn, user_text, agent_text, turn_taking_ms (only overwrite when spec.turn_taking_ms is not None), tool_count, tool_errors, interrupted}` | event_writer.py:197-230 |
| sqlite | DDL | byte-identical SCHEMA: `runs` (run_id PK, scenario_id, room_name, agent_name, status 'running', started_utc, ended_utc, duration_ms, turn_count, tool_errors, verdict, report_dir, summary_json), `run_events` PK (run_id, seq) + `idx_run_events_kind`, `run_turns` PK (run_id, turn); `payload_json` = FULL envelope JSON | sqlite_store.py:11-53 |
| web timing | cue tail / end-hint cap / start floor / start hard cap / min width | 350 / final+800 / final-400 / final-200 / 200 | web/report_time.py + cues.py |
| web timing | `_clamp_end` min span | start+120 | web/cues.py |
| web timing | `mono_to_audio_ms` drop window | > duration+2000 → None | web/report_time.py |
| web dedupe | max_delta user-similar-cross-source / agent-similar-cross-source / similar / not-similar | 15000 / 6000 / 4000 / 2500 | web/transcript_cues.py |
| web dedupe | tie sort | (final_ms, source_rank, agent-before-user); BREAK (not continue) on delta > max_delta | web/transcript_cues.py |
| web ghost | provider sources / window / never dropped | {sim.gemini, sim.openai} / ±2500 ms / sim.script + non-user | web/transcript_cues.py |
| web windows | active-speaker gap / window tail / merge | 2800 / +600 / +1500 | web/windows.py |
| web windows | interim window / prefix truncation | [final−est−3000, final−500] / 12 chars | web/windows.py |
| web windows | utterance estimate | agent 95 ms/word, user 85 ms/word; empty 800/600; clamps (700,22000)/(500,14000) | web/windows.py |
| web markers | barge span (during/not) | 2200 / 1400; `max(span, inj_dur+400)` | web/markers.py |
| web markers | non-barge cue span | `max(400, min(waited,2000) or 400, inj_dur+200)` | web/markers.py |
| web markers | inject duration fallback (non-room_pcm / room_pcm) | 2200 / 800; end = start + max(200, dur) | web/markers.py |
| web markers | recovery / interruption / audio onset / user audio source | +800 / +500 / +300 / +300 | web/markers.py |
| web markers | silence_wait span fallback / end | 1500 / start+200 | web/markers.py |
| web markers | silence span fallback | 4000 | web/markers.py |
| web markers | inject-near window / same bonus | ±900 ms / −200 score | web/markers.py |
| web tools | tail / band min / band cap | 500 / 400 / 400; end = base + `max(400, min(dur, 400))` or base + 500 | web/tool_events.py |
| web speech_origin | marker proximity | ±8000 or ±1200 or interval overlap | web/speech_origin.py |
| web speech_origin | script-match delta / barge / tiny / cue / late-STT | (−800, 15000) / (−500, 15000) / (0, 3500) / (0, 8000) / ≥ −500 | web/speech_origin.py |
| web speech_origin | scores | `100 − min(40, delta/400)`; `70 − min(30, delta/200)`; 50 | web/speech_origin.py |
| web speech_origin | pin window | start inject−80; end max(final+500, inject+audio+350); audio fallback 2200 (barge) / 900 | web/speech_origin.py |
| web speech_origin | synthetic cue | dedupe <600 ms; audio fallback max(1200, end−inject); end max(inject+audio+350, inject+1200); start inject−80; `synthetic: true, source: "sim.script", marker_tags: ["barge_in"]` | web/speech_origin.py |
| web sorts | markers final / cues final / tool spans | (start_ms, type-string alphabetical) / (start_ms, agent-before-user) / (start_ms, name) | web/markers.py, cues.py, tool_events.py |

Python-specific semantics to replicate deliberately: `int(x * 0.45)` and `len(x) / 4` are floor ops; `max(1, int(min(ta,tb) * 0.5 + 0.5))` is round-half-up; `int(frames * 1000 / rate)` = float division then truncation; sort keys are tuple-key comparisons, not piecewise (Python/Rust stable sorts match); `bool("false")` is true; `int(3.5) == 3`; `_percentile` uses nearest-rank `round((pct/100.0) * (len(values)-1))` with Python's banker's rounding (`round`), not Rust's `round_half_away`.

### Appendix B: Metric formulas (metrics.py — byte-exact)

**Percentile helper** (`_percentile(values, pct)`): sorted ascending; `idx = min(len-1, max(0, round((pct/100.0) * (len-1))))` — **Python `round()` (banker's rounding)**, then `float(values[idx])`. Empty → None. `_pct_block(samples)` = `{"count", "p50", "p95", "p99", "max", "min", "mean"}` (mean = `sum/len` if samples else None; max/min None if empty).

**Event extraction** (in event order; `_mono(e) = int(e.get("ts_mono_ms") or 0)` with TypeError/ValueError → 0):
- `transcript.agent.final` → agent_final_ms list; `agent_chars += len(text.strip())`; `turn_taking_ms` spec value appended to turn_taking when not None (TypeError/ValueError → skip); first occurrence sets `ttfw_ms = mono`, `ttfw_source = "transcript.agent.final"`.
- `transcript.agent.preamble` → if ttfw still None: set ttfw_ms + `ttfw_source = "transcript.agent.preamble"`; `agent_chars += len(text.strip())`.
- `transcript.user.final` → user_final_ms; `user_chars += len(text.strip())`.
- `tool.start` → tool_starts += 1; `tool.error` → tool_errors += 1; `interruption` → interruptions += 1; `silence.detected` → silence_events += 1; `sim.caller.audio_source_start` → user_audio_source_ms; `sim.agent.audio_onset` → agent_audio_onset_ms.
- **Barge events** (`_is_barge_event`): `sim.script.cue` with `spec.barge_in` → `counts_for_recovery_barge(barge_in=True, interrupt_class=class or interrupt_class)`; `interruption` with `spec.barge_in` or `by == "sim"` → False if class in (noise, backchannel, dtmf, silence) or `false_positive`, else `counts_for_recovery_barge(barge_in=True, class or "correction")`. `counts_for_recovery_barge(barge_in, icls)`: False unless barge_in; else `(icls or "correction") in {"correction", "escalate"}`.

**Derived**:
- `barge_unique = sorted(set(barge_ms))` (dedup). Per barge: first agent_final with `a > b` → `barges_recovered += 1`, `recovery_samples.append(float(a - b))`. `barge_count = len(barge_unique)`; `barge_recovery_rate = barges_recovered / barge_count` if count > 0 else None.
- `turn_taking_audio_ms = _pair_audio_onsets(user_audio_source_ms, agent_audio_onset_ms)`: temporal pairing — each user source U pairs with the FIRST unused agent onset A with `A.ts > U.ts`; A consumed (used[] latch); U with no following unused onset contributes nothing. `ttfa_run_ms = agent_audio_onset_ms[0]` if any else None.
- `talk_ratio = agent_chars / (agent_chars + user_chars)` if total > 0 else None.
- `slow_turns_2500 = count(turn_taking > 2500)`; `slow_turns_5000 = count(turn_taking > 5000)`.
- `user_words`: whitespace token counts of **consecutive-deduped** non-empty `transcript.user.final` texts (prev_text == text → skip). `user_words_mean` = mean if any. `_split_user_word_counts` splits vs Script catalog: `_script_say_catalog` = `(mono, say)` from `sim.script.cue`/`sim.script.hang_up`/`sim.script_inject` with `say = spec.say or spec.text` stripped, non-empty, NOT starting `[`; `_is_script_classified_final(text, final_ms, catalog)`: `delta = final_ms - inject_ms` in `(-800, 28_000]` (inclusive both bounds — `delta < -800 or delta > _SCRIPT_MATCH_MAX_LAG_MS` skip; `_SCRIPT_MATCH_MAX_LAG_MS = 28_000`) and `_mostly_script_say(text, say)` (P3.5 step 7 speech-origin semantics — same function as web/speech_origin.rs).
- `natural_words` / `script_words` from that split; means per list.

**Return key order (EXACT — 36 keys)**: `schema` (`"agent-sim/metrics/v1"`), `turn_taking_ms` (pct_block), `ttfw_ms`, `ttfw_source`, `ttfa_run_ms`, `ttfa_source` (`"agent.audio_onset"`), `turn_taking_audio_ms` (pct_block), `user_audio_source_count`, `agent_audio_onset_count`, `recovery_ms` (pct_block), `barge_count`, `barges_recovered`, `barge_recovery_rate`, `interruption_count`, `silence_events`, `agent_finals`, `user_finals`, `tool_calls`, `tool_errors`, `tool_error_rate` (`tool_errors/tool_starts` if starts else None), `talk_ratio`, `agent_chars`, `user_chars`, `user_words_count`, `user_words_p10`, `user_words_p50`, `user_words_mean`, `user_words_natural_count`, `user_words_natural_p10`, `user_words_natural_p50`, `user_words_natural_mean`, `user_words_script_count`, `user_words_script_p50`, `user_words_script_mean`, `slow_turns_over_2500ms`, `slow_turns_over_5000ms`. (REVISED: the plan's earlier 31-key list omitted `ttfa_run_ms`, `ttfa_source`, `turn_taking_audio_ms`, `user_audio_source_count`, `agent_audio_onset_count`.)

**`metrics_digest(metrics)`** (12 keys, flat, for suite matrix/compare_runs): `ttfw_ms, turn_p50_ms, turn_p95_ms, recovery_p50_ms, barge_count, barge_recovery_rate, talk_ratio, user_words_p50, user_words_natural_p50, ttfa_ms, turn_taking_audio_p50_ms, turn_taking_audio_p95_ms` — null-safe: non-dict metrics → all None; nested blocks guarded (`isinstance(x, dict)` else `{}`).

### Appendix C: Event kinds + spec payloads

**Complete kind set (union for `EventKind`; `serde` must serialize to the exact string).** Sources: `script.verify` / `assert.verify` / `judge.verdict` emitted at finalize; observer/leg/bridge catalogs below.

| Kind | Spec payload | Source | include_dialogue |
|---|---|---|---|
| `run.started` | `{scenario_id, caller_mode, config_snapshot}` | "mcp"/orchestrator | False |
| `run.end_condition` | `{reason}` | orchestrator | False |
| `run.ended` | `{status, summary_digest: {turn_count, tool_errors, duration_ms}}` | writer.finalize | False |
| `run.error` | `{where, error}` | orchestrator | False |
| `dispatch.created` | `{room, agent_name, dispatch_id, metadata_set, mode}` | SimLeg | False |
| `dispatch.agent_joined` | `{identity, mode}` | SimLeg | False |
| `dispatch.agent_timeout` | `{error, mode}` | orchestrator (AgentJoinTimeout) | False |
| `sim.connected` | `{identity, room, mode}` | SimLeg | False |
| `sim.observer_joined` | `{identity, room, mode}` | SimLeg | False |
| `sim.leg_error` | `{error, mode}` | SimLeg factory catch | False |
| `sim.hold_timeout` | `{timeout_s, agent_idle_ms, note: "Caller gave up waiting on agent dead air (hold_music_timeout_s)"}` | orchestrator | False |
| `sim.mic_published` | `{sample_rate: 24000, mixer: "parallel"}` (+ `provider: "openai"` on OpenAI) | bridge | True |
| `sim.agent_audio_bridged` | `{track_sid}` (+ `provider: "openai"`) | bridge | True |
| `sim.agent_listen_room` | `{agent_identity, listen: "agent_room", note}` (OpenAI note: "OpenAI ears on agent-room WebRTC (sim-room SIP track missing)") | bridge | True |
| `sim.gemini_connected` | `{model, voice, language, voice_gain, silent_mode, resume}` | gemini | True |
| `sim.openai_connected` | `{model, voice, language, voice_gain, silent_mode}` | openai | True |
| `sim.gemini_activity` | `{edge: "activity_start"\|"activity_end"\|"activity_end_skipped", reason, error?}` | gemini | True |
| `sim.gemini_socket_drop` | `{attempt\|phase, max_attempts?, error, retryable}` | gemini | True |
| `sim.openai_socket_drop` | `{attempt\|phase, max_attempts?, error, retryable}` | openai | True |
| `sim.gemini_resumption_handle` | `{resumable: True, handle_set: True}` | gemini | True |
| `sim.gemini_go_away` | `{time_left}` | gemini | True |
| `sim.gemini_reconnecting` | `{generation}` | gemini | True |
| `sim.caller_midcall` | `{kind, label (may be None), text: text[:240]}` | bridge | False |
| `sim.agent_greeted_nudge` | `{debounce_s: 1.0, audible: False}` | nudge | True |
| `sim.agent_greeted_nudge_skipped` | `{reason: "silent_mode"}` | nudge | True |
| `sim.interrupt_rate` | `{rate, interval_ms, class, say, asset, delivery, min_agent_active_ms}` | interrupt_rate | False |
| `sim.interrupt_rate_skip` | `{reason: "agent_not_active", active_ms}` \| `{reason: "active_ms_below_min", active_ms, min}` | interrupt_rate | True |
| `sim.silent_mode` | `{enabled: True, note: "Caller stays mute: no freestyle, no nudge, no auto barge/noise"}` | orchestrator/bridge | True |
| `sim.silent_mode_skip_inject` | `{label, delivery, text: (text or "")[:120]}` | bridge | True |
| `sim.caller.audio_source_start` | `{provider, voice_gain, gain, via}` | bridge ("sim.gemini"/"sim.openai") | True |
| `sim.caller_role_flip_suppressed` | `{heard, note: "freestyle matched assistant-persona cues"}` | gemini | True |
| `sim.script_deferred_end_call` | `{text: clean, reason: "script_steps_pending"}` | "sim.gemini"/"sim.openai" | True |
| `sim.end_call_token` | `{text: clean, reason: "end_call_token"\|"farewell"}` | "sim.gemini"/"sim.openai" | True |
| `sim.hang_up` | script variant `{step_id, label, say, error?}` source="sim.script"; bridge variant `{source: "script", by: "sim"}` source="sim" | script/bridge | False |
| `sim.heard_agent` | `{text}` | "sim.gemini"/"sim.openai" | True |
| `sim.script_inject` | room_pcm: `{text, label, delivery, asset: str(wav_path), mix: "speech"\|"parallel"\|"parallel_loop", duration_ms: int(duration_s*1000), gain, voice_gain, loop}`; gemini_text: `{text, label, delivery, gain, voice_gain, effective_gain: clamp(gain*voice_gain), attempt: 1}`; sapi adds `duration_ms` + `delivery: "sapi"` | "script" | True |
| `sim.script.cue` | `{step_id, label, say, trigger, action, barge_in, class, overlay, delivery?, asset?, digits?, gain?, loop?, waited_ms, hold_silence_ms?, agent_active, agent_active_ms, during_agent_speech, error?}` | "sim.script" | False |
| `sim.script.wait` | `{step_id, label, say, trigger, action, barge_in, class, overlay, waited_ms, hold_silence_ms, agent_active, agent_active_ms, during_agent_speech}` | "sim.script" | False |
| `sim.script.hang_up` | `{step_id, label, say: resolved say_text, trigger, action, barge_in, class, overlay, waited_ms, agent_active, agent_active_ms, during_agent_speech, error?}` — emitted TWICE (runner + after drain) | "sim.script" | False |
| `sim.script.dtmf` | `{step_id, label, say, trigger, action, barge_in, class, overlay, digits, waited_ms, agent_active, agent_active_ms, during_agent_speech, error?}` | "sim.script" | False |
| `sim.script.hang_up_deferred` | `{step_id, reason, prior_reason, deferred_ms, budget_ms, last_agent_final: (text or "")[:240]}` (reason "defer_budget_exhausted" or first-defer reason) | "sim.script" | False |
| `sim.script.error` | `{step_id, label, delivery, asset, error: f"{TypeName}: {msg}"}` | "sim.script" | False |
| `sim.agent_audio_recorded` | `{track_sid, source: "observer.agent_room", sample_rate: 16_000}` | observer | True |
| `sim.agent.audio_onset` | `{channel: "agent", sample_rate: 16_000, onset_frame_idx, frames_before_first_onset, vad: {method, threshold, win_ms, energy_frames, refractory_ms}}` — ts_mono_ms backdated `max(0, audio_t0_mono + onset_ms)`; turn=None | "sim" | False |
| `sim.error` | `{where, error}` (+ `track_sid` on observer.agent_record) | "sim" | True |
| `observer.error` | `{where, error}` | observer | True |
| `observer.warning` | `{where, message, call_id, output_call_id, index}` (exact message "call/output call_id mismatch; paired by array index") | observer | True |
| `room.participant_connected` | `{identity, name, kind}` | observer | True |
| `room.participant_disconnected` | `{identity}` | observer | True |
| `room.track_subscribed` | `{identity, kind, sid}` | observer | True |
| `room.active_speakers` | `{identities}` | observer | True |
| `room.disconnected` | `{}` | observer | True |
| `data.message` | `{topic, sender, payload}` | observer | True |
| `data.raw` | `{topic, bytes: len(packet.data), sender}` | observer | False |
| `transcript.{user\|agent}.interim` | `{text, final, segment_id?}` | observer | True |
| `transcript.user.final` | `{text, final, segment_id?[, same_turn]}` | observer | True |
| `transcript.agent.final` | `{text, final, segment_id?[, turn_taking_ms]}` | observer | True |
| `transcript.agent.preamble` | `{text, final, segment_id?, note: "agent spoke before user; not counted as a turn"}` (EXACT note) | observer | True |
| `tool.start` | `{name, call_id, payload}` | observer (data-topic + L3) | True |
| `tool.end` | `{name, call_id, payload[, duration_ms]}` | observer | True |
| `tool.error` | `{name, call_id, payload[, duration_ms, error]}` | observer | True |
| `session.agent_state` / `session.user_state` | `{old_state, new_state}` (enum value→name, numeric fallback) | agent_session_observer | False |
| `session.usage` | MessageToDict; dedupe identical consecutive usage dicts | agent_session_observer | False |
| `session.error` | `{message}` | agent_session_observer | False |
| `session.overlapping_speech` | MessageToDict | agent_session_observer | False |
| `session.debug` | MessageToDict — ONLY if any value not in ("", None, [], {}); non-message → `{message: str(...)}` | agent_session_observer | False |
| `session.tool_execution` | MessageToDict (fallback for tool_execution_updated) | agent_session_observer | False |
| `session.chat_history` | `{items}` — item oneof: function_call → `{type, id, call_id, name, arguments}`; function_call_output → `{type, id, call_id, name, output, is_error}`; message → `{type, ...MessageToDict}`; agent_handoff/agent_config_update → `{type, ...MessageToDict}`; else `{type: "unknown"}` | agent_session_observer | False |
| `session.handoff` | `{id, old_agent_id, new_agent_id, created_at}` (Timestamp → ISO via ToJsonString; no-op when not old_id or old_id == new_id) | agent_session_observer | False |
| `handoff` | (agent_handoff conversation item) | agent_session_observer | False |
| `interruption` | bridge: `{by: "agent", note: "Gemini output interrupted by agent audio"}` / `{by: "agent", note: "OpenAI output interrupted by agent audio (input_audio_buffer.speech_started)"}`; script barge: `{by: "sim", barge_in: True, class, false_positive, step_id, label, say, note: "Script barge-in while agent was active speaker"}`; interrupt-rate: `{by: "sim", barge_in: True, class, false_positive, step_id, label, say, note: "Interrupt-rate barge while agent was active speaker"}` | "sim"/"sim.script" | True |
| `silence.detected` | `{duration_ms, scripted_user_silence}` | "observer" | True |
| `script.verify` | full script_verify dict (Appendix E §5: `script_steps, cues_fired, waits_fired, hang_ups_fired, counts, interruptions, checks, plugin_results, pass`) | orchestrator | False |
| `assert.verify` | full assert result dict (`{pass, checks, detail}`) | orchestrator | False |
| `assert.goals_met` | `{pass: True, skipped: True}` on skip paths | orchestrator | False |
| `judge.verdict` | FULL aggregated verdict dict (Appendix G §4: `{verdict, score, confidence, criteria[], notes, ...}` or multi-judge aggregate) | orchestrator | False |
| `inbound.*` / `outbound.*` (P6) | `inbound.answered` / `inbound.dial_started` / `inbound.dial_failed` / `inbound.agent_room_discover` / `inbound.agent_room_resolved` / `inbound.agent_room_deterministic` / `outbound.prepare` / `outbound.dial_started` / `outbound.dial_answered` / `outbound.dial_failed` / `outbound.wait_agent_dial` / `outbound.handset_isolated` — spec per R4 sim_leg/* | SimLeg | False |

**Event-source names are report consumers' contract** (metrics.py counts `sim.script.cue`, `sim.script.hang_up`, `sim.script_inject`; web/markers.py groups by `sim.script_inject`; verify matches `spec.step_id` on cue kinds). Port names byte-for-byte.

### Appendix D: Gemini / OpenAI wire formats

**0. Rate and audio-shape cheat sheet**

| Property | Gemini bridge | OpenAI bridge |
|---|---|---|
| Agent audio in (room → model) | `rtc.AudioStream(track, sample_rate=16_000, num_channels=1)` — SDK resamples 48k→16k | `rtc.AudioStream(track, sample_rate=24_000, num_channels=1)` — SDK resamples 48k→24k |
| Sim audio out (model → room) | `rtc.AudioSource(24_000, 1)` mono PCM16 LE @ 24 kHz | `rtc.AudioSource(24_000, 1)` mono PCM16 LE @ 24 kHz |
| Model input PCM | 16 kHz PCM16 LE mono, blob mime `audio/pcm;rate=16000` | 24 kHz PCM16 LE mono, base64 in `input_audio_buffer.append` |
| Model output PCM | 24 kHz PCM16 LE mono, `model_turn.parts[*].inline_data` | 24 kHz PCM16 LE mono, `response.output_audio.delta` |
| Base64 variant | urlsafe (SDK `_common.py:693`); standard also accepted | standard |
| Turn detection | OFF — manual `activity_start`/`activity_end` markers | OFF — `turn_detection: null`, manual `input_audio_buffer.commit` + `response.create` |
| Track name / source | `"lks-mic"`, `TrackSource.SOURCE_MICROPHONE` (same both) | same |

Both bridges publish the mic **once** (`publish_mic()` before the session loop — must never re-run on a Gemini reconnect, would double-publish), create ONE `ParallelMicMixer` over ONE `AudioSource`, and call `recorder.mark_start()` right after the track publish (pins recorder t=0). Python does NOT resample Gemini output — the sim mic `AudioSource` is created at 24 kHz and WebRTC handles playback-side resampling to the room; the only resamples are INPUT-side (48k→16k Gemini, 48k→24k OpenAI). The Rust port must mirror this: mixer + AudioSource at 24 kHz, model output injected without conversion.

**1. Gemini caller wire format**

1.1 **Connect URL**: `wss://generativelanguage.googleapis.com/ws/google.ai.generativelanguage.v1beta.GenerativeService.BidiGenerateContent?key=<api_key>` — key as QUERY PARAM, not header. WS args: `ping_interval=30`, `ping_timeout=60` (without them the library's default 20 s ping kills calls ~4 min in with 1011 "keepalive ping timeout").

1.2 **Setup message** (first frame `{"setup": {...}}`; Python SDK serializes speechConfig inner keys snake_case; server accepts both):
```json
{"setup": {"model": "<model>", "generationConfig": {"responseModalities": ["AUDIO"],
  "speechConfig": {"voice_config": {"prebuilt_voice_config": {"voice_name": "<voice>"}},
                   "language_code": "<language>"}},
  "systemInstruction": {"parts": [{"text": "<persona_system_prompt>"}], "role": "user"},
  "tools": [], "realtimeInputConfig": {"automatic_activity_detection": {"disabled": true}}}}
```
Non-negotiables: `responseModalities: ["AUDIO"]` ONLY (TEXT closes socket with 1011 — hard-code); `automatic_activity_detection.disabled: true` (manual activity markers; auto VAD stays OFF); `speechConfig.language_code` = the vendored-crate patch point (see D4 — `#[serde(skip_serializing_if = "Option::is_none")] pub language_code: Option<String>` on `SpeechConfig`; crate serializes camelCase `voiceConfig/prebuiltVoiceConfig/voiceName/languageCode` which the server accepts; byte parity not required, field presence is); `inputAudioTranscription {}`/`outputAudioTranscription {}` default-enabled — send empty objects if the crate requires opt-in; `sessionResumption.handle`: None on first connect, saved handle on reconnect (auto-injected by the crate on reconnect); no tools.

1.3 **Realtime input (agent audio → Gemini) + activity markers**: pump `AudioStream(track, 16_000, 1)`. Activity gate: speaking = `observer.agent_is_active_speaker` OR `rms >= 100.0` (`_AGENT_SPEECH_RMS_THRESHOLD = 100.0`). Constants: `_AGENT_SPEECH_START_FRAMES = 1`, `_AGENT_TRAILING_PAD_MS = 120.0`, `_AGENT_STREAM_END_SILENCE_MS = 650`. Wire: audio blob `{"realtime_input": {"audio": {"data": "<base64>", "mimeType": "audio/pcm;rate=16000"}}}` (urlsafe base64); `activity_start` `{"realtime_input": {"activityStart": {}}}` — SEPARATE call, not wrapped with audio; `activity_end` `{"realtime_input": {"activityEnd": {}}}`; `audioStreamEnd` `{"realtime_input": {"audioStreamEnd": true}}` — auto-VAD-mode only; with manual VAD use `activity_end`, NEVER `audioStreamEnd`.
Pump state machine (replicate exactly): (1) stream closed, first speaking frame (>= 1 frame) → send `activity_start`, `_agent_stream_open=True`, emit `sim.gemini_activity {edge:"activity_start", reason:"agent_speech"}`. (2) While open + speaking → audio blobs per frame. (3) While open + silent → forward pad frames while `_agent_silence_ms <= 120.0`; at >= 650 ms flush: `activity_end` + `sim.gemini_activity {edge:"activity_end", reason}`. Also flush on session teardown ("session_teardown"), agent track end ("agent_track_ended"), before inject text ("inject_before_text" — sending activity_start while the stream is open collides → server closes 1007), freestyle nudge ("freestyle_nudge"). (4) Inject in flight (`_inject_turn_active`): agent frames DROPPED without flushing activity_end (closing mid-inject collides with the inject's own start/end → 1007); if paused and NOT inject-active, flush once ("agent_audio_paused"). (5) Redundant/early `activity_end` failure → server closes 1007 "invalid frame payload data"; bridge catches it, emits `sim.gemini_activity {edge:"activity_end_skipped", reason, error}`, resets state — NOT fatal.

1.4 **Output (Gemini → room)**: receive loop bounded by `asyncio.wait_for(..., timeout=15.0)`; TimeoutError → continue unless end_call (lets `bridge.stop()` tear down even when the old session's socket is dead after a mid-call reconnect).
- `session_resumption_update`: if `resumable and new_handle`: save handle, emit `sim.gemini_resumption_handle {resumable: True, handle_set: True}`.
- `go_away`: emit `sim.gemini_go_away {time_left}`, set reconnect-required, return. "Graceful — do NOT set `end_call` or `transport_dropped`."
- `interrupted`: emit `interruption` `{by: "agent", note: "Gemini output interrupted by agent audio"}` source="sim" — the barge signal; NO `mixer.clear_speech()` on this path (Gemini handles its own barge state; mixer only gets `end_speech_turn` on turn_complete).
- `output_transcription.text` (the caller's own speech as the sim's voice, source `sim.gemini`): if `_allow_persona_room_audio()`: append to `_sim_out_text` (and `_inject_heard_text` when an inject turn). Role-flip suppression (only when not inject and not script hang-up farewell): if `looks_like_assistant_persona(self._sim_out_text)` → `_mixer.clear_speech()`, `suppress_persona_output(4000)`, emit `sim.caller_role_flip_suppressed {heard, note:"freestyle matched assistant-persona cues"}`, reset `_sim_out_text=""`, continue. Assistant-persona cue tuple (exact, lowercase match): `("thanks for calling", "thank you for calling", "how can i help", "how may i help", "let me check that for you", "let me check on that for you", "i'd be happy to help", "i would be happy to help", "we're here to help", "we are here to help", "i'll check that for you", "i will check that for you")`. Early-bye mute: if (early_bye or contains_end_call_signal) and not scripted_farewell → `_mute_hang_up_audio()`; if pending_script and early_bye → `suppress_persona_output(4000)`. Log: `observer.on_transcript("user", log_text, final=False, source="sim.gemini")` (strip_farewell_signal if pending else strip_end_call_signal).
- `input_transcription.text` (agent as heard): emit `sim.heard_agent {text}` source="sim.gemini" (low-priority mirror; lk.transcription is primary). NOT fed to on_transcript.
- `model_turn.parts[*].inline_data`: if non-empty and `_allow_persona_room_audio()` → playout. Output PCM16 mono @ 24 kHz.
- `turn_complete`: `_mixer.end_speech_turn()`; if inject turn → `_inject_playout_done.set()`; if not inject → `_inject_playback_gain = 1.0`; if not inject and not farewell and `_persona_output_suppressed()` → `_sim_out_text=""`, `_mute_persona_audio=False`, continue. Final commit: `ended = contains_end_call_signal(text)`, `farewell = contains_farewell_signal(text)`, `pending = script steps pending`, `clean = strip_farewell_signal if pending else strip_end_call_signal`; `observer.on_transcript("user", clean, final=True, source="sim.gemini")`; `_sim_out_text=""`; reset audio-source latch. Then: if pending and (ended or farewell) and not script_farewell: `_mute_persona_audio=True`, `suppress_persona_output(5000)`, emit `sim.script_deferred_end_call {text: clean, reason: "script_steps_pending"}` source="sim.gemini", `_mute_persona_audio=False`, continue (NO teardown). Else if `should_end_call_on_turn(pending_script=pending, ended=ended, farewell=farewell, scripted_farewell=self._script_hangup_farewell)`: `_mute_persona_audio=True`, `await self._drain_persona_speech(timeout_s=3.0)`, emit `sim.end_call_token {text: clean, reason: "end_call_token" if ended else "farewell"}` source="sim.gemini", `self.end_call.set()`, return. Else `_mute_persona_audio=False`.
- `_play_pcm` (24 kHz output path): skip if `_mute_persona_audio and not _inject_turn_active and not _script_hangup_farewell`. Gain = `_inject_playback_gain` if inject active else `_voice_gain`. Mixer path: `_emit_user_audio_source_start(gain=gain, via="freestyle_tts")` (latched — fires at the FIRST push_speech of an utterance only), `push_speech(pcm, gain)`. Fallback (no mixer): `recorder.push_sim(pcm, 24000)` + `capture_frame`. Gemini `_play_pcm` AWAITED INLINE (model_turn playout serialized per part); OpenAI's dispatched via `ensure_future` (no ordering guarantee).

1.5 **Transport drop classification + reconnect**: transport iff `isinstance(e, ConnectionError)` OR `"1006" in str(e)` OR `"abnormal closure" in str(e).lower()` OR type name contains `"ConnectionClosed"`. Gemini ADDS `"1008"` (preview-model transient) and `"1011"` (server internal — transient, resumable with handle). OpenAI ADDS `"ConnectionLost"`/`"TimedOut"` in type name.
Gemini mid-call drop: `transport_dropped = (self._resume_handle is None or self._reconnect_required.is_set())`; emit `sim.gemini_socket_drop {phase:"mid_call", error, retryable: handle is not None and not reconnect_queued}`; ALWAYS also `sim.error {where:"gemini->lk", error}`. If transport AND handle available AND no reconnect queued: `transport_dropped=False`, set reconnect-required, return. Otherwise `end_call.set()`. Reconnect loop: on go_away or resumable drop → teardown session cm, clear reconnect, `end_call.clear()`, generation += 1, reconnect_count += 1. If `reconnect_count > 2`: "Bounded mid-call reconnect..." → `end_call.set()`, break. Else emit `sim.gemini_reconnecting {generation}` and reconnect with the saved handle. Mixer torn down ONCE after the whole session incl. reconnects; `publish_mic()` called ONCE before the loop. Handshake retry (`_connect_live_with_retry`): `max_attempts=3`, retryable by the same classification; backoff `min(2.0*attempt, 6.0)` → 2 s, 4 s (3rd attempt final, no sleep); emit `sim.gemini_socket_drop {attempt, max_attempts, error, retryable}` — pre-dialogue only ("once dialogue has begun we do not reconnect").
Crate divergence to manage: `gemini-live`'s `ReconnectPolicy` (session.rs:71-87: `{enabled: true, base_backoff 500ms, max_backoff 5s, max_attempts Some(10)}`) does NOT distinguish GoAway vs ConnectionLost — internal `DisconnectReason` (session.rs:408-414) collapses both (session.rs:436-462); Close frame maps to ConnectionLost (session.rs:500-506). Patch per D4: `reconnect_on_drop: bool` (default false) + close-code surfacing (transport.rs:311-324).

1.6 **Close-code handling table (Gemini)**: 1006 → transport, retryable IF resume handle (else `transport_dropped=True`, end_call); 1008 → transport, same retryable logic (include "1008" in classifier); 1011 → transport, retryable with handle (also the "keepalive ping timeout" symptom; also text-only native-audio setups); 1007 → `sim.gemini_activity {edge:"activity_end_skipped", ...}` — harmless, NOT fatal, also guarded preventively; 1000 + go_away → reconnect via handle, no end_call/transport_dropped.

1.7 **`script_speak_directive`** (port verbatim; non-farewell variant — do NOT add "stay silent" instructions):
```
PRIVATE SIMULATOR CUE — do not read these instructions aloud. You are UNMISTAKABLY the HUMAN CALLER on this phone call. Any other voice you just heard is the assistant (the other party) — never speak as them, never greet callers, never offer to help or check availability for someone else. Ignore silence rules for this one turn only. Speak aloud now, exactly once, ONLY the caller line between <<< and >>>. Verbatim: no paraphrase, no extra words before or after, no added fillers.
<<<
{line}
>>>
After that exact line, stop this cue turn.
```
Farewell variant (`hangup_farewell=True`):
```
SIMULATOR CUE — ignore silence rules for this one turn only. You are the HUMAN CALLER (not the assistant). Speak the following goodbye aloud now as the phone caller, exactly once, then stop and wait silently for disconnect:
{line}
```
The OpenAI text-inject path ALSO uses this prose verbatim (role-lock) for its say line.

1.8 **Gemini inject (gemini_text delivery)**: `_inject_gemini_text`: `_inject_playback_gain = clamp(gain * voice_gain)`; `_inject_turn_active=True`; `_inject_heard_text=""`; `_agent_audio_paused=True`; fresh `_inject_playout_done`. If `_agent_stream_open`: flush (reason="inject_before_text"). `await asyncio.sleep(0.15)`; then `send_realtime_input(activity_start)`, `send_realtime_input(text=speak_directive)`, `send_realtime_input(activity_end)` ("Manual VAD: wrap the cue text in activity_start/end so Live generates TTS"). Emit `sim.script_inject {..., attempt: 1}` source="script". Wait loop deadline **2.8 s** for `speech_queued_ms() > 0` or STT mismatch (`len(heard.split()) >= 5 and not _inject_matches_say(heard, text)`); on mismatch: `_mixer.clear_speech()`, emit `sim.script.error` "gemini_text inject role/say mismatch; aborting for sapi_fallback" `{heard[:240], expected[:240]}`, raise. No audio (`saw_ms <= 0`): farewell → best-effort return, else raise "gemini_text inject produced no mic audio (model stayed silent)". Drain deadline **8.0 s** watching STT for mid-utterance role-flip (same >=5-word mismatch → clear_speech, raise); `speech_queued_ms() <= 0` → wait up to **1.6 s** for trailing STT, break. Then 0.15 s settle; heard_final empty → farewell ? return : raise "gemini_text inject STT missing (cannot verify caller identity)"; mismatch → clear_speech + raise. finally: `wait_for(_inject_playout_done.wait(), timeout=8.0)` (TimeoutError → pass), `_agent_audio_paused=False`, `_inject_turn_active=False`, `_inject_playback_gain=1.0`, `_inject_heard_text=""` ("Resume agent audio only after the model's turn_complete for the injected text lands — resuming earlier collides with the still-open Live activity and closes the socket (1007)").
SAPI fallback (`_inject_sapi_fallback`, script identity primary when mixer exists and not hangup farewell): synthesize `synthesize_pcm16_mono(text, rate=TARGET_RATE)` in a thread; `duration_s = max(0.05, len(pcm)/2/TARGET_RATE)`; `suppress_persona_output(int(duration_s*1000)+400)`; `speech_gain = clamp(gain * voice_gain)`; `_emit_user_audio_source_start(gain=speech_gain, via="sapi_fallback")`; `push_speech` + `end_speech_turn()`; emit `sim.script_inject {delivery:"sapi", effective_gain}`; sleep duration_s. Final failure → `RuntimeError("gemini_text inject failed: local TTS unavailable and Gemini Live failed")`.

1.9 **Bootstrap / reground / nudge / role gates**: bootstrap cues: only `kind == "bootstrap"`, empty text skipped, sent at connect via `send_realtime_input(text=text)` — a realtime-input TEXT turn, NOT wrapped in activity markers; emit `sim.caller_midcall {kind, label, text[:240]}`. `inject_reground`: no-op if no live session; only the FIRST cue with `kind=="reground"` → text turn + event. `release_after_milestone`: no-op ("On Gemini Live that text is a user turn and frequently caused role-flip... or double-opens."). `nudge_freestyle_answer`: no-op if no session; skip if silent_mode / hangup farewell / inject active / agent paused / persona suppressed; no-op when `not _agent_stream_open` ("redundant ends caused Live 1006"); else flush agent stream (`reason="freestyle_nudge"`); exceptions → `sim.error {where:"nudge_freestyle_answer", error}`.
Suppress logic (shared both bridges): `_suppress_output_until_mono` monotonic deadline extended to max (`suppress_persona_output(ms)`: `<= 0` no-op; `until = max(prev, now + ms/1000)`); `_persona_output_suppressed()` self-clears on expiry; `begin_scripted_user_silence(duration_ms, grace_s=20.0, mute_persona=False)` extends `_script_hold_until_mono` (max) and `_script_hold_grace_s` (max), with mute_persona also suppresses; `scripted_silence_active()` True while `now <= hold + grace`, self-clears on expiry. `_allow_persona_room_audio()`: True if `_script_hangup_farewell`; True if `_inject_turn_active`; False if `_silent_mode`; False if `_mute_persona_audio or _persona_output_suppressed()`; else True.

1.10 **Voice gain**: `resolve_voice_gain(persona)`: `sc = persona.get("speech_conditions") or persona.get("speechConditions") or {}`; `raw = sc.get("voice_gain", sc.get("voice_volume", sc.get("volume", 1.0)))`; must parse float; ValueError unless `0.0 <= gain <= 1.0` ("Persona.speech_conditions.voice_gain must be a number between 0.0 and 1.0"); default 1.0. Applies ONLY to sim speech (freestyle + inject): freestyle gain = `_voice_gain`; inject speech_gain = `clamp(gain * voice_gain)`. Noise beds use step gain only.

**2. OpenAI caller wire format**

2.1 **Connect URL + auth**: `wss://api.openai.com/v1/realtime?model=<model>`; headers `{ "Authorization": "Bearer <api_key>" }` (no beta header on GA); `max_size=None` — no message-size limit (tokio-tungstenite: unset/large max). NO reconnect loop — one socket for the whole call; mid-call transport drop → `transport_dropped=True` (retryable: False) + `end_call.set()`. No session resumption.

2.2 **session.update (exact JSON)**:
```json
{"type": "session.update", "session": {"type": "realtime",
  "instructions": "<persona_system_prompt>", "output_modalities": ["audio"],
  "audio": {"input": {"format": {"type": "audio/pcm", "rate": 24000},
    "transcription": {"model": "gpt-4o-mini-transcribe"}, "turn_detection": null},
    "output": {"format": {"type": "audio/pcm", "rate": 24000}, "voice": "<voice_name>"}}}}
```
- `turn_detection: null` — "VAD OFF (push-to-talk)... server VAD would treat agent speech as a caller interruption, cancel the in-flight model response, and the caller turn would never finalize."
- NEVER send an output-language parameter — "session.audio.output.language is NOT a valid GA parameter — the server rejects it with `Unknown parameter` and the model never responds."
- Voice: `_openai_voice_name(voice) = str(voice or "").strip().lower() or "marin"` ("Voice cannot change after first audio response").

2.3 **Client→server events**: `{"type": "input_audio_buffer.append", "audio": "<base64 PCM16 24k>"}` (agent room audio, pushed per frame while speaking; no activity markers); `{"type": "input_audio_buffer.commit", "event_id": "evt_commit_<monotonic_ms>"}`; `{"type": "response.create", "event_id": "evt_resp_<monotonic_ms>"}` (nudge: `"evt_nudge_..."`); `{"type": "input_audio_buffer.clear", "event_id": "evt_clear_<monotonic_ms>"}` — after every commit (buffer not auto-cleared with VAD off); `{"type": "conversation.item.create", "item": {"type": "message", "role": "user", "content": [{"type": "input_text", "text": "<speak_directive>"}]}}` then `response.create`; `{"type": "conversation.item.truncate", "item_id": "<id>", "content_index": 0, "audio_end_ms": max(0, played_ms - 200)}` — barge truncation, `_TRUNCATE_GRACE_MS = 200`.
`_send` guard: if ws None or not `_send_ok` → return; `if payload.type == "response.create" and self._response_in_flight: return` (server rejects response.create while another response is in flight — drop instead of erroring); `_response_in_flight=True` after sending response.create.

2.4 **Agent-audio manual VAD pump**: `speaking = obs_speaking or rms >= 100.0` → `_agent_last_speech_mono = now` (and `_agent_commit_pending=False` if set); else if `_agent_last_speech_mono is not None and (now - last)*1000 >= 650` → `_agent_commit_pending=True`, `_agent_last_speech_mono=None`, spawn `_commit_and_respond()`. Skip sending when `not speaking and frame_ms >= 650` (long silence — avoid flooding buffer). Else append (base64). Paused (inject) → continue (no flush logic). `_commit_and_respond` (on commit boundary AND caller output done): skip if ws None/not send_ok; skip if `_script_hangup_farewell or _mute_persona_audio`; skip if `_response_in_flight`; else commit → create → clear (event ids above); `_response_in_flight=True`; exceptions swallowed.

2.5 **Server→client events**: `error` → `sim.error {where: "openai_server", error: message or code or err}`. `input_audio_buffer.speech_started` → `_on_speech_started()` — emit `interruption {by: "agent", note: "OpenAI output interrupted by agent audio (input_audio_buffer.speech_started)"}`; `_mixer.clear_speech(); _mixer.end_speech_turn()`; best-effort truncate (`_last_item_start_mono` set → `played_ms = (now - start)*1000`; `_try_send_truncate(played_ms)` with `audio_end_ms = max(0, played_ms - 200)`); reset item state, `_mute_persona_audio=False`. (`speech_stopped` not consumed.) `response.output_audio.delta` → if delta and `_allow_persona_room_audio()`: decode; track item playback (`_sim_out_item_id`, `_last_item_ms_played += 1`); `ensure_future(_play_pcm(pcm))` — pump does NOT await playback; no `end_speech_turn` per delta. `response.audio_transcript.delta` → append to `_sim_out_text` (+ `_inject_heard_text` if inject); arm one-shot watchdog `_out_done_watchdog` (sleep 6.0 s; if `_sim_out_text.strip()` → `_on_output_done_fallback()`); early-bye mute (same as Gemini); log `observer.on_transcript("user", log_text, final=False, source="sim.openai")`. `response.audio_transcript.done` → `_on_output_transcript_done()`: cancel watchdog; join-strip; commit (ended/farewell/pending/clean exactly like Gemini's turn_complete, source="sim.openai"); if pending and (ended or farewell) and not farewell → `sim.script_deferred_end_call` + suppress 5000 + mute latch toggle, NO teardown; if `should_end_call_on_turn(...)` → mute, `ensure_future(_drain_persona_speech(timeout_s=3.0))`, `sim.end_call_token {text, reason}`, `end_call.set()`; else unmute + `ensure_future(_commit_and_respond())` ("Manual turn hand-off (VAD disabled): the caller just finished speaking. Commit any buffered agent audio + request the next model response"). Empty text → reset only. `response.created` → `_response_in_flight=True`; `response.cancelled` | `response.failed` → `_response_in_flight=False`. `response.output_item.added` → if type=="message" and role=="assistant": `_last_item_start_mono = now; _last_item_ms_played = 0`. `conversation.item.input_audio_transcription.delta` → `_agent_in_text += chunk`; log interim `observer.on_transcript("agent", text, final=False, source="sim.openai")`. `...completed` → `_agent_in_text = transcript` if non-empty; log final; emit `sim.heard_agent {text}` source="sim.openai"; reset `_agent_in_text=""`; `ensure_future(_commit_and_respond())`. `...failed` → `sim.error {where:"openai_agent_transcription"}`. `response.done` → `_response_in_flight=False`; `_mixer.end_speech_turn()`; if not inject → `_inject_playback_gain=1.0`; reset item-state and `_sim_out_text=""`. "Do NOT cancel the output-done watchdog here: response.done fires for the *agent's* barge response too, while the caller's own `.done` may still be pending (STT lag)."
Mid-call drop: if transport (classifier §1.5 + `"ConnectionLost"`/`"TimedOut"` in type name) → `transport_dropped=True`; emit `sim.openai_socket_drop {phase:"mid_call", error, retryable: False}`; emit `sim.error {where:"openai->lk"}`; `end_call.set()`. Receive loop unbounded (`async for raw in ws`).

2.6 **OpenAI inject**: text path (say-lines): `_user_text_item(speak)` + `response.create`; verify loop 2.8 s for `speech_queued_ms() > 0`; True on audio → success; False on silence → sapi fallback → else `RuntimeError("openai_text inject failed: local TTS unavailable and OpenAI session failed")`. `hangup_farewell` SKIPS the OpenAI text path and goes straight to sapi. No STT-mismatch abort on the OpenAI path (the item is exact text; only "did any audio play" is verified). `room_pcm` inject: identical to Gemini at 24 kHz (mono-only; rate must equal 24000 else ValueError; voice.* → speech layer + suppress(duration+400) + gain*voice_gain; noise → parallel layer, loop beds sleep min(0.05, duration_s)).

**3. Wire-verification deltas and crate decisions (sealed — do not revisit)**: (1) **Gemini**: vendor `gemini-live 0.1.8` (MIT, ~1400 LOC, tokio + tokio-tungstenite 0.29 + rustls 0.23 + serde + base64 0.22). Two patch deltas, both confirmed against the 0.1.8 source: `SpeechConfig.language_code` (src/types/config.rs:56-60 — add `#[serde(skip_serializing_if = "Option::is_none")] pub language_code: Option<String>`; struct already `#[serde(rename_all = "camelCase")]` → serializes `languageCode`; keep optional so existing callers compile; mirrors python-genai SpeechConfig types.py:5317); reconnect policy (src/session.rs:71-87 — add `pub reconnect_on_drop: bool` default false; in the runner session.rs:436-462 branch on internal DisconnectReason session.rs:408-414: GoAway → always reconnect (inject resume handle, already at session.rs:634-644); ConnectionLost → reconnect only if `reconnect_on_drop`, else emit `ServerEvent::Closed{reason}` + set Closed, mirroring Python `transport_dropped=True`; the crate default auto-reconnect-on-both is MORE aggressive than Python — do not ship unpatched); surface close codes (patch src/transport.rs:311-324 so `RawFrame::Close` carries the code, enabling the §1.6 classification table). Do NOT rely on the crate for VAD/SpeechStarted — ServerEvent has no speech-started/ended variants; manual activity markers + TurnComplete/Interrupted is the contract (same as Python). Setup JSON from the vendored crate is camelCase (`realtimeInput`, `activityStart`); Python sends snake_case (`realtime_input`, `activityStart`); the server accepts both — field presence is what matters; do not chase byte parity on key casing. (2) **OpenAI**: hand-roll on tokio-tungstenite (matches Python's raw `websockets`); protocol surface ~10 event types; `oai-rt-rs 0.4.0` (tokio-tungstenite, GA-shaped ClientEvent/ServerEvent models incl. truncate + speech_started/stopped) is the documented fallback if the event layer grows — protocol layer only, never LiveKit media. (3) **Audio**: mixer + AudioSource at 24 kHz for BOTH providers; Gemini output PCM (24 kHz) un-resampled (Python does the same — the only resample is SDK-side on agent-audio input: 48k→16k Gemini, 48k→24k OpenAI). (4) **Timing constants** (the ones that bite): suppress monotonic max-extension; script-hold grace default 20.0 s (extends); inject settle 0.15 s; verify window 2.8 s; drain 8.0 s; STT tail 1.6 s; trailing pad 120 ms; silence 650 ms; receive timeout 15 s (Gemini) / unbounded (OpenAI); handshake backoff `min(2*attempt, 6)` (2 s, 4 s); Gemini ws ping_interval 30 / ping_timeout 60 (without this the 20 s default kills calls ~4 min in with 1011); reconnect cap 2; truncate grace 200 ms; out-done fallback 6000 ms; sapi suppress pad +400 ms; audio_source_start once-per-utterance latch; room_pcm `duration_s = max(0.05, len(pcm)/2/rate)`; loop bed sleep `min(0.05, duration_s)`; dtmf digit gap 150 ms / `w` = 120 ms; script tick 50 ms; agent-reply budget 45.0 s; settle 900 ms; post-cue gap tolerance 1200 ms; hang-up drain `min(10.0, max(5.0, 1.2 + words*0.45))` + 0.55 s; `_wait_agent_idle` 6.0 s (speak) / 5.0 s (hang_up). (5) **Event/source names are report consumers' contract** — port byte-for-byte as listed in Appendix C.

### Appendix E: Script runtime semantics

**1. Step model (`ScriptStep`, frozen dataclass)** — fields: `id` (required); `trigger` (`agent_speaking`\|`silence`\|`time`, required); `delay_ms` (required); `say` (`""`); `label` (`""`); `once` (True); `min_agent_active_ms` (400); `delivery` (`"gemini_text"`\|`"room_pcm"`); `asset` (None); `silence_after_cue_ms` (0); `action` (`speak`\|`wait`\|`hang_up`\|`dtmf`); `mute_persona` (None; None/False = pace only, True = mute freestyle — only meaningful for wait + silence_after_cue_ms); `digits` (`""`, chars `0-9*#w`, `w` = 120 ms gap); `loop` (False, room_pcm noise beds only); `require_agent_spoke_first` (True); `require_agent_reply_this_turn` (True); `defer_on_open_question` (True); `open_question_idle_ms` (20000); `barge_in` (False); `with_blip` (True, barge_in + gemini_text → builtin `noise.blip` first); `gain` (1.0); `interrupt_class` (None: `correction`\|`backchannel`\|`noise`\|`dtmf`\|`silence`\|`escalate`); `overlay` (None: `fixture`\|`line`\|None → auto).

**Parse pipeline** (error message strings are load-bearing): id falls back `label` then `f"step-{i}"`; `say` accepts `text` alias; trigger default `agent_speaking`; action default `speak` (unknown action error literally "action must be speak|wait"); speak with empty say → parse error; delivery default `gemini_text` (validated to 2 values); room_pcm speak requires asset. Defaults `delay_ms=800`, `min_agent_active_ms=400`. **barge_in FORCES `trigger="agent_speaking"`, `action="speak"`** and changes defaults (`delay_ms=250`, `min_agent_active_ms=200`); accepts `interrupt` alias. DTMF: digits from `digits`/`digit`, charset `set("0123456789*#w")` enforced ONLY when action==dtmf; `say = "[DTMF: <digits>]"`; backward compat: dtmf with trigger not in (silence, time) → trigger="time". `with_blip` default = `barge_in and delivery != "room_pcm"`. `gain`/`volume` alias, both 0.0-1.0. `loop`/`repeat` (`repeat is True` → loop; repeat present and not in (None, 0, 1, "0", "1") → hard parse error "use loop=true for continuous ambient beds (repeat count is not supported)"); `loop` requires delivery=room_pcm and action=speak; asset normalized (strip `builtin:`/`@`), starts with `voice.` → "loop is for noise/ambient beds, not voice.* speech assets". `interrupt_class` via `normalize_interrupt_class(raw, barge_in)`: None/blank → `"correction"` if barge_in else None; key lowercased, `-`/space → `_`; aliases: `true_correction`/`correct`/`barge`→correction; `ack`/`uhhuh`/`uh_huh`→backchannel; `false_positive`/`false_interrupt`/`click`→noise; `digit`/`digits`→dtmf; `human`/`handoff`/`safety`→escalate; unknown → ValueError listing supported classes. `overlay`/`speech_role` alias, lowercased, `-`→`_`; `forced_line`/`forced`/`say` → `"line"`; only fixture|line accepted. `effective_overlay(step)`: explicit overlay wins; else `barge_in` OR delivery==room_pcm OR class in (noise, backchannel, dtmf, silence) → `"fixture"`; else action==speak with non-empty say → `"line"`; else `"fixture"`. `counts_for_recovery_barge(barge_in, icls)`: False unless barge_in; else `(icls or "correction") in {"correction", "escalate"}`.

**2. Runner loop**: tick every **50 ms**; ONLY the step at `_armed_step_index` evaluated; skip if `once and id in _fired` or `id in _firing`. `has_pending_steps()` = `not _stop.is_set() and _armed_step_index < len(steps)`. Empty steps → run() returns immediately. Wait-states: agent-reply wait (after a spoken milestone): aged >= 45.0 s → clear, arm proceeds; else `_maybe_nudge_freestyle(min_quiet_ms=1600)`; requires a NEW agent final after wait start, then 900 ms settle (`_post_speak_settle_ms`). Post-cue gap (agent_speaking-trigger non-speak steps): 1200 ms of agent-quiet after the cue (`_active_speaker_gap_tolerance_ms`). `_trigger_active`: agent_speaking → `observer.agent_is_active_speaker`; silence → False if `require_agent_spoke_first and not agent_has_spoken` else `not agent_is_active_speaker`; time → True. Trigger debounce: >= 1200 ms inactive resets `_trigger_since`/`_trigger_gap_since`. Delay: `elapsed = now - _trigger_since[id]`; **if trigger == "agent_speaking": `need = min_agent_active_ms + delay_ms`**; hang_up steps that ever deferred: `elapsed_ms = max(elapsed_ms, need)`. On elapsed for hang_up: `bridge.suppress_persona_output(1500)` (wrap-up mute so Gemini cannot invent loops while Script bye awaits), then `_hang_up_ready(step)`; not ready → continue WITHOUT popping the trigger timestamp (popping would reset elapsed and the step could never fire again). `_fire` finally: `_firing.discard`; `once` → `_fired.add`; **if `inject_error and action == "speak"`: `_armed_step_index = len(steps)` (abort chain) AND `bridge.sim_hang_up()`**; else `_armed_step_index += 1`; if more steps remain and action==speak and not barge_in → `_await_agent_reply_since = now`; else `_await_post_cue_gap = (trigger == "agent_speaking")`. Then clear trigger timers. `silence_after_cue_ms`: computed at fire time; drives `begin_scripted_user_silence` + paced hold ONLY for action=wait; for speak/dtmf/hang_up it appears in the emitted spec only, NOT applied as mute ("Intentional caller silence: action=wait + silence_after_cue_ms").

Actions: **speak**: if not barge_in → `_wait_agent_idle(timeout_s=6.0)` (never talks over agent). If barge_in and with_blip and delivery != room_pcm → inject `"[barge blip]"` label `f"{label or id}-blip"` delivery=room_pcm asset=`builtin:noise.blip` (blip failure → `sim.script.error`, chain continues). Then `bridge.inject_cue(say, label, delivery, asset, scenario_dir, gain, loop)`. Exception → `inject_error = f"{TypeName}: {e}"` + `sim.script.error`. If `barge_in and during_agent_speech and no inject_error` → emit `interruption` `{by:"sim", barge_in: True, class: icls, false_positive: icls in ("noise","backchannel"), step_id, label, say, note: "Script barge-in while agent was active speaker"}` source=sim.script. **wait**: `hold_silence_ms > 0` → `mute = False if step.mute_persona is None else bool(step.mute_persona)`; `bridge.begin_scripted_user_silence(hold_silence_ms, mute_persona=mute)` (TypeError fallback: if mute, duration-only); if no begin_scripted_user_silence and mute → `suppress_persona_output(hold_silence_ms)`. Paced hold: sleep in <= 0.25 s slices, nudge only when `not mute`. **hang_up**: `silent = bridge._silent_mode`; if silent → `say_text=""`; elif empty → locale default farewell (en/en-us/en-gb → "Okay, thanks. Bye."; vi/vi-vn → "Cảm ơn bạn. Tạm biệt."; ja/ja-jp → "ありがとうございます。失礼します。" — exact or base-lang match, fallback en). `_wait_agent_idle(5.0)`; if say_text → `begin_script_hangup_farewell()`; `inject_cue(...)` (error captured, not raised); emit `sim.script.hang_up` + `sim.hang_up`; drain: `drain_s = min(10.0, max(5.0, 1.2 + words*0.45))`; `bridge.drain_persona_speech(timeout_s=drain_s)` else `asyncio.sleep(min(4.0, drain_s))`; `await asyncio.sleep(0.55)`; finally `end_script_hangup_farewell()`. Emit `sim.script.hang_up` + `sim.hang_up` a SECOND time (verify matches step_id on this kind), then `bridge.sim_hang_up()` and return. **dtmf**: DMAP `{"0":0.."9":9,"*":10,"#":11}`; per char: `"w"` → sleep 0.12 s; digit → `await local.publish_dtmf(code=DMAP[ch], digit=ch)` then sleep 0.15 s; unknown char or publish exception → `inject_error`, break. No `_wait_agent_idle` on dtmf (digits may land mid-agent-speech).
`_maybe_nudge_freestyle` (SI): non-text kick — `audio_stream_end` only; guards: not agent active, bridge has `nudge_freestyle_answer`, `agent_left_open_turn(last_agent_final_text)`, `last_agent_final_mono` set, no user final after agent final, quiet >= 1600 ms, >= 3.5 s since last nudge; exceptions swallowed → False.

**3. hang_up gate**: `agent_left_open_turn(text)`: blank → False; lowercase. `_CLOSING_MARKERS` = ("goodbye","good bye","bye for now","bye.","bye!","have a great","have a good","take care","thank you for calling","thanks for calling","call ended","hanging up") — closing wins (→ False). `"?"` anywhere → True. Else True iff any `_OPEN_PROMPT_MARKERS` substring (27 markers — REVISED; ground truth hang_up_gate.py:6-50): "what's your","what is your","what was your","which car","what sort of","may i have","can i have","could you","can you tell","can you give","can you hear","still there","are you there","please provide","please tell","your name","full name","phone number","email address","card number","how can i help","anything else","shall we","would you like","do you want","are you ready","whereabouts" (+ port the remaining markers byte-for-byte from hang_up_gate.py — verify count == 27). Docstring: "Heuristic only — used to defer Script hang_up, not to ban barge-in."
`_hang_up_ready(step)` — reasons in order: (1) `require_agent_reply_this_turn and user_has_spoken and not agent_replied_this_turn` → "awaiting_agent_reply". (2) `defer_on_open_question and agent_left_open_turn(last_agent_final_text)` and NOT (both finals exist and user_t > agent_t) → "open_agent_question". No reason → discard defer state, return True. **Defer budget**: `budget_ms = max(0, open_question_idle_ms)` (default 20000); `started` set on FIRST defer, NEVER reset by new agent finals; `deferred_ms >= budget_ms` → emit `sim.script.hang_up_deferred` `{step_id, reason: "defer_budget_exhausted", prior_reason, deferred_ms, budget_ms, last_agent_final: (text or "")[:240]}` source=sim.script include_dialogue=False, return True (ghost hang allowed). Else first defer → same event with `reason`, return False. `_wait_agent_idle(timeout_s=5.0)`: poll 50 ms until `not agent_is_active_speaker` or stop or deadline.

**4. End-call / farewell heuristics (regexes byte-for-byte)**: `END_CALL_TOKEN = "[END_CALL]"`. `_SPOKEN_END_RE = re.compile(r"(?i)(?:\[\s*end[_\s\-]*call\s*\]|\bend[_\s\-]*call\b|\bhang[_\s\-]*up\b)[.!?]*")`. `_FAREWELL_RE` = the long alternation incl. Vietnamese `tạm\s*biệt|kết\s*thúc|cúp\s*máy` (port byte-for-byte from end_call.py). `contains_end_call_signal(text)`: falsy → False; token in text → True; else `_SPOKEN_END_RE.search`. `contains_farewell_signal(text)`: falsy → False; end-call → True; else `_FAREWELL_RE.search`. `strip_end_call_signal(text)`: replace token with " ", `_SPOKEN_END_RE.sub(" ", out)`, `re.sub(r"\s+([,.!?])", r"\1", out)`, `re.sub(r"[,\s]+$", "", out)`, `" ".join(out.split()).strip()`. `strip_farewell_signal(text)`: strip_end_call_signal then `_FAREWELL_RE.sub(" ", out)`, same two regex passes, same final collapse. `should_end_call_on_turn(*, pending_script, ended, farewell, scripted_farewell)`: False if scripted_farewell; False if pending_script; else `bool(ended or farewell)` ("Soft bye alone is enough when no Script owns hang-up").

**5. verify.py computation**: cues = kinds (`sim.script.cue`, `sim.script.wait`, `sim.script.hang_up`) — NOT dtmf; agent_finals = `transcript.agent.final`; user_finals = `transcript.user.final`; interruptions = kind `interruption`. Per step: first cue matching `spec.step_id == step.id`; none → fail "script step not fired"; `spec.error` → fail "cue fired with error: ..."; **hardcoded (spec field ignored): `step.trigger == "agent_speaking" and step.action == "speak" and not during_agent_speech` → fail "cue fired but agent was not active speaker"**; else pass with `{during_agent_speech, trigger, action}`. Timestamps: `cue_ms` = first cue's ts; silence_ms from cues with trigger=="silence" or action=="wait"; barge_ms from cues with kind sim.script.cue and (counts_for_recovery_barge OR legacy heuristic: no class AND during_agent_speech AND trigger==agent_speaking AND `int(spec.waited_ms or 9999) < 800`). Counts: `agent_after_cue`/`user_after_cue` with ts >= cue_ms; `agent_after_silence` >= silence_ms; **`agent_after_barge` STRICTLY > barge_ms**. Checks appended only when spec value > 0 (or not None): min_agent_finals_after_first_cue, min_user_finals_after_first_cue, min_agent_finals_after_silence, min_agent_finals_after_barge_in, min/max_interruptions. Plugins: unregistered → fail `plugin:<name>`; missing scenario/project_root → fail; exception → fail `f"{TypeName}: {e}"`; result `{"pass", "checks" (merged with "plugin" key), "detail"}`. Return: `script_steps, cues_fired, waits_fired, hang_ups_fired, counts, interruptions, checks, plugin_results, pass: all(check.pass) if checks else False` — empty checks (empty script) FAILS.

**6. Summary computation (zeros-safe)**: cues = kind `sim.script.cue`; waits = `sim.script.wait`; barges = cues where counts_for_recovery_barge; `barges_during` = barges with during_agent_speech; `cues_during` = all cues with during_agent_speech; silence_events = `silence.detected`; interruptions = `interruption`; agent_finals = `transcript.agent.final`. `barge_ms` = first barge ts; `silence_ms` = first wait ts; `recovery_ms` = first agent final with ts > barge_ms minus barge_ms; `agent_after_silence` = finals ts >= silence_ms; `cue_assets` = unique non-empty spec.asset from `sim.script.cue` + `sim.script_inject`. Output keys: `script_cues_fired, waits_fired, barges_fired, barges_during_agent, cues_during_agent, silences_held (=len(waits)), silence_events, interruptions, agent_finals_after_barge, agent_finals_after_silence, recovery_ms, cue_assets, by_class` (from spec.class or spec.interrupt_class, including non-barge classes).

**7. Delivery semantics recap (bridge inject_cue, both providers)**: silent_mode + not hangup farewell → `sim.silent_mode_skip_inject`, return. room_pcm: `load_wav_pcm`; mono only (ValueError "Only mono room_pcm assets are supported"); rate must equal OUT_RATE 24000 (ValueError "room_pcm asset rate {rate} != sim mic 24000 (resample cue WAV): {path}"); `duration_s = max(0.05, len(pcm)/2/rate)`. voice.* → suppress_persona_output(int(duration_s*1000)+400); speech_gain = clamp(gain * voice_gain); push_speech; end_speech_turn (complete WAV — drain without jitter waterline hold); sleep duration_s. Noise → push_noise(gain=gain, loop=loop); one-shot sleeps duration_s; loop sleeps `min(0.05, duration_s)` ("Continuous bed: arm quickly so Script/freestyle can continue under noise"). Emit sim.script_inject, return. gemini_text (non-farewell): SAPI-first when mixer exists, drain 8 s + 0.2 s; else `_inject_gemini_text` (Live); on exception → sim.script.error "gemini_text failed (...); trying sapi_fallback" → sapi; final failure raises.

### Appendix F: Observer + lk.agent.session protocol

**1. Track subscription + recording**: AudioStream `rtc.AudioStream(track, sample_rate=16_000, num_channels=1)` — 16 kHz mono PCM16 from the agent-identity audio track ONLY (`p.identity == self.agent_identity and track.kind == KIND_AUDIO`); observer NEVER subscribes to sim/caller audio. Two triggers: (a) `room.on("track_subscribed")` → start record; (b) pre-attach catch-up: on `attach()`, iterate `room.remote_participants` for the agent and already-subscribed audio tracks ("Agent track may already be subscribed before attach (common on SIP legs)"). Dedupe by `track.sid or id(track)` — second record of same sid is a no-op. Recording only if `recorder is not None`. Pump: emits `sim.agent_audio_recorded {track_sid, source: "observer.agent_room", sample_rate: 16_000}`; pushes PCM to `recorder.push_agent(pcm, 16_000, track_id=key)` AND feeds the same bytes to the onset detector; exception → `sim.error {where: "observer.agent_record", error, track_sid}`; finally closes the stream. No `track_unsubscribed` handler — the pump just ends.
**Rust SDK mapping (verified, livekit 0.8.3 / livekit-data-stream 0.1.2)**: the SDK is event-driven — NO `register_text_stream_handler`-style registration. Pattern: `room.subscribe()` → `mpsc::UnboundedReceiver<RoomEvent>`; match `RoomEvent::TextStreamOpened { reader: TakeCell<TextStreamReader>, topic, participant_identity }` / `ByteStreamOpened` (room/mod.rs:228-231 / :223-226); `reader.take()` exactly once per event (TakeCell — second take returns None); spawn per-stream read loops with `StreamExt::try_next()` or `read_all()` (observer uses read_all for lk.transcription). `TextStreamInfo` (info.rs:66-104): `id` (uuid — NO `sid` field), `topic`, `timestamp`, `total_length`, `mime_type`, `operation_type`, `version`, `reply_to_stream_id`, `attached_stream_ids`, `generated`, `encryption_type`, `attributes() -> HashMap<String,String>` (header attributes merged with trailer). Internal topics `lk.rpc_request`/`lk.rpc_response` (rpc/mod.rs:37-38) consumed by the SDK's RpcServerManager/RpcClient, never surface as RoomEvents — same as Python. Fallback: deprecated `StreamHeaderReceived`/`StreamChunkReceived`/`StreamTrailerReceived` raw proto events.

**2. RmsOnsetDetector + `sim.agent.audio_onset`**: construction args EXACT (observer.py:107-115): `sample_rate=16_000` (hardcoded), `win_ms=ao.win_ms` (default 20), `threshold=ao.threshold` (default 0.012), `energy_frames=ao.energy_frames` (3), `exit_frames=ao.exit_frames` (5), `refractory_ms=ao.refractory_ms` (60), `on_onset`. Derived: `win_samples = (16000*20)//1000 = 320`; `refractory_samples = (16000*60)//1000 = 960`. VAD method = `ao.vad`, default "rms", validated `in ("rms",)` else ConfigError "must be `rms`... other VAD backends are future work"; threshold `0.0 < t < 1.0`. Detector built only `if recorder is not None and observe.audio_onset.enabled`. State machine: SILENCE→SPEECH requires `energy_frames` CONSECUTIVE windows with RMS >= threshold; onset fires at the FIRST frame of the run: `onset_start = start - (energy_frames-1)*win_samples`, clamped >= 0. First onset records `frames_before_first_onset = onset_start`. Refractory checked BEFORE speech entry: if `(onset_start - _last_onset_consumed) >= refractory_samples` → fire; else still enters SPEECH without firing (one long burst = one onset). SPEECH→SILENCE: `exit_frames` consecutive windows below threshold. Chunk-boundary invariant: rolling `array("h")` buffer; windows consumed only when fully available; odd byte dropped; flush() drops a partial window ("no onset can begin mid-window"). Window energy: `RMS = sqrt(sum(s^2)/n)/32768.0`; `pcm16_mono_rms()` same formula, 0.0 for empty. `onset_to_audio_ms(frame_idx, rate) = int(round(frame_idx * 1000 / rate))`. Payload (EXACT): `{channel: "agent", sample_rate: 16_000, onset_frame_idx, frames_before_first_onset: int(frames_before_first_onset or 0), vad: {method, threshold, win_ms, energy_frames, refractory_ms}}`; source="sim", include_dialogue=False, turn=None, ts_mono_ms=corrected_ts. **Backdating**: `audio_t0_mono = int((recorder.started_mono - writer.t0_mono) * 1000)`; `onset_ms = onset_to_audio_ms(onset_frame_idx, 16_000)`; `corrected_ts = max(0, audio_t0_mono + onset_ms)` — "the *perceived* agent speech onset — never detection time". Returns silently if recorder None or started_mono None. EventWriter additionally clamps `mono = max(0, int(ts_mono_ms))`. Caveat: `started_mono` pinned by `mark_start()` (mic publish) OR lazily at first push; `_push` gap-pads only when `gap_s > 0.02`.

**3. Transcription intake**: (a) **lk.transcription text stream** — registered only if `observe.lk_transcription` (default True). Handler (reader, participant_identity) wrapped in a task. Read `text = await reader.read_all()` — FULL stream, not incremental; exception → `observer.error {where: "lk.transcription", error: "TypeName: msg"}`. The payload is PLAIN TEXT (not JSON). Attributes from `reader.info.attributes`: `ATTR_FINAL = "lk.transcription_final"` (`attrs.get(...).lower() == "true"`); `ATTR_SEGMENT_ID = "lk.segment_id"`. Empty text dropped silently. Role: `"agent" if participant_identity == agent_identity else "user"` (ANY other participant → "user"). Calls `on_transcript(role, text, final, segment_id, source="lk.transcription")`. Rust: attribute keys NOT named constants in the SDK — define them in the port. (b) **TranscriptionReceived**: Python observer.py has NO TranscriptionReceived handler — transcription intake comes from data-topic `transcript_turn` payloads and direct `on_transcript` calls from the bridges. The Rust SDK DOES expose `RoomEvent::TranscriptionReceived` — available if ever wanted, but Python parity does NOT consume it. (c) **sim.heard_agent**: informational mirror from the bridges, NOT fed to on_transcript.

**4. `_accept_final` dedupe + turn model (observer.py:426-547)** — priority tables verbatim (lower index = higher priority; do not unify):
```
_SIM_TRANSCRIPT_SOURCES = ("sim.gemini", "sim.openai")
_USER_FINAL_PRIORITY    = ("sim.gemini", "sim.openai", "data", "lk.transcription")
_AGENT_FINAL_PRIORITY   = ("data", "lk.transcription", "sim.gemini", "sim.openai")
```
Rationale: provider sim-transcript sources are the most trustworthy caller transcripts; data-topic + lk.transcription are mirrors; the agent's own transcript is most trustworthy from the agent SDK.
`_canonical_source(source)`: unchanged if in (*_SIM_TRANSCRIPT_SOURCES, "lk.transcription"); EVERYTHING else (incl. arbitrary data topics) → "data". Two different data topics collide on the same dedupe key.
`_accept_final(role, text, source)`: (1) `norm = re.sub(r"\s+", "", text.strip())` — ALL whitespace stripped, case-sensitive; empty → False. (2) `key = (role, norm)`; `window = observe.transcript_dedupe_window_ms / 1000` (default 15_000 ms, checked `now - prev_mono <= window_s` inclusive). (3) Prev entry exists and within window → DROP iff `rank(new) >= rank(prev)` — the new source must be STRICTLY higher priority (lower rank) to replace. (4) Accepted → store (source, now), return True. Stored entry is OVERWRITTEN even by a lower-priority source arriving > 15 s later (no tombstone; a later duplicate with lower priority CAN win outside the window). Entries never pruned/expired. `_similar_text(a, b)`: a==b → True; either empty → False; shorter-in-longer substring → `len(shorter)/len(longer) >= 0.85`; else False.
`on_transcript(role, text, final, segment_id=None, source="lk.transcription")` — ordering (side effects BEFORE dedupe; dropped finals still count as activity): (1) `writer.update_dialogue(role, text, final, at_ms=wall_now_ms)`. (2) spec = {text, final} + segment_id if present. (3) Any transcript: role==agent → `last_agent_activity_mono=now`, `_agent_has_spoken=True` (interim counts — "realtime providers deliver final late"); ALWAYS `last_activity_mono=now`, `_any_activity=True` ("Caller activity also counts: the dead-call net must not fire while the agent is still working on its first reply"). (4) Not final: if `_role_has_final(role)` → return (DROP late interim after final — "OpenAI bridge can deliver trailing .delta after .done"); else emit `transcript.{role}.interim`, return. (5) Ordering guard: if `source == "sim.openai"` and `_last_interim_key != (role, text)` → set key, emit `transcript.{role}.interim` with spec {**spec, "final": False} (final-flavored interim — "Emit a final-flavored interim first so consumers never see interim-after-final within a turn"). Only for source=="sim.openai". (6) `if not _accept_final(...): return`. (7) USER branch: if `_last_user_final_mono is not None and not _agent_replied_this_turn:` → emit `transcript.user.final` spec {**spec, "same_turn": True}, return (does NOT advance turn). If `_agent_replied_this_turn and (norm == _current_turn_user_norm or _similar_text(norm, _current_turn_user_norm or ""))` → return (duplicate utterance within the turn). Else new turn: `_user_has_spoken=True; turn += 1; _current_turn_user_norm = norm; writer.begin_turn(turn); _finalized_roles.clear(); _last_user_final_mono = now; _agent_replied_this_turn=False; _finalized_roles.add("user")`; emit `transcript.user.final` spec=spec. (8) AGENT branch: if `turn == 0 and first_speaker == "user" and not _user_has_spoken` → emit `transcript.agent.preamble` spec {**spec, "note": "agent spoke before user; not counted as a turn"} (EXACT note), return — no turn increment, no `_agent_replied_this_turn`. If `turn == 0` → turn=1, `writer.begin_turn(1)` (first_speaker=="agent" case). If `not _agent_replied_this_turn and _last_user_final_mono is not None`: `spec["turn_taking_ms"] = int((mono_now - _last_user_final_mono)*1000)` — set ONLY on transcript.agent.final, ONLY when this is the first agent reply of the turn AND a user final exists. Then `_agent_replied_this_turn=True; _last_agent_final_mono=now; _last_agent_final_text=text; _finalized_roles.add("agent")`; emit `transcript.agent.final`.
Two clocks — monotonic for dedupe/turn_taking/ts_mono_ms; wall clock for `ts`/`at_ms`. Do not merge.

**5. Data topics**: `room.on("data_received")` → topic = packet.topic or ""; FILTER: `if observe.data_topics and topic not in observe.data_topics: return` — empty list (default) = observe ALL topics ("empty=all"). Payload parse: `json.loads(packet.data.decode("utf-8"))`; parse failure → emit `data.raw {topic, bytes: len(packet.data), sender}` (include_dialogue=False, source=topic or "data"), return. On success: (1) `_match_tool_patterns(topic, payload)` — first matching pattern emits and returns True. Patterns from `observe.tool_event_patterns` (ToolEventPattern: match: dict key→expected, emit: str). Matching: for each (key, expected): key=="topic" → exact topic comparison; else `_lookup_path(payload, key) != expected` → False; ALL must match (AND). `_lookup_path`: dotted-key traversal, non-dict → None. (2) `_parse_transcript_payload(payload)`: `payload.get("type") in observe.transcript_payload_types` (default ["transcript_turn"]); `payload.get("interim")` truthy → None; `turn = payload.get("turn")` must be dict; `role = turn.get("role")` in ("user","agent"); `text = turn.get("text")` str non-empty-after-strip; returns (role, text.strip()). (3) If parsed: `on_transcript(role, text, final=True, source=topic or "data")` — always final=True; source = actual topic string (canonicalized to "data" for ranking). (4) If neither: emit `data.message {topic, sender, payload}` (source=topic or "data", include_dialogue True).
Tool event emission: `name = payload.get("tool") or payload.get("name") or _lookup_path(payload, "spec.name")`; `call_id = payload.get("call_id") or payload.get("toolCallId") or _lookup_path(payload, "spec.call_id")`; spec = {name, call_id, payload}. emit_kind=="tool.start" → emit("tool.start", source=topic), store event in `_open_tools[call_id]` if call_id. Else (tool.end/tool.error): pop `_open_tools[call_id]` → `parent_id = start["event_id"]`, `spec["duration_ms"] = int((mono_now - writer.run_start_mono)*1000) - start["ts_mono_ms"]` (data-topic path does NOT clamp — can go negative); tool.error adds `spec["error"] = payload.get("error") or payload.get("message") or _lookup_path(payload, "spec.error")`; emit with parent_event_id. No start → parent None, no duration_ms.
Rust SDK: `RoomEvent::DataReceived { payload: Arc<Vec<u8>>, topic: Option<String>, kind: DataPacketKind, participant: Option<RemoteParticipant> }` — participant is Option; treat None gracefully. Sending: `LocalParticipant::publish_data(DataPacket { payload, topic, reliable, destination_identities })`.

**6. lk.agent.session (agent_session_observer.py)**: topic `TOPIC_SESSION_MESSAGES = "lk.agent.session"`; handler filters `participant_identity != agent_identity` → return. **Framing**: accumulate ALL chunks into a bytes list; parse ONE concatenated `AgentSessionMessage` protobuf per byte stream — NOT length-prefixed, NOT repeated. Parse or read error → `observer.error {where: "lk.agent.session", error}`. Dispatch: HasField("response") → pop `_pending_requests[response.request_id]`, set result. HasField("event") → handle.
Event oneof (order): `function_tools_started` → per call `_emit_tool_start`; `function_tools_executed` → `_handle_tools_executed`; `tool_execution_updated` → `_handle_tool_execution_updated`; `conversation_item_added` → `_handle_conversation_item_added`; `agent_state_changed` → emit "session.agent_state" {old_state, new_state} (enum value→name via `_enum_name`, numeric fallback); `user_state_changed` → "session.user_state"; `session_usage_updated` → "session.usage" (dedupe: identical consecutive usage dicts → only first); `error` → "session.error" {message}; `overlapping_speech` → "session.overlapping_speech" (MessageToDict); `debug_message` → "session.debug" ONLY if any value not in ("", None, [], {}) — non-message debug → {message: str(...)}. All via `_emit_session`: source="lk.agent.session", include_dialogue=False. MessageToDict flags: `preserving_proto_field_name=True`, `use_integers_for_enums=False`.
tool_execution_updated fallback: always emit "session.tool_execution" (MessageToDict). "started" with HasField → function_call with name/call_id/id → `_emit_tool_start`. "ended" → key = call_id or id; skip if in `_completed_call_ids`; `status_name = _enum_name(ended, "status")`; `is_error = status_name in ("TC_ERROR", "TC_CANCELLED")` (EXACT tuple); output from `ended.message or status_name or ""`; emit paired. ("Some agent SDKs emit tool_execution_updated before function_tools_executed... teardown races still record tools.")
conversation_item_added fallback: item oneof "function_call" → _emit_tool_start; "agent_handoff" → `_emit_handoff`. `_emit_handoff`: `old_id = handoff.old_agent_id or ""; new_id = handoff.new_agent_id or ""`; if `not old_id or old_id == new_id` → return (no-op — "initial agent assignment, NOT a transfer" false-positive guard; OpenAI Realtime emits AgentHandoff at session start with old_agent_id empty). Emit "session.handoff" {id: handoff.id or None, old_agent_id, new_agent_id, created_at: created.ToJsonString() if created is not None else None} — Timestamp → ISO string via ToJsonString() (raw Timestamp crashed the writer — regression note).
`_tool_key(call_id, item_id) = call_id or item_id or None`. `_emit_tool_start(call)`: spec = {id: call.id or None, call_id: call.call_id or None, name: call.name or None, arguments: call.arguments}; dedupe via `_started_call_ids`; emit "tool.start" source=_SOURCE; store in `_open_tools[key]`. `_emit_tool_output(output, paired_start, paired_key)`: dedupe via `_completed_call_ids`; spec = {id, call_id, name, output, is_error: bool}; fill name from paired start; `duration_ms = max(0, int((mono_now - writer.run_start_mono)*1000) - int(start["ts_mono_ms"]))` (L3 path CLAMPS); is_error → spec["error"] = output.output; emit "tool.error" if is_error else "tool.end" with parent_event_id = start["event_id"] if start. `_handle_tools_executed`: pair by array index; call_id mismatch between call and output → emit "observer.warning" {where: "function_tools_executed", message: "call/output call_id mismatch; paired by array index", call_id, output_call_id, index} (EXACT message); leftover outputs emitted unpaired.
Requests: `future = loop.create_future(); _pending_requests[request_id] = future`; `stream_bytes(name=f"AS_{uuid.uuid4().hex[:12]}", topic=TOPIC_SESSION_MESSAGES, destination_identities=[agent_identity])`; ONE write of the serialized message; aclose; `await asyncio.wait_for(future, timeout=60.0)` (request_timeout_s default 60.0). Exception → pop pending, re-raise. `response.error` truthy → `RuntimeError(f"session request {request_type} failed: {response.error}")`. Snapshots: `fetch_session_snapshot()` = get_chat_history then get_session_usage, each swallowing errors via `observer.error {where: "lk.agent.session.<op>", error}` ("without making snapshot failure fatal"). Request ids `req_<12 hex>`; bodies SessionRequest(request_id, get_chat_history=...) / get_session_usage=.... Chat history → emit "session.chat_history" {items} (item dicts per Appendix C), then `_reconcile_history(items)`: function_call → _emit_tool_start; function_call_output → ensure paired start then _emit_tool_output; agent_handoff → _emit_handoff.
Detach order: `drain_ingress(timeout_s=0.75)` FIRST ("Prefer draining late tool events before tearing the handler down"), then unregister handler, then cancel pending futures + tasks. drain_ingress (default 1.5 s) = `wait_for(gather(*pending, return_exceptions=True), timeout=...)`; on TimeoutError leave tasks for detach ("tools that delete the room publish function_tools_*/... as the peer disconnects; cancelling ingress drops frames → false-negative tool.start/tool.end"). Observer integration: constructed if `observe.lk_agent_session` (default True); attach on observer attach; `finalize_session_snapshot()` = drain_ingress(1.5) then fetch_session_snapshot; public post-disconnect hook drain_session_ingress(1.5); detach cancels record tasks then agent_session.detach().
**Rust SDK mapping (verified)**: `ByteStreamReader` implements the StreamReader trait (Stream<Item=StreamResult<Bytes>>) with `info()`, `progress()`, `read_all()`, `write_to_file(directory, name_override)`. `ByteStreamWriter` (outgoing/stream_writer.rs:62-65) + StreamWriter trait: `info()`, `write(&[u8])`, `close()`, `close_with_reason(reason)`, `close_with_options(reason, attributes)` (trailer attributes). `StreamByteOptions` HAS `pub destination_identities: Vec<ParticipantIdentity>` (line 40) with `with_destination_identities`/`with_destination_identity` builders AND `sender_identity: Option<ParticipantIdentity>` for agent impersonation; `StreamTextOptions` likewise. Entry points: `local_participant.stream_bytes(options) -> StreamByteWriter`, `stream_text(options)`, `send_bytes(data, options)`, `send_text`, `send_file`. Receiving the RESPONSE on the same topic: RoomEvent carries no request correlation — correlate via attributes/stream id. RPC (if used): `RpcServerManager` (room/rpc/server.rs:47): `register_method(method: String, handler: Fn(RpcInvocationData) -> Pin<Box<dyn Future<Output=Result<String,RpcError>>+Send>>+Send+Sync+'static)`; handles v1 packets and v2 data-stream requests (wired from TextStreamOpened on lk.rpc_request at room/mod.rs:2415-2423).

**7. Active-speaker / activity state**: `room.on("active_speakers_changed")` (energy-based, not transcripts): agent in speakers → `_agent_active_since_mono = now`, `agent_is_active_speaker = True`, `last_agent_activity_mono = now`, `_agent_has_spoken = True`; else `_agent_active_since_mono = None`, flag False; emits "room.active_speakers" {identities}. `agent_active_duration_ms()` = `int((mono_now - _agent_active_since_mono)*1000)` or None. `agent_replied_this_turn`: user final sets False + turn += 1; agent final sets True. `agent_has_spoken` flips via EITHER active-speaker energy OR any agent transcript (interim or final) — gates never hang on missing transcripts. Rust: `RoomEvent::ActiveSpeakersChanged` (identity list); same energy semantics.

### Appendix G: Asserts / suite / midcall / nudge / interrupt / evals / judge / config / preflight contracts

**1. Asserts** (asserts.py — exact params + `assert.verify` event spec): tool asserts (`tool_called`/`tool_not_called` with name/calls/times?), transcript asserts (`transcript_contains`/`agent_said`/`user_said` with text/mode/turn windows), SIP asserts (`sip_participant`/`sim.leg_error` context), `goals_met` with `min_goals: int = 0` (default `min_goals=1` from `int(raw.get("min_goals", 1))`). **Heuristics (REVISED — port verbatim from asserts.py):**
- **`backchannel_agent_continued`** (outcome type, asserts.py:514-545): collect `bc_cues` = ts of every `sim.script.cue` with `spec.class == "backchannel"`; empty → pass=True + `skipped: True, reason: "no backchannel cues in run"`. Else `first_bc = bc_cues[0]`; `agent_after = [t for t in agent_final_ms if t > first_bc + 100]`; `continued = len(agent_after) >= 1`; `tool_near = count of events with kind in ("tool.start", "sim.script.cue") AND ts in [first_bc-2000, first_bc+5000]`; `tool_near > 5` → `continued = False`; check payload `{continued, agent_finals_after: len(agent_after)}`.
- **`constraint_respected`** (outcome type): caller must NOT leak forbidden phrases/patterns — `must_not` list of substrings or regexes matched against CALLER transcript (user finals + interim); any match → fail; regex mode via `spec.mode == "regex"`. Missing `must_not` → ValueError (`outcomes[i] constraint_respected needs must_not`).
- **`ended_by`** mapping: `sim_hang_up` → `"sim_hang_up"`; else `end_condition.reason`; else `end_call_token`; precedence exactly that order (REVISED).
- **`false_positive`** (interruption assert): `spec.get("false_positive")` — an `interruption` event with `false_positive: true` counts as pass for the "noise/backchannel treated as non-barge" case.
- **OutcomeExpect defaults** (asserts.py:181): `{"end_condition", "goal_met", "goals_met", "constraint_respected", "backchannel_agent_continued", ...}` — port the full allowed set + default `expect` value. **`judge_goals` is NOT a PassCriteria field** — driven by AssertSpec outcome type "goals_met". NO `min_confidence`/`must_include`/`must_avoid` anywhere — do not port them. Hard-assert semantics: `evaluate_asserts` runs only when `status == "done" and scenario.asserts is not None and not scenario.asserts.empty`; `assert_result.pass == False` → status flipped to "failed", `meta["assert_failed"] = True` (run_orchestrator).

**2. Suite gate** (`evaluate_run_result(result, strict_judge)` — hard-reason ORDER matters for reports): hard = not_executed (+error), validation_failed, status != "done", assert_verify.pass false (unless skipped True), script_verify.pass false; judge: "fail" → soft "judge_fail", hard ONLY if strict_judge; "maybe" → soft "judge_maybe", hard only if strict_judge; "error" → soft "judge_error", NEVER hard (even strict); "skipped" → ignored. `ok = no hard reasons`. gate ∈ {"hard","soft","pass"}. pass@k: `k = pass_at_k or repeat`; `ValueError("repeat must be >= 1")`; `ValueError` if k > repeat; `ok = hard_passes >= k`; ops path computes gate WITHOUT strict_judge.

**3. Midcall / nudge / interrupt** (full contracts — P2 bootstrap slice + P3 completion):
- `build_persona_system_instruction` facade: `persona=dict(persona or {}), locale=str(locale or "en-US"), context=dict(context or {}), script_steps=list(script_steps or []), first_speaker=str(first_speaker or "agent")`; `policy = policy or DefaultCallerPolicy()`; returns `policy.build_system_instruction(ctx)` with `CallerPolicyContext{persona, locale, context, script_steps, first_speaker}`. The SAME policy instance drives SI + midcall cues in the orchestrator.
- `MidcallCue` is text-only (NO delay_ms/silence_after_cue_ms/trigger). Kinds "bootstrap" | "reground" | "custom". `DefaultCallerPolicy.midcall_cues` order: bootstrap (first_speaker=="user" AND NOT script_owns_opening) → goal_reground (goals non-empty) → script_no_early_bye (script_steps). `script_owns_opening` = any step with trigger in {time, silence, None} AND action in {None, "speak"} AND bool(say); dict-or-attr field access; hang_up/wait does NOT own the opening. Bootstrap text by verbosity: quiet → "greet briefly and state why you are calling in one short clause"; chatty → "greet and state why you are calling in a natural opening turn"; else → "greet briefly and state why you are calling in one natural turn"; payload `(The call just connected. You speak first per PERSONA: {open_hint}.)`. Goal reground: `g0 = goals[0][:120]` (code-point slice, no ellipsis, goal 1 only); text `(Stay on your caller goals. Current focus: GOAL 1 — {g0}. Do not end the call early. Do not switch into assistant mode.)`. script_no_early_bye: `(Timed Script overlay is active. Do not say bye / goodbye / [END_CALL]. Between cues, {between} the simulator will hang up.)` — literal `[END_CALL]`, em-dash. `sim.caller_midcall` spec `{kind, label (may be None), text: text[:240]}` source="sim" include_dialogue=False. Bootstrap sent at connect via `send_realtime_input(text=text)` (Gemini) / `_user_text_item(text)` + `response.create` (OpenAI). `inject_reground(label=None)`: FIRST cue kind=="reground" wins (only ONE per call), no-op if session/ws None; label param overrides the cue's label.
- **PromptSection composition** (10 sections, deterministic, golden-testable): order RoleSection, GoalsSection, StyleTraitsSection, NaturalSpeechSection, ConstraintsSection, SpeechConditionsSection, ContextSection, ScriptTimingSection, FirstSpeakerSection, GuardrailsSection. `render(ctx) -> Vec<String>`; sections may return `[]` (no blank placeholders); join ALL lines with `"\n"` (no trailing newline); blank lines appear INSIDE sections only. All fixed template strings byte-exact incl. unicode (en-dashes, curly quotes, `→`, `…`). Key rules: GoalsSection [] if no goals; NaturalSpeechSection [] if verbosity=="quiet"; ConstraintsSection [] if no constraints; SpeechConditionsSection [] if speech_conditions empty; ContextSection NEVER injects author `notes`; ScriptTimingSection counts `_step_overlay(step)=="fixture"` vs `"line"` (effective_overlay).
- `resolved_verbosity` source order: (1) `speech_conditions.verbosity` non-empty, key lower().replace("-","_"), must be in {quiet, natural, chatty} else debug-log "unknown speech_conditions.verbosity=%r; using natural" and fall through; (2) trait keys normalized, "chatty" in keys → chatty; (3) keys ∩ {quiet, silent, terse} → quiet; (4) else natural. NOTE: quiet traits LOSE to chatty (chatty checked first).
- `expand_traits`: 17-trait library, each → 1–2 instruction bullets (verbatim); normalize key lower/space→_/hyphen→_; dedupe first-occurrence-wins; unknown tags → one line `Additional caller behavior (follow naturally): {joined}.` preserving original casing. `list_trait_ids` = sorted keys.
- `neutralize_style_length_hints(style, verbosity)`: `_STYLE_LENGTH_CONFLICTS` = exactly 9 phrases ("short turns","terse replies","brief replies","brief answers","one-word answers","one word answers","keep it short","keep replies short","keep answers short"); strip ALL occurrences case-insensitively; only for natural/chatty (quiet keeps verbatim); tidy: split on "," after ";"→"," replace, `strip(" ,;")` per part, drop empties, rejoin ", ".
- **Silent mode**: `silent_mode_enabled(sc)`: `sc.get("silent_mode", sc.get("silentMode", sc.get("silent")))`; True or ==1 or str in ("1","true","yes","on","silent") → True. Effects: `midcall_cues=[]`; `sim.silent_mode` `{enabled: True, note: "Caller stays mute: no freestyle, no nudge, no auto barge/noise"}` source="sim"; `meta["silent_mode"]=True`; nudge skipped (`sim.agent_greeted_nudge_skipped` reason "silent_mode") or task never created; interrupt-rate None (validation STILL runs first so bad values fail); explicit steps filtered to `action in (wait,hang_up) and not barge_in and delivery != "room_pcm"` (fallback: wait/hang_up only); hang_up steps with say/asset: say="", delivery="gemini_text", asset=None, barge_in=False, with_blip=False, gain=1.0, loop=False; `inject_cue` blocked → `sim.silent_mode_skip_inject` `{label, delivery, text: text[:120]}`; SI gets the two SILENT MODE lines (SpeechConditionsSection + FirstSpeakerSection); `silence_ms < 500` under silent → `silent_hold_ms or 12000`; `_persona_audio_allowed` False unless `_script_hangup_farewell`.
- **caller_nudge.rs**: `AGENT_GREETED_NUDGE = "(The agent has finished greeting you. Respond now in the language of your persona.)"` — NON-AUDIBLE (Gemini: activity_end only, no text; OpenAI: `input_audio_buffer.commit` + `response.create`, event_id `evt_nudge_{int(time.monotonic()*1000)}`). Gate: first_speaker != "agent" → return (no event); silent → `sim.agent_greeted_nudge_skipped` {reason: "silent_mode"}. Loop poll 0.15 s: `agent_has_spoken and not user_has_spoken` → sleep 1.0 s debounce → re-check end_call/user_has_spoken (abort silently) → `nudge_freestyle_answer(agent_hint=...)`; success → `sim.agent_greeted_nudge` {debounce_s: 1.0, audible: False}, return. RuntimeError → sleep 0.15 s retry. Task created ONLY when `run.first_speaker == "agent" and not scenario.script_steps and not _silent`.
- **interrupt_rate.rs**: `INTERRUPT_RATE_INTERVALS_MS = {none: None, low: 90_000, medium: 45_000, high: 30_000}`; `MIN_INTERVAL_MS=1000`; `DEFAULT_SAY="Sorry — one second —"`; `DEFAULT_MIN_AGENT_ACTIVE_MS=700`. `parse_interrupt_rate(persona)`: raw = speech_conditions.interruption_rate or interrupt_rate; None or in ("","none","off","0","false") → None; not in map → ValueError `Persona.speech_conditions.interruption_rate must be one of none|low|medium|high (got {rate!r})`; `interruption_interval_ms` override int() >= 1000 else ValueError `…interruption_interval_ms must be >= 1000 (got {v!r})`; gain 0..1 else ValueError; `normalize_interrupt_class(interruption_class or "correction", barge_in=True)` with wrapped `Persona.speech_conditions.interruption_class: {e}`; silent → None (validation first); delivery "room_pcm" if asset else "gemini_text"; default_blip = (not voice-asset) if asset else True; `min_agent_active_ms = max(100, int(interruption_min_agent_active_ms or 700))`. Runner: `sim.interrupt_rate` ONCE (spec keys exactly: rate, interval_ms, class, say, asset, delivery, min_agent_active_ms; source="sim.interrupt_rate", include_dialogue=False). Loop poll 0.05 s: wait for `agent_has_spoken` (silent loop); arm on first active burst and re-arm when agent_active and not previously active; `recently_spoke = last_agent_activity within 3.0 s`; `agent_active = agent_is_active_speaker or recently_spoke`; interval from `_last_fire_mono or _armed_mono`; if elapsed but not active → `sim.interrupt_rate_skip` {reason: "agent_not_active", active_ms}; if active_ms < min → skip {reason: "active_ms_below_min", active_ms, min}. `_fire`: step_id `rate-barge-{n}` from 1; blip only when with_blip && delivery != "room_pcm" — inject "[barge blip]" label `{step_id}-blip` delivery room_pcm asset "builtin:noise.blip"; any Exception → `sim.script.error` (keep alive); main inject `inject_cue(spec.say, label=step_id, delivery, asset, gain, loop=False)`; interruption event ONLY if `agent_is_active_speaker` at fire AND no inject error: `{by: "sim", barge_in: True, class, false_positive: class in (noise, backchannel), step_id, label, say, note: "Interrupt-rate barge while agent was active speaker"}`; `sim.script.cue` ALWAYS per fire incl. error field (may be None) with the full key set (step_id, label, say, trigger "agent_speaking", action "speak", barge_in True, class, overlay "fixture", delivery, asset, gain, loop False, waited_ms, hold_silence_ms 0, agent_active, agent_active_ms, during_agent_speech, rate, interval_ms, fired, error). Runner exists iff parse returns non-None; scenario_dir = scenario.path.parent if exists else config.scenarios_dir; task "interrupt-rate". Policy is additive to authored script.
- **behavior_compile.rs**: `compile_from_speech_conditions` (auto-ambient id "auto-ambient" trigger time delay `int(noise_delay_ms or after_join_ms or 5000)` say "[ambient]" delivery room_pcm asset noise gain 0..1 (ValueError `Persona.speech_conditions.noise_gain must be between 0.0 and 1.0`) once True loop from when/loop/noise_loop interrupt_class "noise"; auto-barge-1 id "auto-barge-1" trigger agent_speaking delay/min_agent_active_ms `max(100, after//2)` after `int(barge_after_agent_ms or after_agent_ms or 600)` say default "Sorry — one second —" delivery room_pcm if asset else gemini_text with_blip gain 0..1 (ValueError `Persona.speech_conditions.barge_gain must be between 0.0 and 1.0`) barge_in True once True; auto-user-silence id "auto-user-silence" trigger time delay `int(silence_arm_ms or 400)` say "" action wait silence_after_cue_ms `int(silence_ms or user_silence_ms or 0)` (silent + < 500 → `int(silent_hold_ms or 12000)`), only if >= 500). `compile_from_behavior_spec`: ambient (delay 5000, gain 0..1, loop when in background/loop/continuous/bed/always or explicit), barge_ins (after 600, delay & min_agent_active_ms `max(100, int(delay_ms or max(150, after//2)))`, room_pcm-without-asset → ValueError, with_blip explicit else False if voice asset else delivery != room_pcm, class normalize with barge_in), backchannels (after 1200, asset default "builtin:voice.backchannel", barge_in False, with_blip False, class "backchannel"), false_interrupts (after 500, asset default "builtin:noise.loud", barge_in True, class "noise", with_blip False), user_silence (hold `int(hold_ms or silence_after_cue_ms or 0)` must be >= 500 else `user_silence[{i}] hold_ms must be >= 500`, arm `int(delay_ms or arm_ms or 400)`, require_agent_spoke_first default True). `merge_script_steps`: explicit wins on id, compiled fills gaps. `default_verify_for_compiled`: if auto barge/silence added and no explicit verify → ScriptVerifySpec(require_during_agent_speech=False, min_agent_finals_after_barge_in=1 if has_barge else 0, min_agent_finals_after_silence=0, min_agent_finals_after_first_cue=0). `apply_caller_behavior` always runs speech-conditions compile; behavior spec extends only when not silent; silent → only wait/hang_up non-barge steps from behavior spec. `_is_voice_asset`: strip "builtin:" and leading "@", lower, starts_with "voice.".

**4. Judge / evals** (full contract — resolution, wire, prompts, verdict normalization, JSON repair, goals gate, aggregation, relevancy, evidence, presets): see P7 steps 1–13 above (inlined there; this appendix references them — the P7 phase text is the contract).

**5. Config** (`_require` semantics, bool coercion, provider/mode/endpoint coercion, config_snapshot key ORDER, YAML digit-quoting, telephony merge): see P1 steps 1–5 + 10 (inlined there) and Appendix A.

**6. Preflight**: check ORDER + names + early-return semantics + telephony bits: see P2 step 12 (inlined there).

### Appendix H: Edge-case catalog

> 63 rows, CI-gated offline tests. The full table lives in §Test strategy (inlined above); every row pins a behavior from Appendices A–G. Test-name column there is the Rust test. Headings covered: E1–E63 — empty logs, agent-join timeout, dead call, hang-up-before-speech, barge during say, unmatched agent_speaking trigger, DTMF during speech, silent mode end-to-end, first_speaker matrix (user/agent × script-owns-opening × wait-only), goal reground truncation, empty goals, trait conflicts, unknown trait/verbosity/rate/interval/gain, nudge retry, interrupt-rate waits/blip failure, transcript dedupe collisions, YAML shadowing/duplicates/invalid ids/persona-brief, judge JSON repair/all-error/mixed/not-configured/timeout/empty transcript/parse, repeat/pass_at_k validation, empty script, strict_judge without criteria, plugin missing, wav-without-agent-audio, Ctrl+C 130, transport drop, SIP timeout, hold timeout, room full, run_id collision, empty scenario file, BOM/CRLF, YAML 1.1 traps, multi-doc merge, goals_met score, relevancy matrix, optimize no-winner/missing-artifact, preflight config fail, `_require` zero values, `"false"` string traps, tool_event_patterns filtering.

---

*Companion research: `docs/rust-port-research.md` (all crate versions, API claims, VERIFY items, and evidence paths). Porting checklist: this plan (self-contained) + real-run fixtures (R5). Python source remains the ultimate tie-breaker for any ambiguity; the appendices above capture its behavior.*

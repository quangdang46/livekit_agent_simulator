[package]
name = "agent-sim-proto"
description = "Decode the lk.agent.session byte-stream protocol (AgentSessionMessage) — minimal hand-written prost-compatible subset."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde_json = { workspace = true }

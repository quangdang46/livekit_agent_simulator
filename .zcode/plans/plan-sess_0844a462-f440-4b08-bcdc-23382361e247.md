# Plan: làm lksr 1:1 với lks (Python) + xoá TUI

**Mục tiêu:** cùng một `.agent-sim/` + scenario → lksr cho cùng verdict, cùng events.jsonl, cùng report artifacts như lks; đồng thời xoá `lksr tui` (dead feature). Commit theo từng phase; sau mỗi phase `cargo test --workspace` xanh + `uv run pytest -q` vẫn xanh (không sửa Python).

**Gap đã xác định (file:line tra cả 2 phía):** asserts deterministic bị skip; pending/judge merge chưa có; builtin judge presets chưa expand; grounding fix #99 chưa port; `must_not_match` là substring thay vì regex; không emit `tool.*`/`data.*` (proto chưa decode `tool_execution_updated`, `DataReceived` bị drop ở 2 caller loops); `defer_on_open_question`/`mute_persona` parse-only; farewell hardcode tiếng Anh; thiếu skip path interrupt_rate; thiếu `dead_call_silence`/`dispatch.agent_timeout`/`sim.error`/`review.md`; web thiếu route alias; optimize proposer chỉ OpenAI; plugins PyO3 chưa default + thiếu before_run/after_run.

---

## Phase 0 — Xoá TUI (dead-feature cleanup, độc lập, làm trước)

1. Xoá `Command::Tui` + dispatch arm (`crates/lks/src/main.rs:297-299, 351`) và toàn bộ module `crates/lks/src/tui/`.
2. Xoá deps chỉ TUI dùng: `ratatui`, `crossterm` khỏi `crates/lks/Cargo.toml` (`cargo update` dọn Cargo.lock).
3. Dọn plumbing chỉ TUI dùng: live-channel streaming trong `lks-core/src/logging/event.rs` (`with_live_channel`…, comment "used by the lksr TUI") + `lks-livekit/src/run.rs` watch-channel; **kiểm tra consumer còn lại trước khi xoá** — nếu `web`/MCP còn dùng thì giữ và chỉ sửa comment.
4. Sửa tests: `lks/tests/` (`all_23_commands_parse` → assert đúng 23 lệnh còn lại), xoá test TUI nếu có; `grep -ri tui README.md docs/ templates/` dọn mentions.

## Phase 1 — Asserts & judge (quyết định pass/fail) — thuần `lks-core`

**Files:** `lks-core/src/asserts.rs`, `evals.rs`, `judge.rs`, mới module presets trong evals; tests `lks-core/tests/`.

1. **OutcomeExpect đủ fields thật** (thay `Json::Null` cứng ở `to_json` 120–159): parse + emit đúng thứ tự key Python-asdict cho `max_turn_p99_ms`, `max_turn_max_ms`, `max_recovery_p50_ms`, `max_ttfa_p50_ms/p95_ms`, `max_turn_audio_p50/p95/p99/max_ms`, `require_turn_samples`, `require_audio_samples`, `check_agent_transcript` (default false).
2. **5 evaluator deterministic** (thay arm `_ =>` skip ở asserts.rs:732), gọi `lks_core::metrics::compute_voice_metrics(events)` (metrics.rs:98, API có sẵn):
   - `latency` — port `_eval_latency_outcome` (asserts.py:611–695): `_gate` (limit None → bỏ; actual None → fail "no sample"; fail khi `val > limit`, bằng thì pass); `require_turn_samples`; `min_barge_recovery_rate` (0 barges → fail "no barges fired"); JSON check có `reasons/actual/limits` y hệt.
   - `ttfa` / `turn_taking_audio` — port `_eval_audio_latency_outcome` (728–807): ttfa 1 sample run-level (gate p50 skip "insufficient samples", p95 dùng sample đó); `require_audio_samples` → hard fail; counts==0 → `skipped:true`.
   - `agent_must_respond` (711–725): ≥1 `sim.agent.audio_onset`, không fallback transcript.
   - `backchannel_agent_continued` (521–550): bc cues = `sim.script.cue` `spec.class=="backchannel"`; guard +100ms; tool-storm guard (tool.start/sim.script.cue trong [first_bc−2000,+5000] >5 → false); không cue → skipped.
   - `ended_by` sửa detection (810–854): sim.hang_up|sim.script.hang_up → sim; `run.end_condition` `sim_end_call` → sim, `agent_disconnected`/`dead_call_silence` → agent, `max_turns`/`timeout` → detect; fallback `sim.end_call_token` → sim; validate ∈ sim|agent|detect.
3. **Regex `must_not_match`** (regex 1.13 đã là dep lks-core): `RegexBuilder` case_insensitive trong transcript loop (blob gồm cả `.interim`, asserts.py:336–353/408–411) + `constraint_respected`; compile-fail → check fail "invalid regex". Thêm `check_agent_transcript` (leak echo, 911–962).
4. **Pending/judge merge** (asserts.py:603–608 + run_orchestrator.py:583–685):
   - `evaluate_asserts` trả thêm `pending_llm_outcomes`; check pending `pass:true, pending_judge:true`, **loại khỏi hard pass**.
   - `llm_bool` → run.rs:1177 append criteria `"[outcome:{id}] {prompt}"` trước `judge_run` (soft only).
   - `goals_met` → thêm `judge_goals` vào judge.rs (`pass = verdict=="pass" && score>=50`, port evals/runner.py:226–259); skipped/error → `assert.goals_met {pass:true, skipped:true}`; else emit `{outcome_id, min_goals, goals, verdict, score, pass, notes}`; fail → status failed + `meta.goals_failed`.
5. **Builtin judge presets**: port 9 rubric verbatim (evals/presets.py:9–77) + `expand_criteria`/`expand_judge_group` (prefix `builtin:`, key lạ → error rõ); hook tại run.rs:1177–1198 trước judge.
6. **Grounding fix #99**: `parse_judgment_payload` (evals.rs:289) — `met=true` + evidence rỗng → `met=false` + cờ ungrounded, `needs_human_review = raw || ungrounded`; `apply_relevancy` (433): promotion fail→pass set `needs_human_review=true` (relevancy.py:44–53).

**Tests P1:** port case từ test_asserts*.py + test_evals_judge.py (latency gates, require_turn_samples, ttfa skip, tool-storm, ma trận ended_by, pending exclusion, goals_met merge, preset expand + unknown-key error, grounding flip, promotion flag, regex).

## Phase 2 — Tool/data observability (hiện asserts `tools`/`tool_order`, metrics tool_*, judge tool evidence đều chết vì 0 tool events)

1. **agent-sim-proto**: decode `tool_execution_updated` (lấy field-tag từ Python `livekit/agent_session_observer.py`); test decode với fixture.
2. **Consume `DataReceived`** trong caller loops (openai.rs:676–744, gemini.rs:314–362 — hiện `_ => {}`): topic `lk.agent.session` → decode → emit `tool.start` (name, payload/args) + `tool.end`/`tool.error` (parent_event_id, duration_ms từ pairing HashMap<call_id,(event_id,start_mono)>) — mirror agent_session_observer.py:343–379.
3. **Module observe chung** `lks-livekit/src/observe.rs` cho cả 2 bridge (mirror observer.py:264–672): filter `data_topics` (rỗng = all, else exact); parse transcript payload (`transcript_payload_types`, non-interim, turn dict → transcript final); `tool_event_patterns` (match.topic + dotted-path equality → tool.start/end/error + pairing duration); `data.message` (parse ok, không phải tool/transcript); `data.raw` (parse fail). Source = topic hoặc "data".
4. **flow_topics → judge**: đảm bảo FLOW EVENTS trong judge packet nhận `data.message` có source ∈ flow_topics.
5. **`sim.audio_recorded`** (run.rs:1214, spec path/sample_rate/duration_ms/channels/sim+agent_samples/t0_mono_ms; không frame → path:null+note) + **`sim.agent_audio_recorded`** (agent audio pump start, spec track_sid/source/sample_rate:16000).

**Tests P2:** decode fixture, pattern matching, pairing duration, topic filter, emit qua EventWriter với synthetic DataReceived.

## Phase 3 — Script runtime behaviors (`lks-livekit/src/script.rs`, `lks-core/src/script/`)

1. **hang_up deferral** (arm hang_up script.rs:204, port script/runtime.py:184–211): `require_agent_reply_this_turn` → defer `awaiting_agent_reply`; `defer_on_open_question` (dùng `agent_left_open_turn` hang_up_gate.rs:52 với `last_agent_final_text`) → defer `open_agent_question`; 1 budget `open_question_idle_ms`; emit `sim.script.hang_up_deferred` lần đầu + lần 2 khi hết budget (`defer_budget_exhausted` + `prior_reason`) rồi cho hang-up.
2. **`mute_persona` runtime**: suppress freestyle caller trong lúc cue (qua bridge API), khớp semantics Python (mute freestyle, không mute cue).
3. **`sim.script.error`**: tại các site `let _ = (self.on_action)(...)` (script.rs:237/261/283/390) — Err → emit {step_id, label, delivery, asset, error} source "sim.script"; cộng nhánh room_pcm error run.rs:525–529.
4. **Farewell theo locale**: port `_DEFAULTS` (en/en-us/en-gb, vi/vi-vn, ja/ja-jp) + `default_hangup_farewell` vào `lks-core/src/script/farewell.rs`; thay hardcode "Thanks, that's all for now. Bye." (script.rs:204–209); locale = `simulator.language` else `voice.language`, thread qua `ScriptRuntime::new`/run.rs.
5. **interrupt_rate skip** (run.rs:746–854, port interrupt_rate.py:205–219): `active_ms < min_agent_active_ms` → `sim.interrupt_rate_skip {reason:"active_ms_below_min", active_ms, min}`; nhánh !active (784–787) → `{reason:"agent_not_active", active_ms}`; đối chiếu re-arm.
6. `overlay`: verification-only — đối chiếu `step_overlay` prompt-side, sửa nếu lệch.

**Tests P3:** deferral state machine với injectable clock, farewell map, skip reasons, script.error emission.

## Phase 4 — Run loop, reports, web, optimize

1. **`dead_call_silence`** (watchdog song song hold watchdog run.rs:687–740): armed sau transcript đầu tiên; im ≥ 3×`observe.silence_threshold_ms`, không scripted silence/hold → end_reason `dead_call_silence`; đối chiếu spec `silence.detected` (1 lần/window).
2. **`dispatch.agent_timeout` + `sim.leg_error`**: emit khi `wait_for_agent_join` fail (dispatch.rs:58–63) / error path sim_leg (sim_leg.rs) — spec {error, mode}, include_dialogue=false.
3. **`sim.error`** tại error points bridge (spec {where, error}, source "sim").
4. **review.md** (event.rs finalize hoặc run.rs sau 1208): port `_render_review` (event_writer.py:250–284): điều kiện render, section multi-judge (`## Judge: <id>`, Overall/Strengths/Findings/Missing/Language/Final Assessment table 5 categories), `*{met}/{total} criteria met.*`, Notes; render test.
5. **web**: thêm `.route("/api/runs/{run_id}", get(api_cues))` — Python `/api/runs/<id>` trả cùng cues payload (server.py:180–191).
6. **optimize proposer Gemini** (ops_execute.rs:783 `propose_llm`): mode gemini → dựng `GeminiRestBackend` như judge.rs:599–666; giữ "proposer lỗi không hỏng run".
7. **Verify handshake retry Gemini**: 3 attempts + emit `sim.gemini_socket_drop` (callers/gemini.py:620–660) — port nếu thiếu.
8. **Cosmetic**: surface `Context.notes` trong export_scenario (author-only, không inject).

## Phase 5 — Plugins PyO3 default

1. **Audit `lks-plugins`/plugin_bridge.rs**: verify hook merge `plugin_results` vào script_verify như Python; bổ sung **before_run** (sau prepare, trước SimLeg connect) + **after_run** (sau finalize) với context mirror Python api.py (BeforeRunContext{scenario, project_root, run_id, run_name, meta, dispatch_metadata, options}; AfterRunContext thêm report_dir/status/summary/events/verdict).
2. **Default feature**: `lks-core` Cargo.toml `default = ["python-plugins"]`; giữ no-default cho build tối thiểu; thiếu libpython runtime → lỗi rõ.
3. **Build/CI/install**: install.sh + CI release build với PyO3 (python3 dev headers); ghi chú vào README/GUIDE.
4. **Tests** (feature-gated): chạy example_verify.py trong temp `.agent-sim`, assert plugin_results + before/after hook được gọi.

## Ngoài scope
Update check qua GitHub Releases (đúng theo installer); cơ chế serve SPA rust-embed; YAML 1.1 emulation (đã xong).

## Verification & definition of done
- `cargo test --workspace` xanh (mỗi phase có tests parity mới, fixture mirror từ pytest); `uv run pytest -q` xanh.
- Parity harness: script nạp cùng synthetic events.jsonl vào `evaluate_asserts`/`compute_voice_metrics` cả 2 bên, diff JSON (`scripts/parity_asserts.py` + test Rust đọc cùng fixture).
- `lksr tui` không còn tồn tại trong help/binary.
- README/GUIDE cập nhật (PyO3 requirement, bỏ TUI).

**Rủi ro:** thứ tự key JSON là contract (serde preserve_order); khác biệt nhỏ Regex vs re.I (unicode); PyO3 tăng binary size + cần python3 headers trên CI; clock injection cho test deferral.